# 2026-03-25

## 02:35 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, yesterday's memory, current git history, and the recent #krypto Discord context before choosing work.
- Explicit top-down check: the last 3-5 sessions spent most of their time on trend-benchmark refinement / trust cleanup, with only one deliberate broaden-the-search session. That was no longer the highest-value use of time, so I biased this cycle toward **Track C — Broaden edge discovery**.
- Built successfully with `cargo build`.
- Added a new breadth-stress harness: `krypto/examples/cross_sectional_momentum_stress.rs`.
  - Purpose: stress **CrossSectionalMomentum** — the only non-trend family that looked competitive — against the current trend yardsticks on harsher baskets.
  - Same execution assumptions for all candidates: **signal at close, next-open entry, 21-bar hold, 0.1% taker each side**.
  - Stress universes tested: `Base5`, `NoDOGE`, `Legacy4`, `Legacy5BNB`, `OldGuardNoBNB`, `LargeCaps5`.
- Ran `cargo run --example cross_sectional_momentum_stress --profile sweep 2>&1`.
- Key findings:
  - Base modern-alt basket (`ETH/SOL/XRP/DOGE/ADA`): `CrossSectionalMomentum` stays competitive at **+3171.7%**, **514 trades**, **4/4** quarter windows, **14/15** resamples — but still trails `MACD+Regime` (**+3624.5%**, **15/15**) and plain `MACD` (**+3220.3%**, **15/15**).
  - Legacy stress weakens it materially:
    - `Legacy4`: **+1083.2%**, **11/15** resamples
    - `Legacy5BNB`: **+1657.3%**, **12/15** resamples
    - `LargeCaps5`: **+1659.4%**, only **2/4** quarter windows, **12/15** resamples
  - Harsher no-BNB old-guard basket (`ETH/XRP/LTC/EOS/BCH`) is mixed:
    - `CrossSectionalMomentum`: **+1400.4%**, **4/4** quarter windows, **13/15** resamples
    - Still behind `MACD+Regime` on resample quality (**15/15**) and behind plain `MACD` on raw return (**+1733.9%**)
  - Universe win count across all 6 stress baskets:
    - `MACD+Regime`: **5/6**
    - `MACD`: **1/6**
    - `CrossSectionalMomentum`: **0/6**
- Honest read: Cross-sectional momentum is **not dead**, but the first breadth result now looks more like a **secondary candidate / partial alt-beta expression** than a genuinely equal second family. It survives, but it does not dominate once the basket gets older or more large-cap / legacy-skewed.
- Overfitting / trust risk: this family still may be borrowing the same trend exposure as the current leaders rather than offering a truly orthogonal edge.
- Next step: either (a) add attribution / market-beta decomposition for `CrossSectionalMomentum`, or (b) test a more orthogonal breadth idea (funding / carry / leader-laggard) instead of continuing to polish trend-family variants.

## 05:33 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, current git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check: the last 3-5 sessions were dominated by MACD-family benchmark refinement plus one cross-sectional breadth stress. That was useful, but still not the highest-value use of time once `CrossSectionalMomentum` failed to win any harsher basket.
- Chose **Track C — Broaden edge discovery** again, but this time toward a genuinely more orthogonal family rather than more work on the same cross-sectional variant.
- Built successfully with `cargo build`.
- Re-ran the existing orthogonal structural family check with `cargo run --example funding_rate_backtest --profile sweep 2>&1`.
- Key findings from the strict funding-rate walk-forward harness:
  - `BTCUSDT` funding reversion remains clearly bad: **1h -21.2%**, **0/1** robust windows; **4h -25.9%**, **0/1**
  - `ETHUSDT`: **1h -7.6%**, **0/1**; **4h -8.4%**, **0/4**
  - `SOLUSDT`: **1h no valid windows**; **4h +1.9%**, **0/1**
- Honest read: the current **contrarian extreme-funding mean-reversion** implementation still looks dead / underpowered, not like a hidden structural winner we’ve been neglecting.
- Overfitting / trust read: this was still a useful cycle because it checked a more orthogonal family and prevented recent trend-family focus from becoming an unchallenged default.
- Next step: move breadth work to a different orthogonal family — most likely **leader-laggard / cross-asset propagation** (or basis if better data becomes available) rather than spending another cycle on this exact funding-reversion setup.

## 09:11 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, current git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check: the last 3-5 sessions spent most of their time on benchmark-trust cleanup plus breadth probes in cross-sectional momentum and funding. That made **Track C** still the right focus, but only if I tested a genuinely different family rather than circling back to MACD.
- Built successfully with `cargo build`.
- Added a dedicated propagation-family stress harness: `krypto/examples/leader_laggard_stress.rs`.
  - Compared four daily leader-laggard variants under the same fair assumptions (signal at close, next-open entry, 21-bar hold, 0.1% taker):
    - `LeadLag BTC fast` (`2.0%` leader shock, `0.4%` max self move, `decay=2`)
    - `LeadLag BTC base`
    - `LeadLag BTC slow` (`3.0%`, `1.0%`, `decay=5`)
    - `LeadLag ETH base`
  - Stress universes: `Base5`, `NoDOGE`, `Legacy4`, `Legacy5BNB`, `OldGuardNoBNB`, `LargeCaps5`
- Ran `cargo run --example leader_laggard_stress --profile sweep 2>&1`.
- Key findings:
  - Best modern-alt read: **`LeadLag ETH base` on Base5 = +1953.0%, 295 trades, 15/15 resamples`** — materially better than the earlier one-line `LeadLag` scan, but still far behind `MACD+Regime` (**+3624.5%, 15/15**) and behind `CrossSectionalMomentum` (**+3022.3%, 13/15**) in this harness.
  - The sharper BTC-triggered variants had some respectable legacy-basket prints:
    - `Legacy4`: `LeadLag BTC fast` **+1081.8%, 13/15** vs `MACD+Regime` **+1038.3%, 14/15**
    - `Legacy5BNB`: `LeadLag BTC slow` **+1755.9%, 13/15** vs `MACD+Regime` **+1829.9%, 14/15**
  - But none of the propagation variants actually won a stress universe; `MACD+Regime` finished **6/6** on universe wins.
- Honest read: daily leader-laggard / propagation is **not dead**, but under current daily fixed-hold assumptions it still looks like a **secondary breadth family**, not the next main edge candidate.
- Overfitting / trust read: this was still a good use of time because it prevented the project from dismissing propagation on one weak default run — but the fairer rerun still did not justify another immediate cycle on this family.
- Next step: pivot breadth work again toward a more structural orthogonal family — most likely **basis / carry-spread structure** or **beta-neutral funding spreads** — rather than keep tuning daily propagation thresholds.

## 12:43 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, current git history, and the recent #krypto Discord context before choosing work.
- Explicit top-down check: the last 3-5 sessions were mostly **Track C breadth work** (cross-sectional stress, funding recheck, leader-laggard propagation), so **Track A — Trust the lab** was now the under-served track.
- Built successfully with `cargo build`.
- Added a new trust-audit harness: `krypto/examples/trend_portfolio_realism_audit.rs`.
  - Purpose: keep the same daily trend benchmark assumptions (**signal at close, next-open entry, 21-bar hold, 0.1% taker**) but evaluate the current leaders on a **time-aligned equal-weight portfolio basis** instead of mainly summed per-trade returns.
  - Audit metrics: aligned portfolio return, Sharpe, max drawdown, average / peak concurrent positions, idle-capital days, and the gap vs the usual trade-sum headline.
- Ran `cargo run --example trend_portfolio_realism_audit --profile sweep 2>&1`.
- Key findings on `BTC/ETH/SOL/XRP/DOGE/ADA`:
  - `MACD+Regime`: **+4101.4% aligned**, **Sharpe 1.28**, **MaxDD 60.0%**, trade-sum **+3740.3%** (**+361.1% gap**)
  - `Turtle+Regime+MACD`: **+2350.9% aligned**, **Sharpe 1.43**, **MaxDD 40.6%**, trade-sum **+2931.6%** (**-580.7% gap**)
  - `Turtle+Regime`: **+1851.3% aligned**, **Sharpe 1.31**, **MaxDD 43.6%**, trade-sum **+2922.8%** (**-1071.5% gap**)
  - `MACD`: **+1196.7% aligned**, **Sharpe 1.06**, **MaxDD 61.5%**, trade-sum **+3002.9%** (**-1806.2% gap**)
- Concurrency / capital-usage read:
  - `MACD` is almost always loaded (**avg 5.40 / 6 active symbols**, only **10.0%** idle days), which helps explain why simple trade-sum headlines flatter it so much under portfolio realism.
  - `MACD+Regime` uses less of the book (**avg 3.68 active symbols**) but converts that exposure into the strongest aligned portfolio outcome.
- Honest read: this is a real trust lesson. **Summed per-trade returns materially overstate plain MACD**, while `MACD+Regime` survives the portfolio-realism audit much better. On a risk-adjusted aligned basis, `Turtle+Regime+MACD` may now be the cleaner candidate on the base basket.
- Overfitting / trust risk: current benchmark ranking still depends too much on which portfolio lens we choose. A strategy should not be called “best” until it survives both **survivorship stress** and **realistic portfolio-construction / capital-occupancy** scoring.
- Next step: extend the portfolio-realism audit to legacy / stress universes and add a position-capped / capital-budgeted variant.

## 15:18 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check: the last 3-5 sessions were mostly **Track C breadth probes** (cross-sectional stress, funding recheck, leader-laggard stress) plus one **Track A** portfolio-realism audit. That meant the highest-value next step was to **extend the lab-trust audit**, not do another nearby edge scan.
- Built successfully with `cargo build`.
- Added a new stress-audit harness: `krypto/examples/trend_portfolio_stress_audit.rs`.
  - Purpose: extend the base aligned-portfolio audit across **stress universes / legacy baskets** and add a **top-3 strength-capped portfolio lens** instead of assuming we can always hold every active signal.
  - Stress universes tested: `Base6`, `NoDOGE`, `Legacy5`, `LegacyCore4`, `LargeCaps6`, `OldGuard6`.
  - Portfolio lenses per universe:
    - **uncapped equal-weight** across all active positions
    - **top-3 strength-capped book** to approximate realistic concentration / capital limits
- Ran `cargo run --example trend_portfolio_stress_audit --profile sweep 2>&1`.
- Key capped-book findings:
  - `Base6`: `Turtle+Regime+MACD` leads at **+681,746.3%**, **Sharpe 8.09**, **MaxDD 32.9%**; `MACD+Regime` falls to **+283,616.8%**, and plain `MACD` collapses to only **+3,867.6%**.
  - `NoDOGE`: `Turtle+Regime+MACD` still leads (**+114,740.7%**, **Sharpe 7.25**), ahead of `MACD+Regime` (**+40,271.4%**).
  - `Legacy5`: `MACD+Regime` becomes the cleanest capped leader at **+10,494.0%**, **Sharpe 3.63**.
  - `LegacyCore4`: `Turtle+Regime+MACD` leads (**+13,606.5%**, **Sharpe 4.01**), while `MACD+Regime` overtakes plain `MACD` under the capped lens.
  - `LargeCaps6`: `Turtle+Regime+MACD` leads again (**+27,443.0%**, **Sharpe 4.54**); plain `MACD` drops to **+2,497.4%**, **Sharpe 3.28**.
  - `OldGuard6`: `MACD+Regime` is the capped winner (**+6,824.7%**, **Sharpe 4.15**), ahead of `Turtle+Regime+MACD` (**+3,991.4%**) and plain `MACD` (**+1,833.8%**).
- Honest read: once survivorship stress and realistic book concentration are combined, **plain MACD looks much less trustworthy**. The more honest split is now:
  - **modern / broader baskets:** `Turtle+Regime+MACD` is the strongest portfolio candidate
  - **harsher old-guard basket:** `MACD+Regime` survives best
- Overfitting / trust risk: the benchmark story still depends materially on universe composition, and uncapped portfolio lenses can flatter strategies that rely on broad simultaneous exposure. A candidate should not be called “best” until it survives both **selection stress** and **capital-concentration realism**.
- Next step: add an even harsher **old-guard basket without BNB** and rerun this capped-book portfolio audit before widening search again toward basis / carry structure.

## 18:20 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check: the last 3-5 sessions spent most of their time on **Track A trust / portfolio-realism work** with earlier breadth already covered. That still made the unfinished **no-BNB old-guard stress** the highest-value remaining item, because it closes a concrete survivorship / composition question instead of drifting into another nearby trend tweak.
- Built successfully with `cargo build`.
- Extended `krypto/examples/trend_portfolio_stress_audit.rs` with a harsher legacy basket:
  - `OldGuardNoBNB = BTCUSDT, ETHUSDT, XRPUSDT, LTCUSDT, EOSUSDT, BCHUSDT`
  - Purpose: remove **BNB** so the previous old-guard result is not flattered by exchange-token support.
- Ran `cargo run --example trend_portfolio_stress_audit --profile sweep 2>&1`.
- Key capped-book findings on `OldGuardNoBNB`:
  - `MACD+Regime`: **+3324.7%**, **Sharpe 4.60**, **MaxDD 40.9%**
  - `Turtle+Regime+MACD`: **+3111.1%**, **Sharpe 3.86**, **MaxDD 54.0%**
  - `MACD`: **+1935.4%**, **Sharpe 3.60**, **MaxDD 60.1%**
  - `Turtle+Regime`: **+1073.6%**, **Sharpe 2.91**, **MaxDD 67.7%**
- Comparison vs the earlier `OldGuard6` result:
  - removing BNB does **not** rescue plain `MACD`
  - `MACD+Regime` remains the cleanest old-guard / survivorship-harsher candidate under realistic concentration
  - `Turtle+Regime+MACD` stays competitive on return, but with materially worse drawdown than `MACD+Regime`
- Honest read: the old-guard split now looks less like exchange-token bias and more like a real regime / composition split. The durable picture is still:
  - **broader modern baskets:** `Turtle+Regime+MACD`
  - **older legacy / survivorship-harsher baskets:** `MACD+Regime`
- Overfitting / trust risk: we are still learning that the “best” trend candidate depends on basket composition and portfolio lens. That is progress in trust, but it is still a warning against quietly promoting a single daily trend leader.
- Next step: shift back to **Track C breadth** and test a more structural orthogonal family (basis / carry-spread structure or beta-neutral funding spreads) rather than spend another cycle polishing nearby trend variants.

## 21:21 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check: the last 3-5 sessions were split between **Track A trust / portfolio-realism audits** and several **Track C breadth probes**. The under-served gap was not another trend refinement; it was a more structural breadth test using the funding machinery we already have.
- Built successfully with `cargo build`.
- Added a new breadth harness: `krypto/examples/funding_spread_carry.rs`.
  - Purpose: test a first simple **beta-neutral funding-spread / carry-dispersion** idea rather than the already-failed single-asset funding-reversion setup.
  - Universe: **BTCUSDT / ETHUSDT / SOLUSDT** on **4h** bars with aligned Binance funding-rate z-scores.
  - Signal: at each close, **long the most negatively funded asset** and **short the most positively funded asset** when both are extreme enough and the cross-asset funding spread is wide enough; enter at next open, exit after a fixed **1 / 3 / 6 bar** hold.
  - Costs: **0.1% taker each side**, both legs included.
- Ran `cargo run --example funding_spread_carry --profile sweep 2>&1`.
- Key findings:
  - Best chronology-first configuration was only **entry_z 1.0 / spread_z 2.5 / hold 6 bars**:
    - **-0.9%** full return
    - **58 trades**
    - **50.0%** win rate
    - **2/4** positive quarter windows
    - **5/15** positive resamples
  - Nearby variants were mostly worse:
    - `1.0 / 2.0 / 6`: **-4.5%**, **68 trades**, **2/4**, **5/15**
    - `1.0 / 3.0 / 6`: **-7.9%**, **33 trades**, **1/4**, **3/15**
    - shorter-hold variants generally lost more sharply, suggesting noise + fee drag rather than clean carry capture
- Honest read: this was the right Track C cycle, but the result is still a **kill / park**, not a new candidate. The funding/carry theme may still matter, yet this naive cross-asset dispersion expression is too weak and unstable to earn more immediate polish.
- Overfitting / trust risk: even a structurally sensible idea can die quickly once you require chronology consistency and realistic costs; that is a useful corrective against chasing “orthogonal” stories for their own sake.
- Next step: if basis/carry stays on the menu, it should be via a **richer construction** (realized funding-capture accounting, perp-vs-spot basis, residualized / beta-neutral spreads, or event-conditioned carry), otherwise move breadth to another genuinely structural family rather than revisiting this exact rule.
