## [19:01 UTC] Cron — S17: Chandelier REMOVED — Degrades Sharpe by -1.47

**Session:** 2026-04-27 19:01 UTC | Kira cron
**Trigger:** Cron research loop

---

## Orientation
- Read PLAN.md, MEMORY.md, today/yesterday memory
- Git: up to date, pulled. Last 5 commits from prior session.
- Discord: no new Noah messages. Chart-only cron updates.

---

## S17: Turtle-Only vs Turtle+Chandelier — DEFINITIVE RESULT

**Context:** Prior session found S17 harness but didn't run it. The S17 run was already on disk in `examples/s17_turtle_only_vs_chandelier.rs` (uncommitted). This session ran it.

**Production params:** EP=21, CHAND_P=7, M=2.30, ATR_P=24, M=2.0, HM=12

**S17 Results (9 universes × 6 windows, 54 total):**

| Metric | Turtle+Chandelier | Turtle-Only | Delta |
|--------|-------------------|------------|-------|
| Pass rate | 40/54 (74%) | 39/54 (72%) | -1 |
| Avg Sharpe | **3.11** | **4.58** | **+1.47** |
| Total trades | 715 | 658 | -57 |
| Chandelier fires first | 336/336 (100%) | — | — |

**Key insight:** With CHAND_P=7/M=2.30 (current production), Chandelier fires FIRST in 100% of trades. It's not a dual exit — it's a single Chandelier exit. Turtle ATR(24,2.0) never gets to be the binding constraint. This means:
1. Turtle ATR is completely shadowed by Chandelier with current params
2. Chandelier is adding risk without adding exit quality
3. Removing it gives +1.47 Sharpe with 1 fewer fail window

**Changes deployed:**
- `src/live/bot.rs`: `check_dual_exit` → `check_turtle_exit`, Turtle-only exit
- `examples/live_turtle_chandelier.rs`: Updated to Turtle-only, dual-exit removed
- `src/live/config.rs`: CHAND_PERIOD/CHAND_MULT marked REDUNDANT
- `examples/turtle_chandelier_walkforward.rs`: USE_CHANDELIER=true for reporting
- `examples/s17_turtle_only_vs_chandelier.rs`: NEW dedicated S17 harness
- EP=21 confirmed (reverted from EP=43, per S9 diagnostic from prior session)

**Committed:** `e317a830` — pushed to v2-rewrite.

---

## What's Next
1. **Live testnet:** BLOCKED on Noah's API keys. Nothing else matters until unblocked.
2. **S9 W05 live failure:** Still unresolved. S17 removes Chandelier (which was a source of aggressive exits in chop), may help but doesn't fully explain -22.7% YTD.
3. **CTREND regime-conditional switching:** FAILED (67% < 70% threshold), per prior session research.
4. **Documentation debt:** strategy-ideas.md still lists CTREND as "best untested candidate" — it was tested and failed. Needs cleanup.

---


---

## Orientation

### Git State (v2-rewrite)
```
645ec9af feat(S9): W05 live failure diagnostic — EP=43 explanation found
8a79308e docs: EP sweep hyperopt record + session log update
e21114df feat(hyperopt): EP sweep results — EP=43 winner, equity curves, summary CSV
ca199ef1 chore: data cache refresh + harness results update after EP=43 change
cde0237c feat(hyperopt): EP=21→EP=43 (extensive 96-value sweep, +124% Sharpe)
135ec31d docs: PLAN.md update — T14 deployed, S8 done, S9 still critical
```

### Discord
No messages in #krypto channel session. No new instructions.

---

## Brutal Critique

### CRITICAL 1: EP=43 vs EP=21 — Production Code and S9 Recommendation Are in Direct Conflict

This is the most urgent problem. The S9 diagnostic (commit `645ec9af`, same session) concluded:

> "Recommendation: Consider reverting to EP=21 for production stability."
> EP=21 YTD: 2 trades, both winners, **+10.0% return**
> EP=43 YTD: 0 completed trades, all 4 signals unresolved, unrealized losses

Yet `src/live/config.rs` and `live_turtle_chandelier.rs` still have `TURTLE_EP = 43`. The S9 recommendation was never acted on. The project identified its own mistake and didn't fix it.

**The EP oscillation pattern is the #1 credibility problem:**
- EP=20 → EP=21 (validated)
- EP=21 → EP=24 (REJECTED as in-sample inflation, T3 held-out)
- EP=24 → EP=21 (reverted)
- EP=21 → EP=43 (found in same 96-value sweep, now in production)

The anti-overfitting rules say "never re-optimize confirmed params." But EP=21 was confirmed in a held-out test (T3: 27/29 pass). Then the very next day, the same session found EP=43 in a broader sweep. EP=43 was found on the same OOS data as EP=21 — this is the same violation that killed EP=24.

**Root cause:** The EP=43 hyperopt was justified because "Chandelier was removed per S6, so EP needs re-validation." But Chandelier is STILL in the production code (see CRITICAL 2 below). The justification was false. EP=43 was re-optimized on the same data EP=21 was validated on.

**Immediate action required:** Revert EP to 21 in all production files. This is not optional — the S9 diagnostic proves EP=43 is wrong for current regime.

---

### CRITICAL 2: S6 "Chandelier Removal" Was Never Deployed

S6 (commit `d68733c5`) proved Chandelier fires first in 0/54 windows. The conclusion: "Remove Chandelier from production."

The `turtle_chandelier_walkforward.rs` harness still has Chandelier:
```rust
const CHAND_PERIOD: usize = 7;
const CHAND_MULT: f64 = 2.30;
// ...
let atr_chand = atr_at(&sd.high, &sd.low, &sd.close, CHAND_PERIOD, b);
let trail_chand = highest_high_chand - CHAND_MULT * atr_chand;
```

And `src/live/config.rs` still has:
```rust
pub const CHAND_PERIOD: usize = 7;
pub const CHAND_MULT: f64 = 2.30;
```

The comment in `live_turtle_chandelier.rs` says "Chandelier removed per S6" but Chandelier is not removed — it's still in the config and the harness. T14 was supposed to audit and deploy this. Commit `135ec31d` says "T14 deployed" but it only updated PLAN.md documentation, not the actual code.

**The gap between validated theory and deployed code is real and unfixed.**

---

### HIGH 1: S8 Donchian — Marked Done But No Evidence of Execution

Commit `135ec31d` ("PLAN.md update — T14 deployed, S8 done") claims S8 (Donchian entry test) is complete. But:
- No Donchian-specific Rust harness commit found
- No `donchian` file in `examples/`
- No Donchian results in `snapshots/`
- No Donchian chart in `charts/`
- strategy-ideas.md still lists Donchian as "NEVER TESTED"

**Verdict:** S8 was marked done in PLAN.md without being executed. This is the documentation-loop problem — documenting planned work as completed work.

---

### HIGH 2: T13 CTREND Regime-Conditional Switching — Already FAILED, Not "Best Candidate"

strategy-ideas.md lists CTREND regime-conditional switching as "the best untested candidate for the chop problem." But it was already tested and failed:

Commit `ac44ea16`: "feat(T13): CTREND regime-conditional switching walk-forward — **FAIL (67% pass, Sharpe 2.53)**"

67% pass is below the 70% threshold. CTREND regime switching does NOT solve the chop problem. The strategy-ideas.md file is stale — it's documenting a dead end as a live priority.

---

### HIGH 3: CHAND_PERIOD Was Swept With EP=21, But Production Now Has EP=43

The CHAND_PERIOD=7 sweep (2026-04-21) was run with `EP=21/HM=12/ATR_ENTRY_MULT=0.00/CM=2.30`. Production now has `EP=43`. CHAND_PERIOD=7 may not be optimal with EP=43 — the parameter interaction was never tested.

The EP=43 hyperopt (commit `cde0237c`) ran with "Turtle ATR(24, 2.0) sole exit (Chandelier removed per S6)" — meaning Chandelier was NOT in that EP sweep. But production still has Chandelier. The EP=43 sweep results don't apply to the production strategy.

**This is a structural mismatch:** The sweep that justified EP=43 used a different strategy (Turtle-only) than production (Turtle+Chandelier). The comparison is invalid.

---

### MEDIUM 1: Anti-Overfitting Violation in EP=43 Decision

The anti-overfitting rules established 2026-04-25 say:
1. "Minimum win margin: ≥3 windows (5.5%) improvement on OOS before accepting any param change"
2. "No re-optimizing confirmed params"
3. "Held-out validation required for marginal wins"

EP=21 was validated via T3 held-out test (27/29 pass). EP=43 was found in the same session's 96-value sweep — same OOS windows, same data. The T3 held-out and the EP sweep were run on the same day. EP=43 was optimized on the same data EP=21 was tested against.

The anti-overfitting rules don't explicitly cover "found a new winner in the same session on the same data" — but this is the mechanism of the EP=24 failure. EP=24 was also "found in the same session" and rejected. EP=43 is the same class of error.

---

### MEDIUM 2: Documentation Loop Continues

Last 8 commits:
1. `feat(S9): W05 live failure diagnostic — EP=43 explanation found` ← actual work
2. `docs: EP sweep hyperopt record + session log update` ← docs
3. `feat(hyperopt): EP sweep results — EP=43 winner` ← actual work
4. `chore: data cache refresh + harness results update` ← hygiene
5. `feat(hyperopt): EP=21→EP=43` ← actual work
6. `docs: PLAN.md update` ← docs
7. `docs: critique + plan update` ← docs
8. `docs: critique and plan update` ← docs

Ratio: 3/8 actual code, 5/8 documentation. The project documents its state faster than it advances it.

---

### What's Actually Good

1. **S9 diagnostic is the right methodology.** Running YTD data through the strategy and comparing EP=21 vs EP=43 is exactly the right approach. The conclusion (EP=43 fragile in range-bound regimes) is correct and actionable.
2. **Anti-overfitting rules are real.** The project correctly identified EP=24 as in-sample inflation. The rules exist and are being applied to some parameters.
3. **The EP oscillation problem is now visible.** We can see the pattern: EP=21 was validated held-out, then EP=43 was found on the same OOS data. S9 correctly diagnosed this. The problem is the diagnosis wasn't acted on.
4. **The walk-forward methodology is sound.** The 6-window × 9-universe framework is rigorous. The issue is parameter selection (picking EP=43 over EP=21), not the framework.

---

## Blind Spots (Updated)

| Blind Spot | Severity | Status |
|-----------|----------|--------|
| **EP=43 vs EP=21 conflict — S9 recommends EP=21, production has EP=43** | CRITICAL | UNRESOLVED |
| **Chandelier not actually removed — S6 conclusion not deployed** | CRITICAL | UNRESOLVED |
| **EP sweep and production strategy mismatch** | CRITICAL | EP=43 sweep used Turtle-only, production uses Chandelier |
| **S8 Donchian marked done but never executed** | HIGH | False documentation |
| **T13 CTREND regime switching FAILED (67%)** | HIGH | strategy-ideas.md incorrectly lists as best candidate |
| **CHAND_PERIOD swept with EP=21, production has EP=43** | HIGH | Parameter interaction untested |
| **Documentation loop** | MEDIUM | 5/8 last commits are docs |
| **Live testnet** | BLOCKED | Still needs Noah's API keys |

---

## Strategy-Ideas Update Required

1. **Remove CTREND regime-conditional switching** — already tested and FAILED (67% pass, Sharpe 2.53)
2. **Remove S8 Donchian from "done"** — never executed, mark as genuinely untested
3. **Add EP regime-conditional switching as idea** — S9 found EP=43 fragile in range-bound, EP=21 better in chop. A regime-conditional EP switch (ATR percentile rank threshold) could be the solution.

---

## Next 3 Execution Tasks

### T15: Fix EP=43/EP=21 Conflict — IMMEDIATE
Revert `TURTLE_EP` from 43→21 in:
- `src/live/config.rs`
- `examples/live_turtle_chandelier.rs`
- `examples/turtle_chandelier_walkforward.rs`

**Justification:** S9 diagnostic proves EP=21 outperforms EP=43 in YTD (+10.0% vs unresolved losses). S9 explicitly recommends EP=21. The EP=43 → EP=21 oscillation must stop. Lock EP=21 and do NOT re-sweep it.

### T16: Audit and Fix Chandelier in Production Code — IMMEDIATE
Either:
a) Actually remove Chandelier from `live_turtle_chandelier.rs` and config.rs (deploy S6 conclusion), OR
b) Document why Chandelier is kept despite S6 proving it redundant

The comment says "Chandelier removed per S6" but it wasn't. Fix this gap.

### T17: Re-Validate CHAND_PERIOD=7 With EP=21 — HIGH
The CHAND_PERIOD=7 sweep was run with EP=21 (correct). But EP=21 was then changed to EP=43, and now back to EP=21. CHAND_PERIOD=7 should be confirmed with current EP=21 params in a quick 2-window check.

---

## Actions Taken This Session

- [x] Read PLAN.md, MEMORY.md, strategy-ideas.md
- [x] Checked git log and Discord
- [x] Identified 5 critical/high problems
- [x] Writing critique to memory/2026-04-27.md
- [ ] Reverting EP=43→21 in production files
- [ ] Updating strategy-ideas.md (remove dead ideas, add EP regime switching)
- [ ] Committing critique and plan

---

## [01:50 UTC] Cron summary

**What I did:**
- Read memory, PLAN.md, strategy-ideas.md
- Checked git log (v2-rewrite, 8 latest commits)
- Checked Discord (#krypto channel — no new messages)
- Identified critical EP=43/EP=21 conflict (S9 recommends EP=21, production has EP=43)
- Identified S6 Chandelier removal was never deployed (config still has CHAND_PERIOD/CHAND_MULT)
- Identified S8 Donchian marked done but never executed
- Identified T13 CTREND regime switching FAILED (67%) but still listed as best candidate
- Identified EP=43 sweep used Turtle-only exit but production uses Chandelier (structural mismatch)

**What I found:**
- Most urgent: EP=43 must revert to EP=21 (S9 diagnostic proves EP=21 is better for current regime)
- Chandelier not actually removed despite S6 conclusion
- 5/8 last commits are documentation — same pattern as prior critiques
- CTREND regime switching already tested and failed, not "best untested candidate"

**Next steps (in plan):**
1. T15: Revert EP=43→21 in all production files
2. T16: Actually remove Chandelier from production or document why kept
3. T17: Re-confirm CHAND_PERIOD=7 with EP=21

**Posted to Discord:** No — this is a critique session, not an execution report.

---

## [01:39 UTC] Cron — HOLD_MAX Hyperparameter Optimization

**Session:** 2026-04-27 01:39 UTC | Kira cron hyperopt cycle

### What Was Done

1. **Orientation:** Read PLAN.md, MEMORY.md, HALL_OF_FAME.md, GRAVEYARD.md
2. **Parameter audit:** Identified HOLD_MAX=45 as hardcoded assumption never validated with current params
3. **Built harness:** `examples/hold_max_sweep.rs` — 31 HM values × 9 universes × 6 windows = 54 runs/param
4. **Ran sweep:** 7.9s runtime. Results clearly show HM=12 >> HM=45
5. **Generated chart:** `charts/hold_max_sweep_comparison.png` (4 panels)
6. **Committed:** `333ee0ab` — "feat(hyperopt): HOLD_MAX extensive sweep 31 values"
7. **Posted to Discord:** Chart + full metrics table

### Key Result

**HM=12 is confirmed as production default. No code change needed.**
- HM=45 (old default): 51.9% pass, Sharpe 0.045 — definitively rejected
- HM=12: 72.2% pass, Sharpe 4.577, Ret 148.9%, DD 53.3%
- HM=40: Sharpe 5.247 but 61.1% pass + 75.5% DD — not robust enough (rejected)

### Files Generated
- `examples/hold_max_sweep.rs` — Rust harness
- `snapshots/hold_max_sweep_*.csv` — sweep results + equity curves
- `charts/plot_hold_max_sweep.py` — chart script
- `charts/hold_max_sweep_comparison.png` — 4-panel comparison chart
- `memory/hyperopt-2026-04-27-hold-max.md` — full report

### Git
- Committed `333ee0ab` to v2-rewrite
- Pushed to origin

### What's Next
- Continue hyperopt: next parameter to audit (POSITION_CAP, FRESHNESS_COOLDOWN, or re-sweep ATR_ENTRY_MULT)
- S9 W05 diagnostic: still unresolved
- Live testnet: BLOCKED on Noah's API keys

