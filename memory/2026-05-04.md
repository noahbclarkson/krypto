# Memory — 2026-05-04

**Kira | 00:18 UTC | v2-rewrite | Strategy Research & Critique Cycle**

---

## Orient

- Read: PLAN.md, MEMORY.md, strategy-ideas.md
- Git: v2-rewrite at 3013a2b6 (ATR_RANK=24 GRAVEYARD'd, HOF regenerated). Clean pull.
- Discord #krypto: empty — no new instructions
- Date: Monday 2026-05-04 00:18 UTC

---

## Critique: Last 8 Commits

| Commit | Type | Assessment |
|--------|------|------------|
| 3013a2b6 | docs/hof | HALL_OF_FAME regenerated — ATR_RANK=24 properly removed. CLEANUP. |
| a324a0fa | test | T52 ATR_RANK=24 held-out VALIDATION. LEGITIMATE. Rejected (10/22 pass/-0.964 Sharpe vs T=5 14/22/+0.664). |
| cfeb242d | docs | SIZE_MULT memory + session log. DOCS. |
| 6a6e366e | test | SIZE_MULT sweep. INERT — result is cosmetic. M=0.70 confirmed, no production change. |
| 9b6986dc | test | T51 short-side sleeve GRAVEYARD. LEGITIMATE. 37.5% pass (fails 69.1% guardrail). |
| b6a642fe | docs | Critique + plan update. DOCS. |
| f8dedff9 | test | T44 REGIME_ATR_PERIOD fine sweep — AP=12 confirmed. CONFIRMATION (AP=12 was already default). |
| 6e240b7b | config | VL=8→96 reconciled. META FIX. |

**Score: 3/8 = 37.5% produce results. 5/8 are docs/chore/meta.**
Legitimate results: T52 rejection (real held-out), T51 GRAVEYARD (real negative result), VL reconciliation (meta fix). The SIZE_MULT sweep was real but found "inert" — the overlay is a risk knob with no alpha, so no production change. AP=12 confirmation was confirming a settled default at higher resolution.

**Anti-spin: The two most recent docs commits (b6a642fe, cfeb242d) are the same critique-and-plan-update pattern we've run for 4+ sessions. The PLAN.md is detailed. What we need is execution, not more planning.**

---

## What We Built That Is Actually Good

1. **ATR_RANK=24 rejected cleanly via held-out validation.** T52 correctly killed the third sequential optimization on `live_compatible_wf.rs`. Same-harness artifact pattern detected and acted on. Anti-overfit discipline held.
2. **Short-side sleeve GRAVEYARD'd correctly.** 37.5% pass vs 69.1% guardrail. No more vol-threshold bear-regime short signals. This closes an entire strategy class.
3. **VL=96 reconciled in config.rs.** Config drift between config.rs (VL=8) and live_compatible_wf.rs (VL=96) is now resolved. Both use VL=96.

---

## What We Built That Is NOT Good

### #1: `reports/daily_progress.csv` Still Contains Stale ATR_RANK=24 Entry

```
2026-05-03,Turtle+ATR_RANK=24,77.4x,0.97,regime filter T=24; walk-forward: 54/63 pass; Sharpe 7.65; +138% ret. live_compatible_wf (unchanged),n/a
```

This entry is from 2026-05-03 — AFTER T52 (a324a0fa) already rejected T=24. ATR_RANK=24 was rejected via held-out validation (10/22 pass, Sharpe -0.964). The daily_progress.csv is showing a GRAVEYARD'd strategy as live. This is a reporting integrity failure.

Also missing: **ATR_RANK=5 never has an equity run.** The regime filter is in production (live bot uses T=5), but `progress_equity_curves.rs` never produced a T=5 equity curve. We don't know the real equity impact of the filter.

### #2: HALL_OF_FAME Equity Is Stale

The HOF says "Validated daily equity: $10K → $1,136,000 (113.6x)" — but this is from the dual-exit Turtle+Chandelier harness (`progress_equity_curves.rs`), not the Turtle-only live bot path. The live bot is Turtle-only with ATR_RANK=5. We don't have:
- Turtle-only (no Chandelier) equity from `progress_equity_curves.rs`
- ATR_RANK=5 (production regime filter) equity run

The 113.6x is NOT the live bot's equity. It's the wrong harness.

### #3: `data/cache/lob_nobi/` Is Empty

Last session (2026-05-03 21:40 UTC) claimed T49 is "one harness run away" — but this was wrong. The directory is empty. The collector ran but never persisted data. T49 is back to zero.

---

## Strategy-Ideas.md: Top 3 Unbuilt — REASSESSED

### #1: T49: LOB NOBI Signal Harness — DATA NOT PRESENT

**Status: DATA MISSING. Multi-session project.**
- `data/cache/lob_nobi/` is empty (0 files)
- Old `examples/depth_imbalance_pipeline.rs` is a 6-line stub: `"DXY+Vol compound regime and DVOL options skew filters are fully implemented and backtested."` (hardcoded print — never connected to real data)
- T49 requires: daemon script that fetches Binance depth API → persists to parquet → test harness
- This is genuinely novel market microstructure research (arxiv 2602.00776). Worth doing but not "one run away."

### #2: T53: Mock Exchange Bypass — UNBUILT, 5+ WEEKS BLOCKED

**Status: High priority, unbuilt.**
- Live testnet BLOCKED on Noah's Binance testnet API keys for 5+ weeks
- A local mock exchange (Rust HTTP server seeded with historical 1m data) would let us test `bot.rs` order placement, state machine, and fill logic without API keys
- Data exists: `data/cache/` has 1m parquet files for all major symbols
- `src/live/bot.rs` uses `binance-rs-async` — mock server could implement the subset of endpoints we use

**This is the highest-leverage unbuilt infrastructure item.** Every other research item (T49 included) is blocked on or would benefit from execution testing.

### #3: ATR_RANK=5 Equity Run — UNBUILT

**Status: Never run, production is live.**
- ATR_RANK=5 is IN the live config (`config.rs: ATR_RANK_THRESHOLD = 5.0`)
- `progress_equity_curves.rs` never ran a Turtle-only with ATR_RANK=5 variant
- We don't know the actual equity impact of the regime filter in practice
- This is a 10-minute harness run, not a new strategy

---

## Reports Directory Health Check

**`reports/daily_progress.csv` issues:**
1. Stale ATR_RANK=24 entry (2026-05-03) for a strategy already GRAVEYARD'd in the same session
2. ATR_RANK=5 has NO entry (never run)
3. DDBudget 7.20 Sharpe vs Turtle 0.98 — methodology mismatch labeled but still misleading

**Action needed:** Clean the daily_progress.csv. Add ATR_RANK=5 equity run.

---

## Biggest Blind Spots

### #1: No ATR_RANK=5 Equity Run — Live Config Has No Equity Metric

The regime filter is production. We validated it helps in simulation. But we never ran the equity curve for it. Every reporting dashboard value for Turtle-only ATR_RANK=5 is missing. We are flying production blind on the one variant we're actually running.

### #2: 425 Example Files — No Clear "What Is Live" Inventory

425 `.rs` files in `examples/`. Almost none have comments indicating live status. Every harness claims to be the "current production" strategy. The gap between research and deploy state is invisible. This is how the Chandelier-removal deception happened (2026-04-27).

**Proposed fix:** `examples/` should have a single `README.md` listing what is live, what is graveyard, what is current harness.

### #3: 5-Week Blocker on Live Testnet — No Workaround Executed

5 weeks and counting. We identified the mock exchange (T53) as a bypass. Zero commits. The project is accepting a 5-week research paralysis without executing the identified workaround.

---

## Project Status (2026-05-04)

**Genuine results this session:** T52 rejection (most important), T51 GRAVEYARD, VL reconciliation.
**Genuinely overdue:** T53 mock exchange, T49 LOB NOBI data collection, ATR_RANK=5 equity run.
**Genuinely stale:** `daily_progress.csv` (ATR_RANK=24 still present post-GRAVEYARD), HOF equity (wrong harness).

**What's closed:** ATR_RANK=24 (held-out confirmed artifact). Short-side sleeve (GRAVEYARD). VL config drift (reconciled). SIZE_MULT (inert — cosmetic only). The easy items are done.

**What remains:** Execution on genuinely unbuilt ideas (T53 mock exchange, T49 data collection, T54 equity run).

---

## Commit

`git add -A && git commit -m "docs: critique and plan update — T53 mock exchange, T54 equity run, daily_progress.csv cleanup" && git push`
