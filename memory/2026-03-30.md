# 2026-03-30

## 22:58 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions had spent too much time on the **rolling linear-model trust stack**, then on a run of failing **daily relational rescue attempts**
- that was no longer the highest-value use of time; the project risk was shifting from useful breadth work into a new local basin around peer / cluster / graph residual variants
- the under-served high-value move from `PLAN.md` was therefore **Track B / trust**, not another relational rescue and not another broader-factor tweak inside the same family
- I deliberately rebalanced toward a **risk-decomposition pass on the actual yardsticks**: extend capped-book worst-drawdown attribution to include the current majority ensemble instead of only the two trend rows

Work completed:
- added `examples/cross_family_drawdown_attribution.rs`
- scope kept narrow and decision-relevant:
  - universes: `Base5`, `Legacy5BNB`, `OldGuardNoBNB`
  - rows: `MACD+Regime`, `Turtle+Regime+MACD`, `Ensemble(Majority 2/3)`
  - same fair daily harness: signal at close, next-open entry, fixed **21-bar** hold, **0.1%** taker each side, **top-3 strength-capped** book
- built and ran with:
  - `cargo build --example cross_family_drawdown_attribution`
  - `cargo run --example cross_family_drawdown_attribution --profile sweep 2>&1`
- new outputs written:
  - `charts/cross_family_drawdown_attribution.png`
  - `snapshots/cross_family_drawdown_attribution_latest.md`
  - `snapshots/cross_family_drawdown_attribution_latest.csv`

Key findings:
- `Base5`:
  - `MACD+Regime` worst drawdown **-34.7%**, driven mainly by **ETH (-24.3%)** then **DOGE (-15.9%)**
  - `Turtle+Regime+MACD` worst drawdown **-34.3%**, driven by **ADA (-20.0%)** and **DOGE (-15.1%)**
  - `Ensemble(Majority 2/3)` worst drawdown **-34.4%**, basically the same modern-basket path-risk event as the Turtle combo (**ADA / DOGE / SOL**)
- `Legacy5BNB`:
  - `MACD+Regime` worst drawdown **-58.4%**, led by **LTC (-30.7%)**, **BNB (-25.1%)**, **BTC (-11.8%)**
  - `Turtle+Regime+MACD` worst drawdown **-59.3%**, led by **XRP (-34.4%)**, **LTC (-24.5%)**, **BTC (-15.5%)**
  - `Ensemble(Majority 2/3)` is slightly cleaner at **-55.9%**, with losses concentrated in **LTC (-17.9%)**, **BNB (-17.8%)**, **XRP (-13.4%)**
- `OldGuardNoBNB`:
  - `MACD+Regime` remains the clean defensive row with worst drawdown **-40.9%**, led by **BCH (-27.8%)**, **LTC (-12.4%)**, **ETH (-12.3%)**
  - `Turtle+Regime+MACD` is uglier at **-54.0%**, with damage spread across **LTC / BCH / EOS / XRP**
  - `Ensemble(Majority 2/3)` is the **worst path-risk row** of the three at **-61.8%**, led by **LTC (-32.8%)**, **BCH (-23.7%)**, **XRP (-18.3%)**, **EOS (-11.6%)**

Honest read:
- this was a good rebalance because it improved **risk interpretation of the actual yardsticks** instead of sinking another session into a failing relational family
- the majority ensemble still looks like a real modern / mixed-basket contender, but the harsher old-guard drawdown path is materially uglier than the broad leaderboard story alone suggests
- `MACD+Regime` remains the cleaner defensive row on the old-guard basket; the majority ensemble and Turtle combo look much more alike on the broad modern basket than simple return tables imply

Overfitting / trust risk:
- the trap from here would be to let the ensemble quietly become “the strategy” because it keeps showing up near the top of breadth tables
- this attribution says that would be premature: on the harsher basket, the ensemble currently has the **worst path risk** of the three realistic yardsticks
- equally, the wrong response would be to snap back into another local relational-rescue loop just because this session was not a fresh breadth scan

What’s next:
- keep the program rebalanced away from the current relational basin
- best next move is either another **surgical Track A/B trust question** around realistic yardsticks, or a genuinely orthogonal **Track C** bucket — not more peer / cluster / graph residual nudging

## 00:54 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **exogenous breadth / portfolio trust**, then the new **rolling linear model** plus its first null-control sanity check
- that was healthier than the older MACD-family tunnel vision, but the newest risk was obvious: the richer model could become the next quietly privileged story if I stopped after the first 6/6 breadth win table
- the highest-value under-served item from `PLAN.md` was therefore clearly **Track A portfolio realism** around the richer model, not another new family and not local feature tuning
- so this cycle deliberately forced the rolling panel model through the same realistic **top-3 strength-capped** book lens as the incumbent yardsticks

Work completed:
- added `examples/cross_sectional_linear_model_portfolio_audit.rs`
- compared under the same fair daily capped-book harness:
  - `LinearAlpha(panel)`
  - `LinearAlpha(shuffled_y)`
  - `MACD+Regime`
  - `Turtle+Regime+MACD`
  - `Ensemble(Majority 2/3)`
- assumptions preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **top-3 strength-capped** book
  - rolling **252-bar** training window for the richer model
- built and ran with:
  - `cargo build`
  - `cargo run --example cross_sectional_linear_model_portfolio_audit --profile sweep 2>&1`
- new outputs written:
  - `snapshots/cross_sectional_linear_model_portfolio_latest.md`
  - `snapshots/cross_sectional_linear_model_portfolio_latest.csv`
  - `snapshots/cross_sectional_linear_model_portfolio_20260330T005030Z.md`
  - `snapshots/cross_sectional_linear_model_portfolio_20260330T005030Z.csv`

Key findings:
- the real model **does survive** the capped-book realism lens better than many recent breadth candidates and becomes the Sharpe-first row on several harsher / legacy-heavy baskets:
  - `Legacy4`: **+1,345,885.2%**, **Sharpe 6.89**, **27.7% MaxDD**
  - `Legacy5BNB`: **+397,852.0%**, **5.79**, **40.7% MaxDD**
  - `OldGuardNoBNB`: **+32,697.8%**, **6.20**, **32.1% MaxDD**
  - `LargeCaps5`: **+5,177,917.5%**, **6.03**, **49.1% MaxDD**
- but the earlier 6/6 breadth-win story gets more mixed once judged in the shared portfolio lens:
  - `Base5`: `LinearAlpha(panel)` is still huge (**+5,771,051.9% / Sharpe 5.41 / 42.6% MaxDD**) but does **not** lead on Sharpe (`Turtle+Regime+MACD` **6.22**) and does **not** lead on full return (majority ensemble **+12,877,075.5%**)
  - `NoDOGE`: `LinearAlpha(panel)` is only **third** on Sharpe (**6.43**) behind `Turtle+Regime+MACD` (**6.79**) and the majority ensemble (**6.44**)
- the shuffled-label control compresses hard under portfolio realism, which is useful trust evidence, but it is still too alive to dismiss:
  - `Base5`: **+7,402.4% / Sharpe 4.01**
  - `Legacy4`: **+21,007.4% / 5.16**
  - `Legacy5BNB`: **+12,134.6% / 3.71**

Honest read:
- this was exactly the right anti-worship step
- the richer-model family is now **more credible than it looked after the null-control scare alone**, because it survives a realistic portfolio lens on several difficult baskets
- but it is still **not trust-clean**, because the shuffled-label control remains materially positive even after that compression

Overfitting / trust risk:
- if I stopped here and called this the new best strategy, I would still be over-reading it
- the honest remaining red flag is not that the model dies under realism — it doesn’t — but that the **null / ranking-structure story is still too strong**

What’s next:
- best next move is either:
  - a **stronger null / leak / rank-structure diagnostic** around the richer model, or
  - a deliberate pivot back to another under-served orthogonal **Track C** bucket so the richer model does not become the next local worship loop
- do **not** start feature or ridge-lambda tuning from here

## 02:17 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **exogenous breadth / portfolio trust**, then the new **rolling linear model** and its first two trust checks (**shuffled-label sanity** and **capped-book portfolio realism**)
- that was still healthier than the older MACD-family tunnel vision, but it was now clearly at risk of becoming a **linear-model trust micro-loop**
- the highest-value under-served item from `PLAN.md` was still **Track A**, but only if it directly answered the remaining red flag: how much of this result depends on the score staying attached to the **right symbol**, versus surviving as generic ranking / book structure?
- so this cycle deliberately avoided tuning and built a sharper **symbol-mapping null diagnostic** around the richer model

Work completed:
- added `examples/cross_sectional_linear_model_mapping_audit.rs`
- compared under the same fair daily harness:
  - `LinearAlpha(panel)`
  - `LinearAlpha(shuffled_y)`
  - `LinearAlpha(score_rotated)` — fit the real model, but rotate predicted scores across symbols each day before turning them into trades
  - `MACD+Regime`
- assumptions preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - rolling **252-bar** training window
  - **4** quarter windows + **15** CPCV-style resamples
- built and ran with:
  - `cargo build --example cross_sectional_linear_model_mapping_audit`
  - `cargo run --example cross_sectional_linear_model_mapping_audit --profile sweep 2>&1`
- new outputs written:
  - `snapshots/cross_sectional_linear_model_mapping_latest.md`
  - `snapshots/cross_sectional_linear_model_mapping_latest.csv`
  - `snapshots/cross_sectional_linear_model_mapping_20260330T022555Z.md`
  - `snapshots/cross_sectional_linear_model_mapping_20260330T022555Z.csv`

Key findings:
- this was a **usefully mixed** trust result, which is exactly what a good null should produce
- the real model still stays strongest across all baskets, but the new `score_rotated` control shows the remaining red flag is **basket-dependent**, not universal
- on some harsher / legacy baskets, breaking the symbol mapping **kills or nearly kills** the edge:
  - `Legacy4`: `LinearAlpha(panel)` **+3387.2%**, **4/4**, **15/15** vs `LinearAlpha(score_rotated)` **-309.4%**, **1/4**, **7/15`
  - `Legacy5BNB`: panel **+2986.4%**, **4/4**, **15/15** vs score-rotated **-84.6%**, **3/4**, **6/15**
- but on several broader / modern baskets, the rotated-score control stays **too alive** to call this trust-clean:
  - `LargeCaps5`: score-rotated still prints **+3136.8%**, **4/4**, **15/15**
  - `NoDOGE`: **+1720.1%**, **4/4**, **13/15**
  - `Base5`: **+1067.9%**, **4/4**, **15/15**
- the old shuffled-label warning remains real too:
  - `Legacy5BNB` shuffled is still **+2175.6%**, **4/4**, **15/15**
  - `Base5` shuffled is **+1732.0%**, **4/4**, **15/15**

Honest read:
- this materially improves the interpretation of the richer-model result
- the model is now **more credible than a pure ranking mirage**, because on some baskets the edge clearly depends on the score being attached to the correct symbol
- but it is still **not trust-clean**, because on other baskets a lot of the result survives even after the symbol mapping is broken

Overfitting / trust risk:
- the honest remaining risk is no longer “maybe the whole thing is fake”
- it is narrower and more useful: some universes look like **real symbol-level predictive structure**, while others still look compatible with **generic cross-sectional ordering / book construction**
- that means this bucket still does **not** deserve tuning or promotion

What’s next:
- best next move is either:
  - a **portfolio-level mapping audit** (same symbol-rotation idea under the top-3 capped-book lens), or
  - a pivot back to another under-served orthogonal **Track C** bucket so the richer model does not consume the whole program
- do **not** move into feature / lambda / threshold tuning from here

## 03:59 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on the new **rolling linear model** and its trust stack: shuffled-label sanity, capped-book realism, then symbol-mapping nulls
- that concentration was justified for a while because the richer-model family was the strongest fresh Track C breadth result in days, but it was clearly at risk of becoming the next local worship loop
- the highest-value remaining question was therefore the most surgical one left in **Track A**: under the same realistic **top-3 capped-book** lens, how much of the panel model still survives if the daily predicted scores are rotated across symbols before trading?
- so this cycle deliberately avoided both feature tuning and another fresh family, and added a **portfolio-level symbol-mapping audit**

Work completed:
- added `examples/cross_sectional_linear_model_portfolio_mapping_audit.rs`
- compared under the same fair daily capped-book harness:
  - `LinearAlpha(panel)`
  - `LinearAlpha(shuffled_y)`
  - `LinearAlpha(score_rotated)`
  - `MACD+Regime`
  - `Turtle+Regime+MACD`
  - `Ensemble(Majority 2/3)`
- assumptions preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **top-3 strength-capped** book
  - rolling **252-bar** training window
- built and ran with:
  - `cargo build`
  - `cargo run --example cross_sectional_linear_model_portfolio_mapping_audit --profile sweep 2>&1`
- new outputs written:
  - `snapshots/cross_sectional_linear_model_portfolio_mapping_latest.md`
  - `snapshots/cross_sectional_linear_model_portfolio_mapping_latest.csv`
  - `snapshots/cross_sectional_linear_model_portfolio_mapping_20260330T035952Z.md`
  - `snapshots/cross_sectional_linear_model_portfolio_mapping_20260330T035952Z.csv`

Key findings:
- this was a good **anti-worship trust** result because portfolio realism makes the score-mapping requirement much clearer on several baskets:
  - `Base5`: `LinearAlpha(panel)` **+5,771,051.9% / Sharpe 5.41 / 42.6% MaxDD** vs `LinearAlpha(score_rotated)` only **+602.6% / 1.93 / 61.7%**
  - `Legacy4`: panel **+1,345,885.2% / 6.89 / 27.7%** vs score-rotated **-24.3% / -0.21 / 79.6%**
  - `Legacy5BNB`: panel **+397,852.0% / 5.79 / 40.7%** vs score-rotated **+410.0% / 1.74 / 67.4%**
- but the remaining red flag is still real even under the capped-book lens:
  - `NoDOGE`: score-rotated still prints **+3,445.2% / Sharpe 3.73**
  - `OldGuardNoBNB`: **+1,478.1% / 3.10 / 49.2% MaxDD**
  - `LargeCaps5`: **+80,266.6% / 5.00 / 49.0% MaxDD**
- the shuffled-label control also stays too alive to ignore on several baskets:
  - `Legacy4`: **+21,007.4% / Sharpe 5.16**
  - `Base5`: **+7,402.4% / 4.01**

Honest read:
- this improves trust in the richer model on a **portfolio-realism basis**: some baskets clearly need the score attached to the correct symbol, not just a generic cross-sectional book shape
- but it still does **not** make the family trust-clean, because broader baskets continue to leave too much edge alive after score rotation
- the right interpretation is now **more credible, still not promotable**

Overfitting / trust risk:
- if I keep orbiting this same model for several more cycles, the work itself becomes the overfitting risk
- the remaining issue is now narrow and explicit: some universes still look compatible with **generic ranking / book construction**, even after harsher nulls and realistic concentration

What’s next:
- the richer-model trust stack is now probably deep enough for this phase
- unless a very surgical leak/null idea appears, the next cycle should deliberately **rebalance back toward another under-served Track C family** rather than keep polishing this one

## 05:35 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on the **rolling linear model trust stack**: shuffled-label sanity, capped-book realism, symbol-mapping nulls, then portfolio-level mapping
- that was valuable anti-worship work, but it had clearly reached the point where **staying there longer would itself become the overfitting risk**
- so the highest-value under-served item from `PLAN.md` was a real **Track C rebalance**, not another richer-model null and not another benchmark-leader interrogation
- I deliberately picked a **simple relative-value breadth probe** instead of a nearby tweak: test whether cross-sectional momentum gets cleaner after stripping out a rolling **BTC market beta**

Work completed:
- added `examples/residual_momentum_benchmark.rs`
- construction tested:
  - estimate each symbol's rolling **252-bar BTC beta** from prior close-close returns
  - compute **21-bar residual momentum = symbol 21-bar return - beta × BTC 21-bar return**
  - each day rank symbols on that residual score
  - long the top **2** positive names, short the bottom **2** negative names
- same fair daily harness preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- compared directly against:
  - `CrossSectionalMomentum`
  - `MACD+Regime`
  - `Ensemble(Majority 2/3)`
- built and ran with:
  - `cargo build`
  - `cargo run --example residual_momentum_benchmark --profile sweep 2>&1`
- new outputs written:
  - `snapshots/residual_momentum_benchmark_latest.md`
  - `snapshots/residual_momentum_benchmark_latest.csv`
  - `snapshots/residual_momentum_benchmark_20260330T053554Z.md`
  - `snapshots/residual_momentum_benchmark_20260330T053554Z.csv`

Key findings:
- this was a good **negative breadth** result: the beta-adjusted family stayed respectable in places, but it did **not** become a new leader and won **0/6** universes overall
- representative rows:
  - `Base5`: `ResidualMomentum(BTC beta-adj)` **+2221.4%**, **4/4** quarter windows, **14/15** resamples — decent, but still behind `MACD+Regime` and the majority ensemble on chronology-first ranking
  - `Legacy5BNB`: **+1254.9%**, **4/4**, **14/15** — the cleanest niche, but still behind `Ensemble(Majority 2/3)` (**15/15**)
  - `LargeCaps5`: **+2071.5%**, **4/4**, **12/15** — level is fine, chronology is weaker than `MACD+Regime` (**15/15**)
  - `OldGuardNoBNB`: only **+275.9%**, **2/4**, **12/15** — the harsher old-guard basket rejects the family most clearly
- universe wins:
  - `MACD+Regime`: **3/6**
  - `Ensemble(Majority 2/3)`: **2/6**
  - `CrossSectionalMomentum`: **1/6**
  - `ResidualMomentum(BTC beta-adj)`: **0/6**

Honest read:
- this was exactly the kind of rebalance the program needed
- stripping out simple BTC beta does **not** reveal a cleaner cross-sectional leader; if anything, it weakens the old-guard / harsher-basket story materially
- useful because it kills a plausible “more structural” idea before it can become another tuning basin

Overfitting / trust risk:
- the trap from here would be to say “the idea is almost there, just tune beta windows / lookbacks / ranking thresholds”
- that would be the wrong lesson: the honest result is that this simple beta-adjusted momentum expression is **not** a breadth leader and does **not** deserve a local optimization loop

What’s next:
- keep rebalancing toward under-served **Track C** work
- best next candidates are a more structural **pair / sector-residual / latent-cluster** relative-value family, or a richer interaction model that is genuinely different from both the old cross-impact bucket and this simple residual-momentum expression
- do **not** reopen the linear-model trust loop by default unless a very surgical leak/null test clearly dominates breadth value

## [05:58 UTC] Arc infra fix — GitHub auth restored

Arc removed the stale `GH_TOKEN` env override from `~/.gitconfig` and pushed all stuck commits. `git push` and `gh` should work again. If you still get 401 errors, run `unset GH_TOKEN` before your git/gh commands — the env var may linger in already-running shells.

## 10:34 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on the **rolling linear-model trust stack**, with the most recent breadth rebalance only just starting via **residual momentum**
- that concentration was no longer the highest-value use of time by itself, and Noah’s freshest direct feedback in Discord was about **visual quality / readability** rather than another model null
- so the highest-value under-served item from `PLAN.md` was a narrow **Track A × communication trust** task: fix the audited chart exporters so future equity charts default to **log scale** and better styling instead of patching one-off images by hand

Work completed:
- updated the actual audited visual exporters:
  - `examples/linear_model_visual_audit.rs`
  - `examples/capped_book_visual_audit.rs`
- changes made:
  - equity curves now default to **log-scale y-axis**
  - improved chart presentation: cleaner captions, lighter mesh, better label sizing, more readable legend placement, and less visual clutter
  - drawdown charts kept **linear scale** but received the same readability cleanup
- built and reran:
  - `cargo build --example linear_model_visual_audit --example capped_book_visual_audit`
  - `cargo run --example linear_model_visual_audit --profile sweep`
  - `cargo run --example capped_book_visual_audit --profile sweep`
- regenerated outputs include:
  - `charts/linear_model_vs_leaders_base5_equity.png`
  - `charts/linear_model_vs_leaders_oldguardnobnb_equity.png`
  - `charts/capped_book_equity_audit.png`
  - `charts/capped_book_drawdown_audit.png`

Honest read:
- this is not a new alpha result; it is **lab-output quality / trust infrastructure**
- value is that future benchmark and richer-model visuals now match Noah’s stated preference: **actual audited harness data, equity on log scale by default, drawdown on linear scale, cleaner presentation**
- it also helps prevent a subtle trust failure where correct charts are ignored because they are visually worse than older approximate ones

Overfitting / trust risk:
- the project had started to spend too many cycles on the richer-model trust loop; doing this instead was a deliberate rebalance toward communication quality without opening another tuning basin
- the trap from here would be to mistake prettier charts for stronger evidence; the underlying research conclusions are unchanged

What’s next:
- keep the new chart rule as default for future shared visuals
- next research cycle should return to an under-served **Track C** family or another high-value trust question, not slip back into chart-only polish

## 13:03 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on the **rolling linear-model trust stack**, with only one fresh Track C rebalance via **residual momentum** and one short chart-quality trust pass
- that concentration was no longer the highest-value use of time; the project risk was shifting from MACD worship to **linear-model trust micro-loops**
- the highest-value under-served item from `PLAN.md` was therefore a genuinely different **Track C relative-value** probe, not another null audit and not more chart polish
- I deliberately tested a **stable peer-cluster residual reversion** family: ask whether symbols mean-revert after overshooting their own calibrated peer cluster

Work completed:
- added `examples/cluster_residual_reversion_benchmark.rs`
- construction tested:
  - calibrate each symbol's top **2** stable peers from the first **900** bars using lagged-return correlation
  - compute **21-bar residual return = symbol 21-bar return minus peer-cluster mean 21-bar return**
  - trade **reversion**: long the biggest negative residuals, short the biggest positive residuals
- same fair daily harness preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- compared directly against:
  - `CrossSectionalMomentum`
  - `MACD+Regime`
  - `Ensemble(Majority 2/3)`
- built and ran with:
  - `cargo build --example cluster_residual_reversion_benchmark`
  - `cargo run --example cluster_residual_reversion_benchmark --profile sweep 2>&1`
- new outputs written:
  - `snapshots/cluster_residual_reversion_benchmark_latest.md`
  - `snapshots/cluster_residual_reversion_benchmark_latest.csv`
  - `snapshots/cluster_residual_reversion_benchmark_<UTC timestamp>.md`
  - `snapshots/cluster_residual_reversion_benchmark_<UTC timestamp>.csv`

Key findings:
- this was a useful **negative breadth** result: the family stayed alive enough to be informative on a few legacy-heavy baskets, but it won **0/6** universes overall
- the cleanest rows were still clearly secondary:
  - `Legacy4`: **+305.1%**, **3/4** quarter windows, **14/15** resamples
  - `NoDOGE`: **+283.0%**, **2/4**, **12/15**
  - `OldGuardNoBNB`: **+276.1%**, **2/4**, **12/15**
  - `LargeCaps5`: **+290.9%**, only **1/4**, **8/15**
- the broad modern basket failed structurally because the calibrated cluster graph was mostly absent:
  - `Base5`: every symbol ended up with **no usable peers**, so the family produced **0 trades**, **0/4**, **0/15**
- universe wins:
  - `MACD+Regime`: **3/6**
  - `Ensemble(Majority 2/3)`: **2/6**
  - `CrossSectionalMomentum`: **1/6**
  - `ClusterResidualReversion`: **0/6**

Honest read:
- this is exactly the kind of result the program needs: a plausible structural relative-value idea tested honestly and killed before it turns into another tuning basin
- the daily peer-relative reversion story looks **too sparse and too weak** under the current harness to become a priority family
- unlike the earlier residual-momentum probe, this one also shows a concrete structural problem: on several broader baskets there simply are **not enough stable positive-correlation peer links** for the construction to even express itself cleanly

Overfitting / trust risk:
- the trap from here would be to rescue the idea with nearby tweaks (`peer_count`, correlation threshold, lookback length, momentum-vs-reversion direction)
- that would be the wrong lesson: the honest result is that this simple daily cluster-relative reversion expression is **not** a breadth leader and does **not** deserve another local optimization loop

What’s next:
- better next Track C directions are a **more structural pair/cluster family** (latent-cluster residuals, sector-style grouping, graph/relationship models) or another orthogonal bucket entirely
- do **not** reopen the linear-model trust loop by default, but also do **not** spend several more cycles trying to rescue this sparse peer-reversion rule

## 13:50 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions still spent too much time on the **rolling linear-model trust stack**, with only brief Track C rebalancing via residual momentum and peer-cluster residual reversion
- that concentration was no longer the highest-value use of time, but the peer-cluster bucket still had one obvious unresolved loophole: maybe the residual should be traded as **momentum**, not reversion
- so this cycle deliberately stayed in **Track C**, but used a clean falsification step instead of another richer-model null or nearby leader tweak: test **cluster-relative residual momentum** under the same fair daily harness

Work completed:
- added `examples/cluster_residual_momentum_benchmark.rs`
- construction tested:
  - calibrate each symbol's top **2** stable peers from the first **900** bars using lagged-return correlation
  - compute **21-bar residual return = symbol return minus peer-cluster mean 21-bar return**
  - trade **momentum**: long the top **2** positive residuals, short the bottom **2** negative residuals
- same fair daily harness preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- compared directly against:
  - `CrossSectionalMomentum`
  - `MACD+Regime`
  - `Ensemble(Majority 2/3)`
- built and ran with:
  - `cargo build --example cluster_residual_momentum_benchmark`
  - `cargo run --example cluster_residual_momentum_benchmark --profile sweep 2>&1`
- new outputs written:
  - `snapshots/cluster_residual_momentum_benchmark_latest.md`
  - `snapshots/cluster_residual_momentum_benchmark_latest.csv`
  - `snapshots/cluster_residual_momentum_benchmark_<UTC timestamp>.md`
  - `snapshots/cluster_residual_momentum_benchmark_<UTC timestamp>.csv`

Key findings:
- this was another useful **negative breadth** result: flipping the peer-cluster residual from reversion to momentum still wins **0/6** universes overall
- representative rows:
  - `Base5`: **0 trades**, **0/4**, **0/15** — the broad modern basket still has no usable calibrated peer graph
  - `Legacy4`: **+266.5%**, **2/4**, **14/15** — alive enough to be informative, but clearly secondary
  - `Legacy5BNB`: **+1174.5%**, **3/4**, **14/15** — the cleanest niche, but still not a universe winner
  - `OldGuardNoBNB`: only **+232.5%**, **1/4**, **7/15** — weaker than the earlier residual cross-impact niche and weaker than plain `CrossSectionalMomentum`
  - `LargeCaps5`: **+1130.5%**, **3/4**, **14/15** — respectable level, still not enough to lead
- universe wins stayed:
  - `MACD+Regime`: **3/6**
  - `Ensemble(Majority 2/3)`: **2/6**
  - `CrossSectionalMomentum`: **1/6**
  - `ClusterResidualMomentum`: **0/6**

Honest read:
- this closes the most obvious loophole from the earlier peer-cluster reversion failure
- the problem was **not just sign choice**; the whole simple daily peer-cluster residual family still looks too sparse / weak under the current harness
- that is good project control because it narrows the family honestly instead of letting it linger as a “maybe one tweak away” story

Overfitting / trust risk:
- the trap from here would be to rescue the bucket with nearby tweaks: more peers, fewer peers, different calibration window, different residual horizon, or yet another momentum/reversion hybrid
- that would be the wrong lesson: the honest answer is that simple daily peer-cluster relative value is **not** a breadth leader in either direction

What’s next:
- if cluster / relational breadth returns, it should do so through a **more structural latent-cluster / graph / sector-style model**, not another nearby simple peer-residual variant
- do **not** reopen the rolling linear-model trust loop by default, but also do **not** sink more cycles into rescuing this simple peer-cluster family

## 17:49 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time first on the **rolling linear-model trust stack**, then on increasingly clear **daily relational rescue attempts**
- that was no longer the highest-value use of time; the risk was shifting from benchmark worship to a new local loop around simple cluster / residual variants
- the highest-value under-served item from `PLAN.md` was therefore a **more structural relational falsification**, not another linear-model null and not another nearby peer tweak
- so this cycle stayed in **Track C**, but tried a slightly richer **cluster-factor residual** construction: remove both BTC market mode and same-cluster factor exposure before ranking residual momentum

Work completed:
- added `examples/cluster_factor_residual_momentum_benchmark.rs`
- construction tested:
  - estimate early-window return correlations across each stress universe
  - build **2 latent clusters** from the least-correlated seed pair
  - compute same-cluster factor return from each symbol's cluster mates
  - estimate rolling **252-bar** exposure to both **BTC** and the same-cluster factor
  - compute **21-bar residual momentum** after removing both exposures
  - long the top **2** positive residuals, short the bottom **2** negative residuals
- same fair daily harness preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- compared directly against:
  - `CrossSectionalMomentum`
  - `MACD+Regime`
  - `Ensemble(Majority 2/3)`
- built and ran with:
  - `cargo build --example cluster_factor_residual_momentum_benchmark`
  - `cargo run --example cluster_factor_residual_momentum_benchmark --profile sweep 2>&1`
- new outputs written:
  - `snapshots/cluster_factor_residual_momentum_benchmark_latest.md`
  - `snapshots/cluster_factor_residual_momentum_benchmark_latest.csv`
  - `snapshots/cluster_factor_residual_momentum_benchmark_<UTC timestamp>.md`
  - `snapshots/cluster_factor_residual_momentum_benchmark_<UTC timestamp>.csv`

Key findings:
- this was another useful **negative breadth** result: the richer factor-style residual still wins **0/6** universes overall
- representative rows:
  - `Base5`: **+1133.0%**, **2/4** quarter windows, **10/15** resamples — alive enough to be informative, but clearly secondary
  - `NoDOGE`: **+1152.6%**, **2/4**, **10/15** — same story
  - `LargeCaps5`: **+1182.5%**, **3/4**, **11/15** — respectable level, still behind the yardsticks
  - `Legacy4`: only **+63.3%**, **2/4**, **6/15** — harsher legacy basket largely rejects it
  - `OldGuardNoBNB`: **-420.5%**, **1/4**, **2/15** — strongest rejection of the family so far
- universe wins stayed:
  - `MACD+Regime`: **3/6**
  - `Ensemble(Majority 2/3)`: **2/6**
  - `CrossSectionalMomentum`: **1/6**
  - `ClusterFactorResidualMomentum`: **0/6**

Honest read:
- this was worth doing because it closes another plausible rescue path cleanly
- daily relational breadth is **not** rescued just by moving from raw peer means to a slightly richer cluster-factor subtraction
- the family is more structural than the earlier peer-pair variants, but still not strong or clean enough to justify another local optimization loop

Overfitting / trust risk:
- the trap from here would be to say the family is "almost there" and start nudging cluster count, beta windows, residual horizons, or factor definitions
- that would be the wrong lesson: the honest result is that the current daily relational family still does **not** deserve priority

What’s next:
- do **not** reopen the rolling linear-model trust loop by default
- do **not** spend the next session rescuing this exact cluster-factor construction
- better next Track C moves are either:
  - a genuinely richer **graph / latent-factor / sector-residual** relational model, or
  - another orthogonal breadth bucket entirely if that looks higher-value after the next focus check

## 19:12 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on the **rolling linear-model trust stack**, then on a run of **simple daily relational rescue attempts**
- that was no longer the highest-value use of time; the project was at risk of turning “relational breadth” into the next local rescue basin
- the under-served high-value move from `PLAN.md` was therefore a **genuinely different structural graph** test, not another nearby cluster-count / factor-subtraction tweak and not another benchmark-polish loop
- so this cycle stayed in **Track C**, but moved from coarse clusters to a **weighted graph-neighbour residual momentum** construction

Work completed:
- added `examples/graph_residual_momentum_benchmark.rs`
- construction tested:
  - estimate early-window pairwise return correlations across each stress universe
  - for each symbol, keep the top **3** positive-correlation neighbours as a local graph
  - weight each neighbour by calibration-period correlation strength
  - compute a weighted **21-bar graph factor** from those neighbours
  - estimate rolling **252-bar** exposure to both **BTC** and that graph factor
  - compute **21-bar residual momentum** after removing both exposures
  - long the top **2** positive residuals, short the bottom **2** negative residuals
- same fair daily harness preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- compared directly against:
  - `CrossSectionalMomentum`
  - `MACD+Regime`
  - `Ensemble(Majority 2/3)`
- built and ran with:
  - `cargo build`
  - `cargo run --example graph_residual_momentum_benchmark --profile sweep 2>&1`
- new outputs written:
  - `snapshots/graph_residual_momentum_benchmark_latest.md`
  - `snapshots/graph_residual_momentum_benchmark_latest.csv`
  - `snapshots/graph_residual_momentum_benchmark_20260330T190713Z.md`
  - `snapshots/graph_residual_momentum_benchmark_20260330T190713Z.csv`

Key findings:
- this was another useful **negative breadth** result: the richer graph-weighted residual still wins **0/6** universes overall
- representative rows:
  - `Base5`: **+1418.6%**, **2/4**, **10/15** — more alive than the recent cluster-factor residual, still clearly secondary
  - `NoDOGE`: **+1076.5%**, **2/4**, **10/15** — same secondary story
  - `Legacy5BNB`: **+774.2%**, **3/4**, **14/15** — the cleanest niche, but still not a universe winner on chronology-first ranking
  - `LargeCaps5`: **+1240.6%**, **3/4**, **14/15** — respectable, still behind the yardsticks
  - `Legacy4`: **-77.3%**, **2/4**, **6/15** and `OldGuardNoBNB`: **-368.8%**, **1/4**, **4/15** — harsher baskets reject the family outright
- graph diagnostics make the structural issue clearer:
  - some strong edges are just obvious broad-risk pairs (`ETH <-> LTC` around **0.85**, `ETH/LTC <-> BNB` around **0.68**)
  - broader modern baskets still mostly collapse into tiny or weak positive links (`0.00` to `0.03`), which is not the kind of durable local interaction structure that would justify trust
- universe wins stayed:
  - `MACD+Regime`: **3/6**
  - `Ensemble(Majority 2/3)`: **2/6**
  - `CrossSectionalMomentum`: **1/6**
  - `GraphResidualMomentum`: **0/6**

Honest read:
- this was worth doing because it answers the next honest relational question without opening another knob-twiddling loop
- moving from raw peer means / clusters to a simple weighted local graph does **not** rescue daily relational breadth under the current fair harness
- the surviving structure still looks more like **broad common-risk linkage** than a trustworthy local relative-value edge

Overfitting / trust risk:
- the trap from here would be to keep nudging peer count, minimum-correlation floors, graph sparsity rules, or weighting schemes until one table looks flattering
- that would be the wrong lesson: the honest result is that this whole simple daily relational family still does **not** deserve priority

What’s next:
- do **not** reopen the rolling linear-model trust loop by default
- do **not** spend the next session rescuing this graph residual construction with nearby tweaks
- better next Track C moves are either:
  - a genuinely richer **latent-factor / graph-diffusion / sector-residual** model with more economic structure, or
  - another orthogonal breadth bucket entirely if that looks higher-value after the next focus check

## 20:55 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions had spent too much time first on the **rolling linear-model trust stack**, then on a run of failing **daily relational rescue attempts**
- the risk was no longer just benchmark worship; it was turning “relational breadth” into the next local basin through peer-count / graph-shape nudges
- the plan explicitly said the next honest branch was either a genuinely richer **latent-factor / graph-diffusion** construction or a full pivot to another orthogonal bucket
- so this cycle stayed in **Track C**, but took the cleanest remaining structural branch inside the relational space: test a **universe-level latent eigenfactor** rather than another local peer / graph heuristic

Work completed:
- added `examples/eigenfactor_residual_momentum_benchmark.rs`
- construction tested:
  - estimate the universe correlation matrix on the first **900 daily bars**
  - extract the dominant latent loading vector with simple **power iteration**
  - build a daily **eigenfactor** return series from those loadings
  - estimate rolling **252-bar** exposure to both **BTC** and that latent eigenfactor
  - compute **21-bar residual momentum** after removing both exposures
  - long the top **2** positive residuals, short the bottom **2** negative residuals
- same fair daily harness preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- compared directly against:
  - `CrossSectionalMomentum`
  - `MACD+Regime`
  - `Ensemble(Majority 2/3)`
- built and ran with:
  - `cargo build --example eigenfactor_residual_momentum_benchmark`
  - `cargo run --example eigenfactor_residual_momentum_benchmark --profile sweep 2>&1`
- new outputs written:
  - `snapshots/eigenfactor_residual_momentum_benchmark_latest.md`
  - `snapshots/eigenfactor_residual_momentum_benchmark_latest.csv`
  - `snapshots/eigenfactor_residual_momentum_benchmark_<UTC timestamp>.md`
  - `snapshots/eigenfactor_residual_momentum_benchmark_<UTC timestamp>.csv`

Key findings:
- this was another useful **negative breadth** result: the broader latent-factor construction still wins **0/6** universes overall
- it is somewhat more alive than several recent peer / cluster / graph variants, but still clearly secondary:
  - `Base5`: **+1213.0%**, **2/4** quarter windows, **12/15** resamples
  - `NoDOGE`: **+868.2%**, **2/4**, **12/15**
  - `Legacy5BNB`: **+1233.2%**, **3/4**, **12/15**
  - `LargeCaps5`: **+1717.9%**, **3/4**, **14/15**
- harsher baskets still reject it as a leader:
  - `Legacy4`: only **+693.4%**, **3/4**, **9/15**
  - `OldGuardNoBNB`: only **+553.7%**, **3/4**, **10/15**
- universe wins stayed:
  - `MACD+Regime`: **3/6**
  - `Ensemble(Majority 2/3)`: **2/6**
  - `CrossSectionalMomentum`: **1/6**
  - `EigenfactorResidualMomentum`: **0/6**

Honest read:
- this was worth doing because it closes one of the last obvious “maybe the graph was just too local” loopholes
- broadening from peer / cluster / graph heuristics to a **universe-level latent factor** makes the relational bucket less sparse, but still does **not** create a new leader
- that is useful narrowing evidence: the current daily relational family is **not** failing only because the local structure guesses were crude

Overfitting / trust risk:
- the trap from here would be to keep nudging factor count, loading signs, or factor normalization until one table looks flattering
- that would be the wrong lesson: the honest result is that even a broader latent-factor view remains **secondary** under the current fair harness

What’s next:
- do **not** spend the next session rescuing this eigenfactor construction with nearby tweaks
- if relational work returns, it should be through a genuinely richer **multi-factor / diffusion / economically-structured** model
- otherwise the better rebalance is another orthogonal **Track C** bucket entirely so relational work does not become the next long basin

## 15:47 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions had already spent too much time on the **rolling linear-model trust stack**, and the most recent Track C work had been two negative **simple peer-cluster** probes
- that concentration was no longer the highest-value use of time, but the relational bucket still had one honest structural follow-up left: test a **broader latent-cluster / sector-style** grouping rather than tiny stable peer pairs
- so this cycle stayed in **Track C**, but used a more structural cluster construction instead of reopening the richer-model trust loop or tweaking the existing benchmark leaders

Work completed:
- added `examples/latent_cluster_residual_momentum_benchmark.rs`
- construction tested:
  - estimate early-window return correlations across each stress universe
  - choose the **least-correlated pair** as two latent cluster seeds
  - assign each symbol to the closer seed to create broad same-cluster groups
  - compute **21-bar residual momentum = symbol return minus same-cluster mean return**
  - long the top **2** positive residuals, short the bottom **2** negative residuals
- same fair daily harness preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- compared directly against:
  - `CrossSectionalMomentum`
  - `MACD+Regime`
  - `Ensemble(Majority 2/3)`
- built and ran with:
  - `cargo build --example latent_cluster_residual_momentum_benchmark`
  - `cargo run --example latent_cluster_residual_momentum_benchmark --profile sweep 2>&1`
- new outputs written:
  - `snapshots/latent_cluster_residual_momentum_benchmark_latest.md`
  - `snapshots/latent_cluster_residual_momentum_benchmark_latest.csv`
  - `snapshots/latent_cluster_residual_momentum_benchmark_<UTC timestamp>.md`
  - `snapshots/latent_cluster_residual_momentum_benchmark_<UTC timestamp>.csv`

Key findings:
- this was another useful **negative breadth** result: the broader latent-cluster construction still wins **0/6** universes overall
- representative rows:
  - `Base5`: **+1565.7%**, **2/4** quarter windows, **10/15** resamples — more alive than the simple peer-pair version, but still clearly behind the yardsticks
  - `NoDOGE`: **+911.1%**, **2/4**, **12/15** — secondary only
  - `Legacy4`: **+892.9%**, **3/4**, **12/15** — respectable, not a winner
  - `Legacy5BNB`: **+852.1%**, **3/4**, **12/15** — still below both `Ensemble(Majority 2/3)` and `CrossSectionalMomentum`
  - `OldGuardNoBNB`: **-74.2%**, **2/4**, **7/15** — the harsher old-guard basket actively rejects the family
  - `LargeCaps5`: **+1326.2%**, **3/4**, **14/15** — decent niche, but still not enough to lead
- cluster memberships themselves were broader and less sparse than the earlier peer-pair family, so this was a cleaner structural test than just changing residual sign or peer count

Honest read:
- this is useful because it closes another obvious rescue path honestly
- simple peer pairs were too sparse; broader two-cluster grouping is more expressive, but still **not** a new breadth leader and not robust enough to displace the current yardsticks
- that means the relational bucket probably needs a genuinely richer **graph / latent-factor / sector-residual** model if it is going to matter

Overfitting / trust risk:
- the trap from here would be to spend several cycles nudging cluster count, seed choice, or assignment rules until one table looks flattering
- that would be the wrong lesson: the honest result is that a simple daily **two-cluster residual momentum** expression is still secondary and sometimes actively wrong

What’s next:
- do **not** reopen the rolling linear-model trust loop by default
- do **not** spend the next session rescuing this exact two-cluster construction
- better next Track C moves are either:
  - a genuinely richer **graph / latent-factor / sector-residual** relational model, or
  - another orthogonal breadth bucket entirely if that looks higher-value after the next focus check
