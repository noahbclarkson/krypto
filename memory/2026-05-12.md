## [00:30 UTC] T97 — FRESHNESS_COOLDOWN=93 REVERTED via Held-Out Validation

**T97 executed.** Built `examples/t97_fc_held_out.rs` — pre-2021 held-out validation for FC=93.

**Result: CATASTROPHIC FAILURE**

```
FC=  0: 20/34 pass | Sharpe +1.540 | Return +29.9 | DD 22.3 | 160 trades
FC= 93:  0/34 pass | Sharpe +0.000 | Return +0.0   | DD  0.0 |   0 trades
```

FC=93 produces ZERO trades on pre-2021 held-out data. 93-bar (~3 months) freshness cooldown is too aggressive for the held-out era — no re-entries occur in the test windows. FC=93 was an in-sample artifact (same false-positive pattern as EP=24, HAP=0.09).

**Action taken:** FC=0 in both `src/live/bot.rs` and `src/live/config.rs`.

**Exact-live verification:** 2.76x / Sharpe 1.02 / MaxDD 22.3% / 286 trades — unchanged (freshness cooldown doesn't fire often, so revert has no equity impact).

**Key finding:** In-sample sweeps can produce mechanisms that produce 0 trades in held-out regimes. The in-sample "robustness winner" (FC=83% pass vs FC=0 51.9% pass) was a statistical mirage — the FC=93 mechanism only fires in the specific in-sample era where some symbols had exactly 93-bar gaps between exits. Pre-2021, the market dynamics produce longer gaps, so FC=93 blocks all entries.

**Files:** `examples/t97_fc_held_out.rs`, `snapshots/t97_fc_held_out.csv`
**Commit:** pending

## [21:01 UTC] Cron — Research Complete, Escalation Blocked

**State:** Research loop CLOSED. All params frozen. Waiting on testnet keys (5+ weeks).

**Actions:**
1. Confirmed progress chart fix (live bot ~2.76x line) already committed (`8e650c73`)
2. Verified exact-live equity: 2.76x / Sharpe 1.02 / MaxDD 22.3% / 286 trades
3. Confirmed config frozen: ATR_RANK_THRESHOLD=5.0, REGIME_ATR_PERIOD=17, etc.
4. Attempted escalation to Arc on testnet blocker — rate limited

**Blocker:** Binance testnet API keys — 5+ weeks

**Remaining work:** None. All simulation questions answered. Only live execution advances the project.