# Hyperparameter Optimization Report — TURTLE_ATR_MULT Fine Sweep
**Session:** 2026-04-30 01:10 UTC | Kira hyperopt cycle  
**Mission:** Audit hardcoded constants, isolate undertested assumptions, run extensive grid sweep, export time-series equity chart.

---

## Parameter Audit

Audited validated Turtle+Chandelier production constants in `examples/turtle_chandelier_walkforward.rs` and `src/live/config.rs` lineage:

| Parameter | Current | Prior coverage | Status |
|---|---:|---|---|
| EP | 21 | full 5..100 step 1 + held-out revert | frozen |
| CHAND_PERIOD | 7 | 5..60 step 2 + held-out confirmation | frozen |
| CHAND_MULT | 2.30 | 1.50..5.00 step 0.05 | frozen |
| HOLD_MAX | 12 | 5..180 broad sweep + fine region | frozen |
| ATR_ENTRY_MULT | 0.00 | dense/current sweep; non-zero rejected | frozen |
| POSITION_CAP | 3 | 1..10 | frozen |
| VOL_LOOKBACK | 8 | 1..100 step 1 current params | frozen |
| ATR_EMA_PERIOD | 1 | 1..200 step 1 | frozen |
| FRESHNESS_COOLDOWN | 0 | 0..70 step 5 | frozen |
| **TURTLE_ATR_MULT** | **2.00** | only coarse {1.0,1.5,...,5.0} | **selected** |

**Chosen parameter:** `TURTLE_ATR_MULT` because it is part of the HALL_OF_FAME validated live variant but was only tested on a coarse 0.5 grid. It could materially affect exit timing if the Turtle ATR exit binds before Chandelier/HOLD_MAX.

---

## Harness + Charting Updates

Built `examples/turtle_atr_mult_dual_fine_sweep.rs`:
- Sweeps **M = 0.5..5.0 step 0.1** (46 values)
- Runs full **9 universes × 6 walk-forward windows = 54 windows/value**
- Records return, Sharpe, max DD, trades, win rate, pass/fail for every parameter/window
- Exports actual Base5 compounded time-series equity curves for every multiplier

Outputs:
- Metrics: `krypto/snapshots/turtle_atr_mult_dual_fine_sweep.csv`
- Summary: `krypto/snapshots/turtle_atr_mult_dual_fine_summary.csv`
- Equity curves: `krypto/snapshots/turtle_atr_mult_dual_fine_equity.csv`
- Harness report: `krypto/snapshots/turtle_atr_mult_dual_fine_report.md`

Built `krypto/charts/plot_turtle_atr_mult_fine.py`:
- Reads summary + equity CSVs
- Plots baseline/winner/representative runner-ups as line curves
- Uses dynamic log-scale Y-axis, clear legend, axes, title, and grid
- Output chart: `krypto/charts/comparison_chart.png`

---

## Results

**Sweep range:** 46 values, M=0.5 through M=5.0 in 0.1 increments.  
**Total validation runs:** 46 × 9 × 6 = **2,484 walk-forward runs**.

Every tested multiplier produced **identical** results:

| Metric | All M values |
|---|---:|
| Pass count | 40/54 |
| Pass rate | 74.0741% |
| Positive universes | 9/9 |
| Avg Sharpe | 3.147143 |
| Avg return | +105.3297% |
| Worst DD | 79.3294% |
| Trades | 721 |
| Avg win rate | 48.2043% |

**Interpretation:** `TURTLE_ATR_MULT` is non-binding under current production params. The Chandelier(7,2.30) and HOLD_MAX=12 exits dominate before the Turtle ATR multiplier can alter any trade outcome. This is a null/invariance result, not a new edge.

---

## Stable Default Decision

**No default change.** Keep `TURTLE_ATR_MULT = 2.00` for compatibility and semantic clarity.

Reasoning:
- All 46 values tie exactly, so choosing M=0.5 as a numeric “winner” would be arbitrary.
- M=2.00 is the existing frozen baseline and remains robust.
- The real finding is not “2.00 is uniquely optimal”; it is “this parameter is currently inert/non-binding.”

---

## Verification

Re-ran current full validation harness after adding the sweep/charting harness:

Command:
```bash
cargo run --example turtle_chandelier_walkforward --profile sweep
```

Verification result:
- Base5: **6/6 pass**
- Global 9-universe: **40/54 pass**
- Avg Sharpe: **3.147143**
- Total trades: **721**

No validated results broke. Production defaults remain frozen.

---

## Chart

`/home/ubuntu/.openclaw/workspace-krypto/krypto/charts/comparison_chart.png`

The chart is an actual line graph of Base5 compounded walk-forward equity over time. The plotted curves overlap because all tested multiplier values produce identical equity paths.

---

# Hyperparameter Optimization Report — HOLD_MAX Current-Params Full Sweep
**Session:** 2026-04-30 04:05 UTC | Kira hyperopt cycle  
**Mission:** Audit hardcoded constants, run an extensive current-parameter sweep, export real equity time-series, and verify validated results.

## Parameter Audit

Selected **`HOLD_MAX`** because it is a HALL_OF_FAME/live production constant (`HOLD_MAX=12`) and the source comment still references older optimization context. It materially affects exit timing and could plausibly interact with current Chandelier(7,2.30), EP=21, ATR_ENTRY_MULT=0.00, and VOL_LOOKBACK=8.

Other production constants audited:
- `EP=21` — full 5..100 + held-out revert; frozen
- `CHAND_PERIOD=7` — extensive sweep; frozen
- `CHAND_MULT=2.30` — 1.50..5.00 step 0.05; frozen
- `TURTLE_ATR_PERIOD=24` — fine sweep; frozen
- `TURTLE_ATR_MULT=2.00` — fine sweep today showed inert under current exits; frozen
- `ATR_ENTRY_MULT=0.00` — 0.00..2.00 step 0.01; frozen
- `POSITION_CAP=3` — 1..10; frozen
- `VOL_LOOKBACK=8` — 1..100 step 1; frozen
- `ATR_EMA_PERIOD=1` — 1..200 step 1; frozen

## Harness + Charting

Built:
- `krypto/examples/hold_max_current_full_sweep.rs`
- `krypto/charts/plot_hold_max_current_full.py`

Outputs:
- Metrics: `krypto/snapshots/hold_max_current_full_sweep.csv`
- Summary: `krypto/snapshots/hold_max_current_full_summary.csv`
- Time-series equity curves: `krypto/snapshots/hold_max_current_full_equity.csv`
- Chart: `krypto/charts/comparison_chart.png`

The chart is a real line graph of Base5 compounded walk-forward equity over time. It plots the baseline (`HM=12`), the numeric Sharpe winner (`HM=42`), and runner-ups (`HM=39`, `HM=44`) with dynamic log Y-axis scaling, legend, labels, title, and grid.

## Sweep Design

- Parameter: `HOLD_MAX`
- Range: **1..=100 step 1** (100 values)
- Validation: **9 universes × 6 walk-forward windows = 54 windows/value**
- Total runs: **5,400 walk-forward simulations**
- Current production params: `EP=21`, `CHAND_PERIOD=7`, `CHAND_MULT=2.30`, `TURTLE_ATR_PERIOD=24`, `TURTLE_ATR_MULT=2.00`, `ATR_ENTRY_MULT=0.00`, `VOL_LOOKBACK=8`, `POSITION_CAP=3`

## Results

Top by average Sharpe:

| Rank | HOLD_MAX | Avg Sharpe | Pass | Avg Return | Avg DD | Trades |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 42 | 4.4754 | 35/54 (64.8%) | +91.1% | 29.5% | 503 |
| 2 | 39 | 4.3616 | 36/54 (66.7%) | +91.0% | 30.4% | 506 |
| 3 | 44 | 4.3016 | 33/54 (61.1%) | +86.2% | 29.5% | 502 |
| baseline | 12 | 3.1471 | 40/54 (74.1%) | +105.3% | 35.4% | 721 |

Top by pass rate:

| HOLD_MAX | Pass | Avg Sharpe | Avg Return | Avg DD |
|---:|---:|---:|---:|---:|
| 1 | 43/54 (79.6%) | 3.1457 | +71.6% | 26.5% |
| 2 | 43/54 (79.6%) | 3.1147 | +61.7% | 26.2% |
| 6 | 41/54 (75.9%) | 3.5669 | +71.2% | 34.2% |
| 12 | 40/54 (74.1%) | 3.1471 | +105.3% | 35.4% |

## Interpretation

The numeric Sharpe winner (`HM=42`) is **not robust enough to promote**: it improves average Sharpe but drops pass rate from 40/54 to 35/54 and gives up return. The high-pass short-hold cluster (`HM=1..6`) improves pass rate or Sharpe in places but materially sacrifices return and changes the trade character.

`HM=12` remains the best stable production compromise: not the top single metric, but it preserves the validated 40/54 pass rate and has the strongest return among the robust/high-pass candidates. This is a classic anti-overfit decision: do not promote a same-grid Sharpe winner when robustness deteriorates.

## Stable Default Decision

**No source default change. Keep `HOLD_MAX = 12`.**

Reason:
- `HM=42` is an in-grid Sharpe winner but fails robustness discipline (pass rate down 9.3pp vs baseline).
- `HM=1/2/6` improve pass rate or Sharpe but cut average return sharply.
- Existing default preserves the HOF validated profile.

## Verification

Re-ran authoritative harness after the sweep:

```bash
cargo run --example turtle_chandelier_walkforward --profile sweep
```

Verified unchanged baseline:
- Base5: **6/6 pass**
- Global 9-universe: **40/54 pass**
- Avg Sharpe: **3.147143**
- Total trades: **721**

No validated results broke.
