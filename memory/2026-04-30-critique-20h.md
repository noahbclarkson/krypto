# Memory — 2026-04-30 20:05 UTC — Critique Cycle

**Session: Seventh Critique Cycle | Kira | 2026-04-30 20:05 UTC**

---

## Orient

- Git: `1cfefd93` latest. ATR_RANK=5 integrated into config.rs AND bot.rs (check_atr_rank_entry_filter wired into live entries). REGIME_ATR_PERIOD=12 and REGIME_LOOKBACK=42 in config.rs.
- Equity: TURTLE collapsed 221.5x → 124.1x (Sharpe 1.04 → 1.00) — after ATR_RANK=5 was added to progress harness. ATR_RANK=5 is net NEGATIVE on the equity curve harness despite being validated as net POSITIVE on the Turtle-only walkforward harness. This is a CRITICAL inconsistency.
- Discord: No new messages from Noah.
- S6 close_losers I=5 Turtle-only validation: STILL PENDING after 3+ sessions.

---

## Brutal Critique: What's Actually Good vs. What We're Convincing Ourselves About

### ✅ Genuinely Good

1. **ATR_RANK=5 actually integrated this session.** config.rs has ATR_RANK_THRESHOLD=5.0, REGIME_ATR_PERIOD=12, REGIME_LOOKBACK=42. bot.rs has check_atr_rank_entry_filter() wired into should_enter(). This is real progress — the Discord-announcement-then-forget pattern was broken.
2. **Fee accounting fixed.** Entry × (1+fee), exit × (1-fee). Honest numbers.
3. **Anti-overfit discipline held.** VL=90 found, correctly flagged as same-harness artifact risk. No promotion.
4. **Regime ATR AP=12 is in config.rs** as REGIME_ATR_PERIOD=12. Not yet wired into bot.rs as the live ATR period source — but it's no longer a pure discovery.

### 🚨 CRITICAL: ATR_RANK=5 Appears to Be Net Negative on Equity Curve

The most urgent problem: Adding ATR_RANK=5 to `progress_equity_curves.rs` (commit d6c7f7e1) caused:
- Equity: 221.5x → 124.1x (Sharpe 1.04 → 1.00)
- A subsequent refresh (a6484c45 → 1cfefd93) pushed it further to 110.9x

This is NOT a data update. The `progress_equity_curves.rs` harness runs on FULL HISTORY (no OOS split). Adding ATR_RANK=5 filtered entries across 8 years of data and REDUCED cumulative returns by 47%.

**Yet ATR_RANK=5 was validated as +24% Sharpe under Turtle-only walkforward.**

The gap: Walk-forward uses 252-bar windows (recent data). The equity harness uses full history (2018-2026). ATR_RANK=5 blocks entries in low-vol chop regimes — which may have been the ONLY profitable periods in the full history test. On 2018-2026 data, crypto's best periods were high-vol crashes (COVID, 2022) — exactly the high-vol regimes ATR_RANK=5 would PASS. But in low-vol chop regimes (2021, 2023-2024), it would block Turtle breakouts that eventually become profitable.

**This means ATR_RANK=5 is time-period dependent.** Validated on recent OOS walk-forward windows (2020-2026), but HARMFUL on full history including 2018-2019 and late 2024-2026.

**Action required:** Either (a) revert ATR_RANK=5 from progress_equity_curves.rs (it's a live-trading filter, not a full-history backtest parameter), or (b) document that ATR_RANK=5 improves recent OOS Sharpe but degrades full-history equity. Do not silently leave the harness broken.

### ⚠️ We Are Convincing Ourselves On

1. **"ATR_RANK=5 validated" — validated HOW?** Under Turtle-only exit logic on 9-universe walkforward. But the live bot's progress harness uses a different entry/exit path than the dedicated turtle_only_atr_rank_sweep.rs. The two harnesses have DIFFERENT trade counts, different equity curves, different window splits. ATR_RANK=5 winning in harness A and losing in harness B is a harness inconsistency, not a validation.

2. **"Regime ATR AP=12 found" — AP=12 in config.rs is NOT the same as AP=12 as the live ATR period.** config.rs has REGIME_ATR_PERIOD=12 as a REGIME DETECTOR parameter. The live bot still uses TURTLE_ATR_PERIOD=24 as the actual Turtle stop ATR. The AP=12 mechanism is used only for the ATR rank entry filter, not for the Turtle ATR exit. The "biggest finding in months" is not actually deployed as the mechanism we thought it was.

3. **S6 close_losers I=5: still pending after 3 sessions.** The candidate was found April 30 midday. Every critique cycle since then has said "needs Turtle-only validation." No evidence it was ever run. This is avoidance, not rigor.

### 🚨 Genuine Blind Spots

| Blind Spot | Severity | Status |
|---|---|---|
| **ATR_RANK=5 equity collapse unexplained** | CRITICAL | 221.5x → 124.1x after adding filter to harness. Harness inconsistency or real degradation. Must diagnose before next session. |
| **S6 close_losers Turtle-only: never run** | HIGH | Found April 30 midday. 3 critique cycles have flagged it. Never executed. |
| **Regime ATR AP=12 NOT used as live ATR period** | HIGH | config.rs has it as regime detector param. TURTLE_ATR_PERIOD=24 is still the live stop ATR. The "biggest finding" is partially deployed. |
| **Only testing bull/bear regimes — not chop** | MEDIUM | 2021 was chop. Turtle lost badly (-16.5% vs BTC). USDT hedge helps but the strategy fundamentally whipsaws in chop. No solution tested. |
| **Live testnet: 4+ weeks blocked** | CRITICAL | No change. All metrics remain simulation bounds. |

---

## The 3 Most Promising Unbuilt Ideas

### 1. Regime ATR AP=12 as the Live Turtle ATR Stop — UNBUILT, GENUINELY NOVEL

config.rs has REGIME_ATR_PERIOD=12 but it's not wired as the live Turtle ATR period. The live bot uses TURTLE_ATR_PERIOD=24. The regime ATR sweep (da6c8b9a) found AP=12 as a joint winner (AP/LB/T) with +78% Sharpe improvement. But this was for the regime detector ATR percentile, not for the Turtle ATR stop.

The idea: use a shorter ATR period (12) as the actual Turtle stop in high-vol regimes. Currently, TURTLE_ATR_PERIOD=24 is a FIXED parameter. Making it regime-adaptive (12 in high-vol, 24 in low-vol) could tighten stops in trending regimes without losing signals in ranging regimes.

What to build:
- Modify `examples/turtle_chandelier_walkforward.rs` to use REGIME_ATR_PERIOD=12 as the Turtle ATR stop (not just the rank filter source)
- Compare against TURTLE_ATR_PERIOD=24 baseline
- Run 9-universe × 6-window validation

**Risk:** This is the 6th parameter we've tried to make adaptive. All prior adaptive mechanisms (vol-contingent Chandelier multiplier, vol-rank A/D×Turtle switching, etc.) failed. The difference: this changes the ATR calculation period, not just a threshold. Possible but must be validated.

### 2. S6 close_losers I=5 Turtle-Only Validation — OVERDUE, ONE RUN AWAY

Found April 30 midday: close_losers I=5 improved pass rate 48/54 vs baseline 46/54, Sharpe +3.067 vs baseline. This was under the rebalancing_9universe harness (dual-exit). Turtle-only validation was never run.

What to build:
- `examples/rebalancing_turtle_only.rs` — add rebalancing logic to the Turtle-only walkforward harness
- OR modify `examples/turtle_chandelier_walkforward.rs` to support turtle_only mode + rebalancing
- Sweep I ∈ {3, 5, 7, 10} on Turtle-only logic

This is genuinely untested. Close_losers means: after a losing position is held for I bars, close it (stop waiting for the Chandelier). This is different from trim_losers (REJECTED — identical Sharpe, lower return). The distinction: closing removes capital from losing trades faster, potentially improving capital efficiency.

### 3. ATR_RANK=5 Equity Harness Fix + Choppy-Regime Analysis — DIAGNOSTIC

Before adding more mechanisms, we need to understand WHY ATR_RANK=5 caused the equity to collapse from 221.5x to 124.1x on the progress harness.

The hypothesis: ATR_RANK=5 blocks entries in low-vol regimes (exactly when Turtle entries are most profitable as reversals). In high-vol regimes (crashes), ATR rank is high so the filter passes — but Turtle also gets stopped out faster in high-vol.

What to build:
- Split progress_equity_curves.rs into two runs: with ATR_RANK=5 and without
- Compare per-year performance, not just cumulative equity
- This will reveal which years ATR_RANK=5 helps vs hurts

This is the diagnostic step before deciding whether ATR_RANK=5 belongs in the production harness at all, or whether it's only appropriate as a live-trading filter (not backtestable on full history).

---

## Reports Directory Health Check

- `daily_progress.csv`: Two different equity numbers for Turtle+Chandelier on the same date (2026-04-29: 221.5x; 2026-04-30: 124.1x). This is because the 2026-04-29 entry was the stale run before ATR_RANK=5 was added. The 2026-04-30 entry is the ATR_RANK=5 run. These are NOT comparable. The decline is NOT real — it's a harness change.
- `snapshots/progress_equity_curves.md`: Shows 110.9x as current. Was 124.1x in earlier refresh. Still changing.
- **The equity collapse is an artifact of adding ATR_RANK=5 to the harness, NOT a real performance decline.**

**Fix needed:** `progress_equity_curves.rs` should run both the baseline (no ATR rank) and the ATR_RANK=5 variant, so they can be compared on the same harness. Currently only the ATR_RANK=5 version is running.

---

## Biggest Blind Spot: We Keep Adding Filters That Look Good in Isolation and Bad in Combination

Pattern:
1. ATR_ENTRY_MULT — NULL (rejected)
2. Vol confirmation — REJECTED
3. ATR_RANK=5 — validated in isolation, collapsed equity in combination

Every filter that gates Turtle entries looks good in a dedicated harness because it removes bad trades. But in combination with other filters, or applied to full-history data, the filtering compounds and removes good trades too. The equity collapse (221.5x → 124.1x) is the most dramatic example.

The lesson: test combinations, not individual components. ATR_RANK=5 + existing strategy was never tested as a joint system on full history before being added to the harness.

---

## Project Status (2026-04-30 20:05 UTC)

**Research loop: CONFIRMATION SPIRAL (unresolved).**
- ATR_RANK=5 integrated into live bot (config.rs + bot.rs ✅) but broken in progress harness
- Regime ATR AP=12 partially deployed (config.rs ✅, live stop ATR still TURTLE_ATR_PERIOD=24 ❌)
- S6 close_losers Turtle-only: PENDING after 3 sessions
- Equity harness needs immediate fix (ATR_RANK=5 broken, stale numbers being reported)
- **Only live testnet advances the project (BLOCKED 4+ weeks on Noah's API keys)**
