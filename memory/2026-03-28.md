# 2026-03-28

## 02:55 UTC — Cron summary

Oriented first: reread PLAN.md, MEMORY.md, and 2026-03-27 memory; pulled latest `v2-rewrite`; checked recent commits and #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions were mostly **Track C**, but clustered around **daily family-combination / macro-state interpretation**
- that is healthier than the old MACD-overfocus, but it is still a local basin
- highest-value use of this cycle looked like a **Track A × Track C bridge**: test whether the macro-stress attribution actually helps **portfolio sizing / drawdown realism**, not whether another switcher tops a table

Work completed:
- added `examples/exogenous_portfolio_overlay_audit.rs`
- tested lagged macro-conditioned capped-book overlays on current family yardsticks:
  - `Baseline(1.0 all states)`
  - `StressTilt(1.0 stress / 0.5 else)`
  - `StressOnly(1.0 stress / 0.0 else)`
- same fair daily harness: signal at close, next-open entry, 21-bar hold, 0.1% taker each side, top-3 strength-capped book
- snapshot artifacts written:
  - `snapshots/exogenous_portfolio_overlay_latest.md`
  - `snapshots/exogenous_portfolio_overlay_latest.csv`
  - `snapshots/exogenous_portfolio_overlay_20260328T025543Z.md`
  - `snapshots/exogenous_portfolio_overlay_20260328T025543Z.csv`

Key findings:
- macro-stress awareness **does** matter for portfolio shape, but mostly as **risk control / sizing context**, not as a new return-maximizing rule
- `StressTilt` often improved or preserved Sharpe / max drawdown while giving up a lot less than `StressOnly`
- examples:
  - `Base5`: `Turtle+Regime+MACD` Sharpe **8.08 -> 8.22**, MaxDD **34.4% -> 30.8%**, but return falls **+1,631,496.3% -> +322,725.6%**
  - `Legacy4`: majority ensemble MaxDD **44.1% -> 38.2%** with only a mild Sharpe slip **2.60 -> 2.55**
  - `OldGuardNoBNB`: `MACD+Regime` MaxDD **39.1% -> 34.6%**, Sharpe **4.21 -> 3.78**
  - `LargeCaps5`: `Turtle+Regime+MACD` MaxDD **27.8% -> 23.6%**, Sharpe **8.74 -> 8.54**
- `StressOnly` is too blunt as a hard gate: better DD in places, but too much return destruction
- baseline still owns most top raw-return rows, so this is not a new leader story

Meta read:
- good session because it pushed exogenous work toward **trust / risk budgeting** instead of another local switch-rule optimization
- but project focus should still rebalance soon; recent cycles have spent a lot of time on **daily family-combination / exogenous interpretation** and should not get trapped there

Files updated:
- `PLAN.md`
- `MEMORY.md`
- `memory/2026-03-28.md`

Need next:
- either continue exogenous work only via **state-conditioned drawdown attribution / exposure calibration**
- or pivot to another orthogonal breadth bucket rather than staying in the same local exogenous loop

## 06:00 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions had already broadened away from pure MACD-overfocus, but they were still clustering around **daily family-combination / exogenous interpretation**
- that was no longer the highest-value local use of time
- the most under-served credible question was a **Track A execution-realism** branch Noah explicitly wanted back in scope: whether current daily candidates change meaningfully under lower-fee maker-style execution
- to keep the comparison honest, I stayed inside the existing fair daily harness instead of mixing in the older stop-based passive engine

Work completed:
- added `examples/trend_fee_surface_audit.rs`
- same fair daily assumptions as the recent benchmark work:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - quarter-window + CPCV-style chronology checks
- fee scenarios tested:
  - `Taker/Taker` = **10 + 10 bps**
  - `MakerEntry/TakerExit` = **2 + 10 bps**
  - `Maker/Maker` = **2 + 2 bps**
- universes tested:
  - `Base5`
  - `NoDOGE`
  - `OldGuardNoBNB`
  - `LargeCaps5`
- snapshot artifacts written:
  - `snapshots/trend_fee_surface_latest.md`
  - `snapshots/trend_fee_surface_latest.csv`
  - `snapshots/trend_fee_surface_20260328T060425Z.md`
  - `snapshots/trend_fee_surface_20260328T060425Z.csv`

Key findings:
- on the stronger modern baskets, lower-fee execution mostly **raises return levels without changing trust**:
  - `Base5` `MACD+Regime`: **+3603.0% -> +3626.5% -> +3650.0%** from Taker/Taker to hybrid to Maker/Maker, while staying **4/4 quarter windows, 15/15 resamples** throughout
  - `LargeCaps5` `MACD+Regime`: **+2816.3% -> +2840.2% -> +2864.0%**, again with **4/4, 15/15** throughout
  - same general story for the majority ensemble and Turtle combo: level lift, but no chronology reclassification
- the more interesting effect is on weaker **old-guard breadth** rows where cheaper execution can move a candidate across pass thresholds:
  - `OldGuardNoBNB` majority ensemble: **+787.7% / 14/15** -> **+810.0% / 15/15** under `MakerEntry/TakerExit`
  - `OldGuardNoBNB` `CrossSectionalMomentum`: **+971.0% / 2/4 / 10/15** -> **+1049.9% / 4/4 / 11/15** under `Maker/Maker`
  - `OldGuardNoBNB` `Turtle+Regime+MACD`: **12/15 -> 13/15** resamples under `Maker/Maker`

Honest read:
- this is a useful **trust / execution** result, not a strategy-crowning result
- maker-style execution matters enough to keep in scope, but the dominant effect for current leaders is still **level assistance, not deeper trust**
- the only place execution looks potentially decision-relevant is on **borderline old-guard rows**, where lower fees may rescue or demote candidates near the robustness threshold

Overfitting / trust risk:
- it would be easy to turn this into another local fee-optimization basin and overread small level improvements
- the right lesson is not “maker saves the strategy”; it is “execution assumptions can change borderline verdicts, especially on harsher old-guard baskets”

What’s next:
- if passive work continues, do a **real passive-entry fill-model follow-up** on one or two borderline old-guard cases rather than a broad new fee grid
- otherwise pivot back to another orthogonal breadth bucket; do **not** let execution-fee tuning become the next local basin

## 09:27 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions were mostly **Track C breadth**, followed by two **Track A / execution-exogenous bridge** sessions
- that was healthier than the older MACD tunnel vision, but it was starting to cluster around **daily family-combination / fee-interpretation** work
- the highest-value under-served question from PLAN was the specific execution follow-up already identified: **does a real passive-entry fill model confirm or kill the old-guard borderline fee-surface improvements?**
- so I stayed in **Track A**, but in a narrow, decision-relevant way rather than another broad fee grid

Work completed:
- added `examples/oldguard_passive_entry_audit.rs`
- universe: `OldGuardNoBNB = ETH, XRP, LTC, EOS, BCH`
- strategies tested:
  - `MACD+Regime` (anchor / control)
  - `Ensemble(Majority 2/3)`
  - `CrossSectionalMomentum`
- scenarios compared:
  - `Taker/Taker (10 + 10 bps)` baseline
  - `PassiveEntry/TakerExit (real fill, 2 + 10 bps)`
- passive modeling details:
  - signal still formed at daily close
  - passive order must actually fill inside the **next daily bar** using **30m** candles
  - limit is **signal-anchored**, 3 ticks off, max wait = full next daily bar
  - exit stays the same fixed **21-bar daily-open** exit so this isolates **entry realism** rather than changing the whole harness
- snapshot artifacts written:
  - `snapshots/oldguard_passive_entry_latest.md`
  - `snapshots/oldguard_passive_entry_latest.csv`
  - `snapshots/oldguard_passive_entry_20260328T092701Z.md`
  - `snapshots/oldguard_passive_entry_20260328T092701Z.csv`

Key findings:
- **fill rate was 100%**, so the main difference came from actual fill price, not skipped trades
- the borderline rows **did not get cleaner** under real passive entry:
  - `Ensemble(Majority 2/3)`: **+797.6% / 3/4 / 14/15** taker -> **+718.4% / 4/4 / 13/15** passive-entry; chronology does **not** improve overall, and average entry edge is actually **-13.2 bps**
  - `CrossSectionalMomentum`: **+971.5% / 3/4 / 11/15** taker -> **+871.7% / 3/4 / 11/15** passive-entry with **-34.7 bps** average entry edge
- the control row is more nuanced:
  - `MACD+Regime`: **+1140.1% / 4/4 / 15/15** taker -> **+1171.7% / 3/4 / 15/15** passive-entry with **+20.5 bps** average entry edge
  - so real passive entry can help some rows, but it still did **not** create a broader trust upgrade

Honest read:
- this is exactly the kind of result the fee-surface audit needed
- the earlier old-guard threshold improvements were **mostly not confirmed** once “maker-style entry” meant an actual passive fill path instead of just cheaper fees
- passive-entry realism is still worth keeping in scope, but it should now be treated as **candidate-specific execution detail**, not a general rescue story for borderline old-guard breadth rows

Overfitting / trust risk:
- a fee table alone would have flattered the narrative that cheaper execution rescues weak rows
- this follow-up shows why execution realism has to model **fill path and price**, not just fee line items
- the passive setup here is still one declared model (signal-anchored, 3 ticks, full-bar wait), so it should inform trust rather than be over-read as the final execution truth

What’s next:
- do **not** spend another cycle locally optimizing passive-entry parameters on these same rows
- better next steps are either:
  - a more general **execution realism summary** that merges fee, latency, and passive-entry findings into one benchmark trust view, or
  - a deliberate pivot back to an orthogonal **Track C** bucket so execution work does not become the next local basin

## 12:41 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions had been healthier than the old MACD tunnel vision, but they were still clustering around **execution / exogenous-local follow-ups**
- the highest-value missing piece was not another fee or passive variant; it was a **single integrated trust read** so the project stops carrying latency, fee, and passive-entry results as disconnected narratives
- I stayed in **Track A**, but specifically to close that summary gap and make the next pivot back to breadth cleaner

Work completed:
- added `examples/execution_trust_summary.rs`
- instead of re-running another heavy benchmark, it consumes the latest audit artifacts directly:
  - `snapshots/trend_portfolio_latency_latest.csv`
  - `snapshots/trend_fee_surface_latest.csv`
  - `snapshots/oldguard_passive_entry_latest.csv`
- new outputs written:
  - `snapshots/execution_trust_summary_latest.md`
  - `snapshots/execution_trust_summary_latest.csv`
  - `snapshots/execution_trust_summary_20260328T124119Z.md`
  - `snapshots/execution_trust_summary_20260328T124119Z.csv`
- built and ran with:
  - `cargo build`
  - `cargo run --example execution_trust_summary --profile sweep 2>&1`

Key findings:
- **latency is still the main trust separator** for the current trend leaders
  - `Turtle+Regime+MACD`: integrated latency score **0.363**, baseline/delay resamples **28/105 -> 30/105**, but still **latency-fragile in level terms**
  - `MACD+Regime`: score **0.289**, delayed universe wins look better than baseline, but aggregate chronology is still weak (**13/105 -> 15/105**)
  - `MACD`: lower integrated score (**0.221**) but best aggregate latency chronology counts (**31/105 -> 31/105**)
- **fees mostly lift level, not trust**, for the stronger rows
  - `MACD+Regime` average fee-surface return only shifts **+2405.3% -> +2428.9% -> +2452.6%** with **0** chronology improvements under the cheaper paths
  - `Turtle+Regime+MACD` averages **+1875.0% -> +1890.0% -> +1904.9%** with only **1** maker/maker resample improvement
- **real passive fills mostly refused to validate the old-guard fee uplift**
  - `Ensemble(Majority 2/3)`: **+797.6% -> +718.4%**, resamples **14/15 -> 13/15**
  - `CrossSectionalMomentum`: **+971.5% -> +871.7%**, resamples stay **11/15**
  - `MACD+Regime` is the only candidate-specific passive positive (**+20.5 bps** average entry edge; **+1140.1% -> +1171.7%**), but still slips **4/4 -> 3/4** on quarter windows

Honest read:
- this did **not** produce a new strategy result; it produced a better **research control surface**
- the cleaner project rule now is:
  1. **latency realism** is the dominant execution trust question
  2. **fee realism** is usually a level adjustment unless the row is already borderline
  3. **passive entry** should be treated as candidate-specific execution detail, not a broad rescue path

Overfitting / trust risk:
- without this consolidation it would be too easy to cherry-pick whichever recent execution table flatters a preferred row
- the summary is only as good as the snapshots it consumes, so it improves continuity and decision hygiene more than it “proves” any candidate

What’s next:
- the execution-summary gap is now closed enough to justify a **deliberate pivot back to under-served Track C breadth** next cycle unless a genuinely new execution hole appears
- avoid opening another local fee / passive optimization loop just because one row looks slightly nicer under a narrower lens

## 16:xx UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions had been healthier than the old MACD tunnel vision, but they were still clustered around **execution-trust cleanup** and **daily family-combination / exogenous interpretation**
- that was no longer the highest-value use of another cycle
- the most under-served credible next step in `PLAN.md` was a **more principled ticker-interaction follow-up**: test whether cross-impact survives when the blunt all-peer rule is replaced by a **cluster-aware / graph-proxy** construction instead of another local threshold tweak
- so I pivoted back to **Track C — Broaden edge discovery**

Work completed:
- added `examples/cross_impact_cluster_benchmark.rs`
- kept the same fair daily harness:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
- calibration design:
  - first **900 bars** only
  - lagged **3-bar** peer returns
  - for each target, rank peers by **positive lagged-return correlation** plus **positive alignment** with the target’s future **21-bar** next-open return
- compared:
  - `CrossImpact slow all`
  - `CrossImpact sparse top2`
  - `CrossImpact cluster mean`
  - `CrossImpact cluster consensus`
  - `CrossSectionalMomentum`
  - `MACD+Regime`
- built and ran with:
  - `cargo build`
  - `cargo run --example cross_impact_cluster_benchmark --profile sweep 2>&1`

Key findings:
- `MACD+Regime` still wins **5/6** universes overall, so this is not a new breadth leader
- the earlier old-guard interaction positive survives best in the **broad-confirmation** versions, not the stricter cluster ones:
  - `OldGuardNoBNB`:
    - `CrossImpact slow all`: **+1521.6%**, **4/4**, **15/15**
    - `CrossImpact sparse top2`: **+1466.8%**, **4/4**, **15/15**
    - `CrossImpact cluster mean`: **+1194.9%**, **4/4**, **15/15**
    - `CrossImpact cluster consensus`: **+520.1%**, **2/4**, **15/15**
    - `MACD+Regime`: **+1104.6%**, **3/4**, **15/15**
- cluster-aware filtering helped some secondary baskets without becoming a winner:
  - `Base5`: cluster mean **+940.1%**, **2/4**, **10/15** vs all-peer **+377.1%**, **1/4**, **9/15**
  - `Legacy5BNB`: cluster mean **+654.2%**, **3/4**, **13/15** vs all-peer **+737.9%**, **3/4**, **10/15**
  - `NoDOGE`: all-peer stayed much cleaner (**+998.9%**, **3/4**, **15/15**) than the cluster variants

Honest read:
- this is a useful **negative / narrowing** breadth result
- the more principled cluster pass did **not** reveal a stronger target-local graph edge
- the earlier cross-impact positive now looks more like **broad peer confirmation / basket-level propagation** than a clean sparse or clustered interaction graph

Overfitting / trust risk:
- a family can look more convincing after attribution, yet still fail to get cleaner when the structure model becomes more principled
- that is exactly why this follow-up mattered: it prevented the project from over-reading the earlier legacy-link story

What’s next:
- do **not** spend the next cycle tuning this same daily cross-impact expression again
- if ticker-interaction returns, it should be through a genuinely different **graph / sector / residualized interaction** model
- otherwise the better move is another orthogonal Track C bucket rather than another local rescue attempt

## 19:13 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions were mostly **execution-trust cleanup** plus **daily cross-impact / exogenous breadth**
- that was healthier than the old MACD tunnel vision, but it was still clustering in a few nearby basins
- the highest-value under-served move looked like a genuinely different **Track C structural bucket**, not another local trend / cross-impact refinement
- I chose a direct test of the roadmap’s basis/carry note: does the weak funding-dispersion family get materially cleaner if trades are forced to align with **actual funding-capture windows** instead of behaving like an always-on spread?

Work completed:
- added `examples/funding_event_conditioned_carry.rs`
- same structural universe / accounting discipline as the earlier carry work:
  - `BTCUSDT`, `ETHUSDT`, `SOLUSDT` on **4h** bars
  - signal at close
  - next-open entry
  - realized funding cashflows included during the hold
  - **0.1%** taker each side
  - rolling **BTC-beta-neutral** sizing
- trigger variants compared:
  - `AlwaysOn`
  - `EventCapture` = only trade when the **next funding print is imminent**
  - `EventCapture+Extension` = funding imminent + obvious crowded-side extension
- built and ran with:
  - `cargo build --example funding_event_conditioned_carry`
  - `cargo run --example funding_event_conditioned_carry --profile sweep 2>&1`

Key findings:
- event-conditioning helps **a bit**, but still does **not** rescue the family
- best `AlwaysOn` row:
  - **-4.8%** total
  - **48 trades**
  - **1/4** quarter windows
  - **1/15** resamples
- best `EventCapture` row:
  - **-3.6%** total
  - **69 trades**
  - **1/4** quarter windows
  - **5/15** resamples
- stricter `EventCapture+Extension` was effectively dead:
  - best row had only **1 trade** and **0/15** resamples
- realized funding contribution stayed tiny even in the improved rows (roughly **+0.1% to +0.2%** total), so the family is still mostly losing on **price path**, not because carry was omitted from accounting

Honest read:
- this is a good **narrowing / falsification** result, not a new edge
- aligning to real funding windows improves the diagnosis and modestly improves chronology, but the current **cross-perp funding-dispersion** expression is still too weak to prioritize

Overfitting / trust risk:
- it would be easy to overread the `1/15 -> 5/15` resample improvement as progress toward a viable carry family
- the honest interpretation is narrower: timing around real funding events matters somewhat, but **not enough** to turn this construction into a trustworthy signal

What’s next:
- if basis/carry returns, do **not** keep tuning nearby funding triggers
- better next steps are a genuinely different **perp-vs-spot basis**, **event-specific carry**, or another orthogonal Track C bucket
- keep breadth broad and avoid turning funding/carry into the next local rescue loop

## 22:xx UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions had spent most of their time on **execution-trust cleanup**, then **cross-impact / exogenous / carry breadth**
- that was much healthier than the old MACD tunnel vision, but still concentrated in a few nearby basins
- the highest-value under-served question from `PLAN.md` was a genuinely different **ticker-interaction** follow-up: does the old-guard cross-impact result survive after removing a simple **BTC market-mode** component, or was it mostly crypto beta in disguise?
- so I stayed in **Track C**, but chose a structural falsification test instead of another local rescue pass

Work completed:
- added `examples/cross_impact_residual_benchmark.rs`
- same fair daily harness as the other breadth scans:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- compared:
  - `CrossImpact slow`
  - `CrossImpact residual BTC`
  - `Residual BTC + MACD conf`
  - `CrossSectionalMomentum`
  - `MACD+Regime`
- residualization method:
  - each symbol’s lagged **3-bar** return is adjusted by a rolling **252-bar BTC beta** before peer aggregation
- built and ran with:
  - `cargo build --example cross_impact_residual_benchmark`
  - `cargo run --example cross_impact_residual_benchmark --profile sweep 2>&1`

Key findings:
- `MACD+Regime` still wins **4/6** universes overall, so this is **not** a new breadth leader
- but the old-guard interaction result is **not just BTC beta in disguise**:
  - `OldGuardNoBNB` residualized result still survives cleanly at **+1063.2%**, **4/4** quarter windows, **15/15** resamples
  - old blunt `CrossImpact slow` is still stronger there in raw return (**+1462.5%**), so the best old-guard edge still seems to need **broad peer confirmation**
- residualization materially improves several other baskets vs the old blunt rule:
  - `Base5`: **+795.4%**, **3/4**, **14/15** vs blunt **+299.8%**, **1/4**, **9/15**
  - `Legacy4`: **+525.9%**, **4/4**, **14/15** vs blunt **+37.8%**, **3/4**, **7/15**
  - `Legacy5BNB`: **+727.8%**, **3/4**, **14/15** vs blunt **+656.4%**, **2/4**, **10/15**
- residualization does **not** help everywhere:
  - `NoDOGE`: residualized weakens to **12/15** while blunt stays **15/15**
  - `LargeCaps5`: residualized drops to **+394.5%**, **2/4**, **9/15** while blunt stays stronger at **+1262.8%**, **3/4**, **12/15**
- `Residual BTC + MACD conf` is too selective to become the preferred expression on most baskets

Honest read:
- this is a good **structural narrowing** result, not a promotion result
- the earlier old-guard cross-impact win survives a simple market-mode removal, which means there is likely **some real interaction structure** there
- but the strongest expression still appears to rely on **basket context / broad peer confirmation**, not a neat market-neutral signal we should rush to privilege

Overfitting / trust risk:
- it would be easy to overread the residualized old-guard survival as proof that ticker-interaction is now a primary edge family
- the honest interpretation is narrower: residualization killed the “it’s just BTC beta” dismissal, but it did **not** turn cross-impact into a clean universal winner

What’s next:
- if ticker-interaction returns, move to a more principled **cluster / sector / residualized graph** model rather than another nearby variant of this same expression
- otherwise keep Track C broad and avoid letting interaction-model follow-ups become the next local optimization basin
