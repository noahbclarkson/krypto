# 2026-03-27

## 01:33 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, yesterday’s memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track A trust audits**, then a smaller **Track C breadth** block on ensembles and exogenous context.
  - That was no longer the highest-value use of time for this cycle.
  - We were at risk of drifting back into benchmark-leader maintenance unless breadth stayed active.
  - The most under-served roadmap bucket was **ticker-interaction / cross-impact**.
- Chose **Track C — Broaden edge discovery**.
- Built successfully with `cargo build`.
- Added a new breadth harness: `krypto/examples/cross_impact_benchmark.rs`.
  - Purpose: test a first **multi-peer cross-impact** family instead of the older single-benchmark lead-lag logic.
  - New signal idea: for each target asset, use the lagged move of the **rest of the basket** as the impulse, but only fire when the target itself has **not already moved too far**.
  - Same fair daily assumptions as the other breadth scans:
    - signal at close
    - next-open entry
    - fixed **21-bar** hold
    - **0.1%** taker each side
  - Stress universes tested:
    - `Base5`
    - `NoDOGE`
    - `Legacy4`
    - `Legacy5BNB`
    - `OldGuardNoBNB`
    - `LargeCaps5`
  - Variants tested:
    - `CrossImpact fast`
    - `CrossImpact slow`
    - `CrossImpact confirm` (peer impulse must agree with `MACD+Regime`)
- Ran `cargo run --example cross_impact_benchmark --profile sweep 2>&1`.
- Key findings:
  - `MACD+Regime` still wins **5/6** universes overall, so this is **not** a new benchmark leader.
  - But the family is not dead: `CrossImpact slow` wins the harsher **`OldGuardNoBNB`** basket with:
    - **+1521.6%**
    - **231 trades**
    - **58.4%** win rate
    - **4/4** positive quarter windows
    - **15/15** positive resamples
    - vs `MACD+Regime`: **+1104.6%**, **3/4**, **15/15**
  - `CrossImpact fast` is a respectable secondary expression on some mixed baskets:
    - `Base5`: **+939.5%**, **3/4**, **14/15**
    - `Legacy5BNB`: **+1239.0%**, **3/4**, **14/15**
  - `CrossImpact confirm` improved selectivity in places (e.g. `Legacy4` **62.3%** win rate) but traded too little / too inconsistently to become the best expression.
- Honest read:
  - This is a useful breadth result, not a promotion result.
  - The simple peer-impulse rule does **not** beat the current trend yardsticks overall.
  - But it does suggest that **legacy / old-guard propagation structure may be more real than broad modern-alt propagation** under the current daily fixed-hold setup.
- Overfitting / trust risk:
  - This family could still just be another way of packaging slower legacy-trend exposure.
  - The good sign is that it survived the harsher old-guard basket cleanly; the risk is that whole-basket averaging hides which peer relationships actually matter.
- What’s next:
  - If cross-impact continues, move to **peer attribution / cluster-aware propagation** rather than threshold polishing on whole-basket averages.
  - Keep breadth active; do **not** let the current benchmark leader quietly become “the strategy.”

## 04:33 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track A trust audits** and then a deliberate **Track C breadth rebalance** through ensemble / exogenous / cross-impact work.
  - That is closer to the right balance than earlier MACD over-focus, but the highest-value next step was still **Track C** rather than another benchmark-leader audit.
  - The open question from the last session was structural: was the `CrossImpact slow` old-guard win a real interaction pattern or just noisy all-peer averaging?
- Chose **Track C — Broaden edge discovery** via a structure / attribution pass on the new cross-impact bucket.
- Built successfully with `cargo build`.
- Added `krypto/examples/cross_impact_attribution.rs`.
  - Purpose: attribute `CrossImpact slow` by **target symbol** and **peer -> target** relationship under the same fair daily assumptions:
    - signal at close
    - next-open entry
    - fixed **21-bar** hold
    - **0.1%** taker each side
  - Focus universes: `Base5`, `Legacy5BNB`, `OldGuardNoBNB`, `LargeCaps5`
- Ran `cargo run --example cross_impact_attribution --profile sweep 2>&1`.
- Key findings:
  - The old-guard win does **not** look like generic basket averaging noise.
  - `OldGuardNoBNB` remains the strongest cross-impact basket:
    - **+1521.6%**
    - **231 trades**
    - **58.4%** win rate
  - The best links are concentrated in a small set of recurring legacy relationships:
    - `ETH -> XRP`: **+612.7%** over **51** trades
    - `BCH -> XRP`: **+535.7%** over **43** trades
    - `LTC -> XRP`: **+503.3%** over **48** trades
    - `ETH -> BCH`: **+307.7%** over **47** trades
    - `ETH -> EOS`: **+276.8%** over **49** trades
  - `LTC` is a useful example of why attribution matters:
    - as a **target**, it is weak on `OldGuardNoBNB` (**-10.0%**)
    - but as a **driver**, it still contributes positively to names like `XRP`, `BCH`, and `ETH`
  - Structure changes meaningfully by basket rather than looking universal:
    - `Base5`: mostly **ETH/XRP/SOL -> ADA** and **ETH/XRP -> SOL`
    - `Legacy5BNB`: mostly **BNB/ETH/LTC -> XRP** and **BNB/ETH/LTC -> EOS`
    - `LargeCaps5`: mostly **ETH/LTC/XRP -> ADA** and **ETH/LTC -> BNB / XRP`
- Honest read:
  - This is a useful positive breadth result because it says the family is **not just vague alt-beta in another wrapper**.
  - But it is still **basket-dependent** and not a new all-around benchmark leader.
- Overfitting / trust risk:
  - The current all-peer averaging rule is still too blunt; it likely mixes good peer links with weak ones.
  - The right next step is **sparse / cluster-aware propagation** rather than threshold polishing or strategy promotion.
- What’s next:
  - build a version where each target listens only to its strongest inferred peers / cluster drivers
  - compare that sparse variant against `MACD+Regime` and the majority ensemble on old-guard vs broad-alt baskets
  - keep breadth active; do not let the current benchmark leader quietly become “the strategy.”

## 07:47 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track A trust audits** and then a deliberate **Track C breadth rebalance** through ensembles, exogenous context, and cross-impact.
  - That balance is healthier than the earlier MACD over-focus, but the highest-value next step was still **Track C** rather than another trend-leader audit.
  - The open structural question from the last session was whether the cross-impact result survives once the blunt all-peer average is replaced by **sparse peer selection**.
- Chose **Track C — Broaden edge discovery**.
- Built successfully with `cargo build`.
- Added `krypto/examples/cross_impact_sparse_benchmark.rs`.
  - Purpose: follow up the first cross-impact breadth result without more threshold polishing.
  - New rule: for each target, use an early fixed **900-bar calibration window** to score each peer by how much its lagged **3-bar** return aligns with the target’s future **21-bar** next-open return, then keep only the **top-2** or **top-3** positive-score peers.
  - Compared:
    - `CrossImpact slow all`
    - `CrossImpact sparse top2`
    - `CrossImpact sparse top3`
    - `CrossSectionalMomentum`
    - `MACD+Regime`
  - Same fair daily assumptions as the earlier breadth scans:
    - signal at close
    - next-open entry
    - fixed **21-bar** hold
    - **0.1%** taker each side
  - Stress universes tested:
    - `Base5`
    - `NoDOGE`
    - `Legacy4`
    - `Legacy5BNB`
    - `OldGuardNoBNB`
    - `LargeCaps5`
- Ran `cargo run --example cross_impact_sparse_benchmark --profile sweep 2>&1`.
- Key findings:
  - The sparse variants did **not** become new breadth leaders.
  - Universe wins:
    - `MACD+Regime`: **5/6**
    - `CrossImpact slow all`: **1/6**
    - `CrossImpact sparse top2`: **0/6**
    - `CrossImpact sparse top3`: **0/6**
  - Important positive-but-limited read:
    - sparse selection improved several middling baskets versus blunt all-peer averaging:
      - `Legacy5BNB`: top3 **+985.3%**, **3/4** quarter windows, **11/15** resamples vs all-peer **+737.9%**, **10/15**
      - `Legacy4`: top2/top3 **+369.6%**, **3/4**, **11/15** vs all-peer **+108.8%**, **8/15**
      - `Base5`: top3 **+473.1%**, **12/15** vs all-peer **+377.1%**, **9/15**
  - But the most important stress case weakened:
    - `OldGuardNoBNB` had been the cleanest prior cross-impact win; sparse selection **reduced** it instead of strengthening it:
      - all-peer: **+1521.6%**, **231 trades**, **4/4**, **15/15**
      - sparse top2: **+851.9%**, **192 trades**, **3/4**, **15/15**
      - sparse top3: **+475.4%**, **165 trades**, **3/4**, **15/15**
  - Example selected peers show the calibration is doing something sensible rather than random:
    - `OldGuardNoBNB`:
      - `ETHUSDT <- LTCUSDT, BCHUSDT, XRPUSDT`
      - `XRPUSDT <- BCHUSDT`
      - `LTCUSDT <- BCHUSDT, XRPUSDT`
      - `BCHUSDT <- LTCUSDT, EOSUSDT, XRPUSDT`
    - but the sparse filter still failed to preserve the earlier old-guard edge magnitude.
- Honest read:
  - This is a useful **negative breadth result**.
  - The earlier cross-impact win now looks more dependent on **basket-level aggregation / breadth of peer confirmation** than on a clean sparse driver graph.
  - The family is **not dead** — sparse selection improved some secondary baskets — but it also did **not** validate the stronger “specific legacy links are enough by themselves” story.
- Overfitting / trust risk:
  - A positive attribution story can still be misleading if the edge disappears once the interaction graph is simplified.
  - This is exactly why breadth work needs follow-up structure tests instead of stopping at a nice-looking attribution table.
- What’s next:
  - Do **not** spend multiple sessions locally optimizing sparse cross-impact thresholds.
  - Better next breadth targets are either:
    - a more principled **cluster / graph-based interaction model**, or
    - another orthogonal bucket such as **richer exogenous conditioning**, **basis/carry**, or **meta-allocation / ensemble switching**.
  - Project focus should stay **broad**; this result is another reminder not to let any one candidate family quietly become “the strategy.”

## 10:58 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions had already rebalanced somewhat toward **Track C breadth**, but they were still clustered around **interaction / ensemble-adjacent** daily families.
  - That was healthier than the earlier pure trend-overfocus, but not the highest-value use of time for another cycle.
  - I chose to keep **Track C** active while pivoting away from another cross-impact rescue attempt.
- Chose **Track C — Broaden edge discovery** via the roadmap’s **meta-allocation / regime-switching** bucket.
- Built successfully with `cargo build`.
- Added `krypto/examples/meta_allocation_benchmark.rs`.
  - Purpose: test whether a **simple pre-declared family switcher** can improve breadth without fitted weights.
  - State driver: lagged **BTC** market state only.
  - State definitions:
    - **bull trend** = `BTC close > SMA200` and **63d momentum > 0`
    - **high vol** = BTC **21d realized vol** above its **252d rolling median`
  - Baselines compared:
    - `CrossSectionalMomentum`
    - `MACD+Regime`
    - `Turtle+Regime+MACD`
    - `Ensemble(Majority 2/3)`
  - New pre-declared switchers:
    - `Switch(Bull->Majority, else MACD)`
    - `Switch(Bull->TurtleCombo, else MACD)`
    - `Switch(HighVol->MACD, else CSM)`
  - Same fair daily assumptions as the other breadth scans:
    - signal at close
    - next-open entry
    - fixed **21-bar** hold
    - **0.1%** taker each side
  - Stress universes tested:
    - `Base5`
    - `NoDOGE`
    - `Legacy4`
    - `Legacy5BNB`
    - `OldGuardNoBNB`
    - `LargeCaps5`
- Ran `cargo run --example meta_allocation_benchmark --profile sweep 2>&1`.
- Key findings:
  - The existing **majority ensemble** still wins the most baskets overall: **3/6** universe wins.
  - The best switcher is `Switch(Bull->TurtleCombo, else MACD)`, which wins **2/6** universes:
    - `NoDOGE`: **+2194.4%**, **4/4** quarter windows, **15/15** resamples
    - `Legacy4`: **+1052.6%**, **4/4**, **15/15**
  - `Switch(Bull->Majority, else MACD)` is respectable but never the top row (**0/6** wins).
  - `Switch(HighVol->MACD, else CSM)` adds little and looks like the weak branch (**0/6** wins).
  - The harsher old-guard basket still favors plain `MACD+Regime`:
    - `MACD+Regime`: **+1189.2%**, **4/4**, **15/15**
    - `Ensemble(Majority 2/3)`: **+928.1%**, **3/4**, **14/15**
    - `Switch(Bull->TurtleCombo, else MACD)`: **+1088.6%**, **3/4**, **14/15**
- Honest read:
  - This is a useful **partial-positive / mostly-negative breadth result**.
  - Simple BTC-state meta-allocation is **not dead**, but it does **not** beat the plain majority ensemble cleanly enough to justify another nearby optimization cycle.
  - The only switcher worth remembering is the bull-state `Turtle+Regime+MACD` / otherwise `MACD+Regime` mix.
- Overfitting / trust risk:
  - Meta-allocation can easily become a prettier wrapper around the same strategy family if we start threshold-chasing.
  - The fact that the majority ensemble still wins more baskets than the switchers is a good warning not to let “state switching” become the next optimization rabbit hole.
- What’s next:
  - If this bucket continues, do **richer regime-conditioned allocation / attribution** rather than more tweaking of these same BTC-state thresholds.
  - Keep breadth broad; do **not** let the majority ensemble quietly become the next privileged default just because it keeps winning breadth tables.

## 14:10 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track C breadth**, but a lot of that breadth was clustering around **daily family-combination ideas** (ensemble, switchers, cross-impact follow-ups).
  - That was healthier than the earlier MACD-overfocus, but it was starting to become its own local basin.
  - I chose to keep **Track C** active while deliberately avoiding another cross-impact rescue pass or another tiny trend-family tweak.
- Chose **Track C — Broaden edge discovery** via a richer follow-up to the exogenous bucket.
- Built successfully with `cargo build`.
- Added `krypto/examples/exogenous_meta_allocation_benchmark.rs`.
  - Purpose: test whether **lagged macro state** can choose between existing families better than the earlier one-bit macro long-gates.
  - Macro inputs: local FRED caches for **SP500**, **VIX**, and **DXY**, aligned conservatively so each crypto bar only sees the latest macro close **strictly before** that crypto date.
  - Pre-declared macro states:
    - **MacroRiskOn** = `SP500 > SMA50` and `VIX < SMA20` and `DXY < SMA50`
    - **MacroStress** = `VIX > SMA20` or `DXY > SMA50`
  - Baselines compared:
    - `CrossSectionalMomentum`
    - `MACD+Regime`
    - `Turtle+Regime+MACD`
    - `Ensemble(Majority 2/3)`
  - New switchers:
    - `Switch(MacroRiskOn->Majority, else MACD)`
    - `Switch(MacroRiskOn->TurtleCombo, else MACD)`
    - `Switch(MacroStress->MACD, else CSM)`
  - Same fair daily assumptions as the other breadth scans:
    - signal at close
    - next-open entry
    - fixed **21-bar** hold
    - **0.1%** taker each side
  - Stress universes tested:
    - `Base5`
    - `NoDOGE`
    - `Legacy4`
    - `Legacy5BNB`
    - `OldGuardNoBNB`
    - `LargeCaps5`
- Ran `cargo run --example exogenous_meta_allocation_benchmark --profile sweep 2>&1`.
- Key findings:
  - The best new rule is **`Switch(MacroRiskOn->Majority, else MACD)`**, which ties the best universe-win count at **2/6**, alongside the plain majority ensemble.
  - It improves the broad-alt baskets modestly while keeping chronology clean:
    - `Base5`: **+3812.6%**, **4/4** quarter windows, **15/15** resamples vs majority **+3784.8%** and `MACD+Regime` **+3781.1%**
    - `NoDOGE`: **+2277.6%**, **4/4**, **15/15** vs `MACD+Regime` **+2178.7%**
  - But it does **not** beat the current leaders cleanly across the whole breadth set:
    - `Legacy4`: switcher **3/4**, **14/15**, so it trails the cleaner `Turtle+Regime+MACD` / majority rows (**4/4**, **15/15**)
    - `Legacy5BNB`: switcher prints the highest raw return (**+1961.0%**) but only **3/4**, **14/15**, so the chronology-first winner remains the plain majority ensemble (**4/4**, **15/15**)
    - `OldGuardNoBNB`: still favors plain `MACD+Regime` (**+1189.2%**, **4/4**, **15/15**) over the macro switcher (**+1178.3%**, **4/4**, **15/15**)
  - `Switch(MacroRiskOn->TurtleCombo, else MACD)` is respectable but weaker overall (**0/6** wins).
  - `Switch(MacroStress->MACD, else CSM)` also adds little and looks like a secondary / likely-dead branch (**0/6** wins).
- Honest read:
  - This is a useful **partial-positive / mostly-negative breadth result**.
  - Macro-state switching is **more promising than blunt macro long-gates**, but it still does **not** beat the existing majority ensemble / `MACD+Regime` pair cleanly enough to deserve another local optimization loop.
- Overfitting / trust risk:
  - Richer context can still just become a prettier wrapper around the same daily family mix if I start state-tuning.
  - The right lesson is that exogenous context may be more useful for **allocation / attribution** than for direct signal gating — but this is still not a new leader.
- What’s next:
  - If exogenous work continues, the best next version is **macro-conditioned attribution / sizing / capped-book portfolio switching**, not more nearby switch-rule tweaks.
  - More importantly: keep breadth broad. The new concentration risk is not just MACD — it is spending too many consecutive cycles on **daily family-combination ideas**.

## 17:xx UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track C breadth**, but a lot of that breadth had clustered around **daily family-combination ideas** (cross-impact follow-ups, switchers, macro-state allocation).
  - That was healthier than the earlier MACD-overfocus, but it was starting to become its own local basin.
  - I deliberately pivoted to a more **structural** breadth bucket instead of doing another daily family remix.
- Chose **Track C — Broaden edge discovery** via a richer **basis/carry-style** follow-up.
- Built successfully with `cargo build`.
- Added `krypto/examples/funding_beta_neutral_carry.rs`.
  - Purpose: test whether the weak funding-dispersion spread was partly just residual market beta.
  - Same structural setup as the earlier funding spread work:
    - `BTCUSDT`, `ETHUSDT`, `SOLUSDT` on **4h** bars
    - signal at close
    - next-open entry
    - fixed **1 / 3 / 6 bar** hold
    - **0.1%** taker each side
    - realized funding cashflows included during the hold
  - New comparison inside the same harness:
    - `EqualWeight` = prior **50/50 gross** long-low-funding / short-high-funding pair
    - `BetaNeutral` = rolling **BTC-beta-neutral** long/short sizing using a **120-bar** beta window on prior open-open returns
- Ran `cargo run --example funding_beta_neutral_carry --profile sweep 2>&1`.
- Key findings:
  - Best **BetaNeutral** config was **entry_z 1.0 / spread_z 2.5 / hold 6** with:
    - **+3.8%** combined return
    - **+3.4%** price component
    - **+0.4%** funding component
    - **58 trades**
    - **50.0%** win rate
    - **2/4** positive quarter windows
    - **8/15** positive resamples
  - Best **EqualWeight** version of the same idea was slightly better in full-sample level but weaker chronologically:
    - **+5.5%** combined
    - **+5.0%** price
    - **+0.4%** funding
    - **58 trades**
    - **6/15** positive resamples
  - Most shorter / tighter variants remained clearly negative and often **0/15** on resamples.
- Honest read:
  - This is a useful **negative structural result**.
  - Beta-neutral sizing helps chronology a bit, which suggests residual market beta was part of the problem.
  - But it does **not** rescue the family; the signal remains far too weak to treat as a real edge.
- Overfitting / trust risk:
  - This is exactly the kind of seductive structural story that can keep wasting cycles if it keeps almost-improving.
  - The right lesson is that a more sophisticated hedge ratio did **not** convert this into a trustworthy carry family.
- What’s next:
  - If funding / basis work returns, move to a **genuinely different construction** (perp-vs-spot basis, event-conditioned carry harvesting, or cleaner neutrality), not another local rescue attempt on this exact rule.
  - Keep breadth broad; today’s best contribution was killing a plausible rescue path rather than polishing it.

## 20:18 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track C breadth**, but too much of that breadth had clustered around **daily family-combination ideas** (cross-impact variants, switchers, macro-state switching, carry rescue attempts).
  - That was healthier than earlier MACD-overfocus, but it was no longer the highest-value local use of time.
  - The best under-served question inside the exogenous bucket was no longer “which switch rule wins?” but **“where do the current families actually make money by macro state?”**
- Chose **Track C — Broaden edge discovery**, but as **state attribution** rather than another switch-rule tweak.
- Built successfully with `cargo build`.
- Added `krypto/examples/exogenous_state_attribution.rs`.
  - Purpose: attribute the current daily family set by **lagged macro state** under the same fair daily harness:
    - signal at close
    - next-open entry
    - fixed **21-bar** hold
    - **0.1%** taker each side
  - Macro buckets from lagged FRED context:
    - **MacroRiskOn** = `SP500 > SMA50` and `VIX < SMA20` and `DXY < SMA50`
    - **MacroStress** = `VIX > SMA20` or `DXY > SMA50`
    - **MacroNeutral** = neither
  - Families attributed across `Base5`, `NoDOGE`, `Legacy4`, `Legacy5BNB`, `OldGuardNoBNB`, `LargeCaps5`:
    - `CrossSectionalMomentum`
    - `MACD+Regime`
    - `Turtle+Regime+MACD`
    - `Ensemble(Majority 2/3)`
    - `Switch(MacroRiskOn->Majority, else MACD)`
- Ran `cargo run --example exogenous_state_attribution --profile sweep 2>&1`.
- Key findings:
  - The recent daily family winners earn **most of their return in `MacroStress` windows, not `MacroRiskOn` windows**.
  - Examples:
    - `Base5`:
      - `MACD+Regime`: **+2252.1%** stress vs **+1386.9%** risk-on
      - majority ensemble: **+2631.3%** stress vs **+1156.7%** risk-on
    - `LargeCaps5`:
      - majority ensemble: **+2556.3%** stress vs **+509.0%** risk-on
    - `Legacy4`:
      - `Turtle+Regime+MACD`: **+916.7%** stress vs **+95.7%** risk-on
  - `MacroNeutral` contributes very little almost everywhere because it contains very few trades.
  - The notable exception is `OldGuardNoBNB`, where `CrossSectionalMomentum` tops the table overall at **+1268.3%** — but even there it is still mostly a **stress-state** result (**+981.3%** stress vs **+363.5%** risk-on).
- Honest read:
  - This is a useful breadth result because it changes the exogenous interpretation.
  - The current daily families are **not** best described as simple “macro risk-on trend” expressions.
  - They look more like **disorder / stress-harvesting** families, which means exogenous context is probably more useful for **sizing / risk control / drawdown framing** than for another binary family switch.
- Overfitting / trust risk:
  - A switcher that wins a few breadth rows can still be the wrong abstraction if it misunderstands where the returns actually come from.
  - Today’s result says the project should stop treating macro context as a search for one more winner row and use it more as a **state-attribution lens**.
- What’s next:
  - If exogenous work continues, move to **macro-conditioned sizing / capped-book allocation / drawdown control**, not more nearby switch-rule tweaks.
  - More generally: keep breadth broad and avoid turning the exogenous bucket into another local optimization basin.
