# 2026-03-29

## 02:16 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution-trust cleanup**, then **cross-impact / carry / exogenous breadth follow-ups**
- that was much healthier than the earlier MACD-family tunnel vision, but it was still clustering around a few adjacent local basins
- the most under-served useful question was a **Track A × Track C bridge**: if the lagged macro-stress attribution is real, is there a **gentle** capped-book exposure setting that improves risk shape without the huge return destruction of the blunt `StressOnly` / `StressTilt` overlays?
- so I stayed away from another family leaderboard and instead calibrated macro overlays as portfolio-risk budget candidates

Work completed:
- added `examples/exogenous_overlay_calibration.rs`
- reused the same fair capped-book portfolio lens as the earlier overlay audit:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **top-3 strength-capped** book
- tested five lagged macro-state exposure overlays:
  - `Baseline(1.0 all states)`
  - `StressLean75(1.0 stress / 0.75 else)`
  - `StressTilt50(1.0 stress / 0.50 else)`
  - `StressLean25(1.0 stress / 0.25 else)`
  - `StressOnly(1.0 stress / 0.0 else)`
- built and ran with:
  - `cargo build --example exogenous_overlay_calibration`
  - `cargo run --example exogenous_overlay_calibration --profile sweep 2>&1`
- snapshot artifacts written:
  - `snapshots/exogenous_overlay_calibration_latest.md`
  - `snapshots/exogenous_overlay_calibration_latest.csv`
  - `snapshots/exogenous_portfolio_overlay_20260329T021545Z.md`
  - `snapshots/exogenous_portfolio_overlay_20260329T021545Z.csv`

Key findings:
- the useful answer is **gentle tilt, not hard gating**
- on the strongest modern baskets, a mild stress tilt often improves Sharpe / DD while preserving far more of the baseline than the harsher overlays:
  - `Base5` `Turtle+Regime+MACD`:
    - baseline **+1,631,496.3%**, **Sharpe 8.07**, **MaxDD 34.4%**
    - `StressLean75` **+728,576.0%**, **Sharpe 8.25**, **MaxDD 32.4%**
    - `StressTilt50` **+322,725.6%**, **Sharpe 8.22**, **MaxDD 30.8%**
  - `LargeCaps5` `Turtle+Regime+MACD`:
    - baseline **+596,989.9%**, **Sharpe 8.74**, **MaxDD 27.8%**
    - `StressLean75` **+336,658.5%**, **Sharpe 8.73**, **MaxDD 25.7%**
    - `StressTilt50` **+189,105.7%**, **Sharpe 8.53**, **MaxDD 23.6%**
- on harsher legacy baskets, the same pattern is weaker and usually does **not** beat baseline risk-adjusted leadership:
  - `OldGuardNoBNB` `MACD+Regime` stays best at baseline **+1919.9%**, **Sharpe 4.21**, **MaxDD 39.1%**
  - `StressLean75` softens to **+1359.6%**, **Sharpe 4.05**, **MaxDD 36.6%**
  - `StressTilt50` softens further to **+952.9%**, **Sharpe 3.78**, **MaxDD 34.6%**
- `StressOnly` remains too blunt almost everywhere: drawdowns improve, but returns compress far too hard to treat it as the practical default

Honest read:
- this is a useful **risk-budget calibration** result, not a new strategy result
- the macro-stress story survives best as a **gentle exposure trim outside stress**, especially on the broader modern baskets where `Turtle+Regime+MACD` already leads under the capped-book lens
- it does **not** justify a universal macro overlay rule, and it does **not** rescue the exogenous bucket into a new alpha family

Overfitting / trust risk:
- this can still become a local exogenous over-interpretation trap if I keep nudging exposure fractions
- the honest lesson is narrower: lagged macro state seems more useful for **portfolio shaping** than for **signal creation**, and only in a **soft** form

What’s next:
- do **not** turn this into a dense exposure-grid optimization loop
- better follow-ups are either:
  - a small **state-conditioned drawdown attribution** / exposure-budget summary that uses just the clearly separated overlay points, or
  - a deliberate pivot back to another orthogonal **Track C** bucket so exogenous work does not become the next local basin

## 02:32 UTC — Durable memory flush

- Noah asked for human-readable visuals showing actual strategy performance paths, not meta-charts about research time allocation.
- Initial response used summary/meta charts (`progress_timeline`, `progress_leaderboard_lenses`, `progress_breadth_scan`) and Noah explicitly said they were not useful for his goal.
- Corrected course by surfacing actual performance visuals from the research outputs:
  - `charts/head_to_head_top2_equity.png`
  - `charts/head_to_head_top2_drawdown.png`
  - `charts/rigorous_validation_equity.png`
  - `charts/macd_failure_pair_symbol_returns.png`
- Durable lesson: when Noah asks for charts/progress, default to **actual equity curves, drawdowns, symbol attribution, and performance-path visuals** — not charts about workflow, time allocation, or project-management structure.
- If new explanatory charts are needed, prefer readable performance tables/curves tied directly to strategy returns, drawdowns, universes, and symbol contributions.

## 04:53 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution-trust cleanup**, then **exogenous overlay interpretation** and **ticker-interaction / carry breadth follow-ups**
- that was healthier than the older MACD-family tunnel vision, but it was still at risk of clustering around exogenous-local and carry-rescue loops
- the higher-value use of this cycle was an under-served **Track C structural bucket**: test a genuinely different **perp-vs-spot basis** construction instead of another cross-perp funding tweak or another macro overlay fraction

Work completed:
- added `examples/perp_spot_basis_carry.rs`
- construction tested:
  - Binance **spot OHLCV** for execution path (`BTCFDUSD`, `ETHFDUSD`, `SOLFDUSD`)
  - Binance futures funding history **mark prices** as the perp proxy (`BTCUSDT`, `ETHUSDT`, `SOLUSDT`)
  - rolling z-score of direct **basis = mark / spot - 1**
  - when basis is rich: **short perp / long spot**
  - when basis is cheap: **long perp / short spot**
  - enter at next open, hold **1 / 3 / 6** bars on **4h** data
  - include realized funding cashflows on the perp leg and charge **0.1%** taker each side
- built and ran with:
  - `cargo build --example perp_spot_basis_carry`
  - `cargo run --example perp_spot_basis_carry --profile sweep 2>&1`

Key findings:
- the direct basis construction is **not** a hidden structural winner; every tested row stayed negative and failed chronology
- best row:
  - **basis window 90 / entry z 2.0 / hold 6**
  - **-7.5% total** = **-10.0% price + +2.8% funding**
  - **84 trades**
  - **1/4** positive quarter windows
  - **0/15** positive resamples
- the rest of the grid was much worse, with many rows in the **-30% to -70%** range

Honest read:
- this is a good **breadth falsification** result, not a new edge
- the funding component is economically real, but it is still too small to overcome weak basis-convergence timing and trading costs under this simple construction
- importantly, this kills another plausible carry rescue story instead of letting the project keep circling the same weak structural family under a new wrapper

Overfitting / trust risk:
- it would be easy to overreact to the `+2.8% funding` component and say “the carry is there, just tune the trigger”
- the honest conclusion is narrower: the carry exists, but **this implementation still loses** and does not deserve another local z-score/hold optimization loop

What’s next:
- if basis/carry returns, it should be through a more realistic **spot-perp structural** design (borrow/inventory-aware, capacity-aware, or event-specific), not another nearby parameter sweep
- otherwise the best next move is either another under-served orthogonal **Track C** bucket or a compact visual/risk-summary task Noah explicitly asked for

## 06:45 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution-trust cleanup**, then **exogenous / carry / ticker-interaction breadth**
- that was healthier than the earlier MACD-family tunnel vision, but it was starting to over-cluster around a few nearby structural ideas
- after Noah’s recent Discord questions, the highest-value under-served item from `PLAN.md` was clearly **Visual analysis** under a realistic lens, not another local signal tweak
- so this cycle deliberately rebalanced toward **Track B × Trust communication**: show actual benchmark-leader path behavior under the fair **top-3 capped-book** lens on one modern basket and one harsher old-guard basket

Work completed:
- added `examples/capped_book_visual_audit.rs`
- scope kept tight and decision-relevant:
  - universes: `Base6` and `OldGuardNoBNB`
  - strategies: `MACD+Regime` and `Turtle+Regime+MACD`
  - same fair daily harness: signal at close, next-open entry, fixed **21-bar** hold, **0.1%** taker each side, **top-3 strength-capped** book
- built and ran with:
  - `cargo build --example capped_book_visual_audit`
  - `cargo run --example capped_book_visual_audit --profile sweep 2>&1`
- new outputs written:
  - `charts/capped_book_equity_audit.png`
  - `charts/capped_book_drawdown_audit.png`

Key findings:
- the charts make the universe split visually obvious instead of leaving it as a table-only narrative
- `Base6` still favors the Turtle-family combo on both endpoint and path risk:
  - `Turtle+Regime+MACD`: **+681,746.3%**, **32.9% MaxDD**
  - `MACD+Regime`: **+283,616.8%**, **41.8% MaxDD**
- `OldGuardNoBNB` flips that story and exposes the harsher path-risk penalty on the Turtle-family combo:
  - `MACD+Regime`: **+3,324.7%**, **40.9% MaxDD**
  - `Turtle+Regime+MACD`: **+3,130.2%**, **54.0% MaxDD**

Honest read:
- this is not a new strategy result; it is a **benchmark-interpretation / trust** result
- the point is that the current leaders are still **yardsticks**, not conclusions: modern baskets make Turtle+Regime+MACD look stronger and cleaner, while harsher old-guard baskets make filtered MACD look sturdier and reveal much uglier Turtle-family drawdowns

Overfitting / trust risk:
- a single “best return” table can still flatter whichever family happens to fit one universe best
- these visuals help, but they are still only two baskets and one capped-book construction; they improve interpretability, not finality

What’s next:
- send the new charts + concise explanation to Noah in `#krypto`
- avoid slipping back into another exogenous / carry rescue loop next cycle
- best next research move is either another genuinely orthogonal **Track C** bucket or a compact drawdown/attribution extension around the benchmark leaders if Noah keeps pulling on the visual-comparison thread

## 08:17 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution-trust cleanup**, then **exogenous / carry / ticker-interaction breadth**, with the most recent cycle on **visual benchmark communication**
- that was much healthier than the earlier MACD tunnel vision, but the project still needed one more **Track B trust** step before pivoting away: not just *how* the capped-book leaders look, but **which symbols are actually causing the worst drawdowns**
- so I stayed narrow and decision-relevant: extend the new visual audit into a **worst drawdown attribution** pass instead of opening another nearby breadth basin

Work completed:
- added `examples/capped_book_drawdown_attribution.rs`
- same fair daily capped-book lens as the recent visual audit:
  - universes: `Base6`, `OldGuardNoBNB`
  - leaders: `MACD+Regime`, `Turtle+Regime+MACD`
  - signal at close, next-open entry, fixed **21-bar** hold, **0.1%** taker each side, **top-3 strength-capped** book
- built and ran with:
  - `cargo run --example capped_book_drawdown_attribution --profile sweep 2>&1`
- new outputs written:
  - `charts/capped_book_drawdown_attribution.png`
  - `snapshots/capped_book_drawdown_attribution_latest.md`
  - `snapshots/capped_book_drawdown_attribution_latest.csv`

Key findings:
- `Base6`:
  - `MACD+Regime` worst drawdown is **-41.8%**, driven mostly by **ETH (-30.9%)** then **BTC (-15.4%)**
  - `Turtle+Regime+MACD` worst drawdown is milder at **-32.9%**, but is mostly an **ADA / DOGE** hit (**-18.2%**, **-17.5%**) rather than a BTC/ETH failure
- `OldGuardNoBNB`:
  - `MACD+Regime` worst drawdown is **-40.9%**, concentrated in **BCH (-27.8%)**, **LTC (-12.4%)**, and **ETH (-12.3%)**
  - `Turtle+Regime+MACD` is the nastiest path at **-54.0%**, with damage spread more broadly across **LTC (-27.5%)**, **BCH (-20.5%)**, **EOS (-18.7%)**, and **XRP (-15.2%)**

Honest read:
- this is still not a new strategy result; it is a **benchmark-risk decomposition** result
- the useful update is that the old-guard Turtle-combo weakness is not just one ugly symbol — its worst drawdown is **broader across the basket**, which makes the harsher old-guard penalty more believable than a single-name fluke
- filtered MACD still has painful drawdowns too, but they look more **concentrated in a few big legs**

Overfitting / trust risk:
- this attribution is still conditioned on one declared capped-book construction and one worst peak-to-trough window per case
- so the point is better interpretation, not a claim that we have fully solved benchmark trust

What’s next:
- send Noah the new drawdown-attribution chart with a concise explanation
- after this, the right move is probably to pivot back toward another under-served orthogonal **Track C** bucket instead of endlessly interrogating the same benchmark pair

## 10:16 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution-trust cleanup**, then **visual benchmark trust work**, with breadth spread across **carry / exogenous / ticker-interaction** follow-ups
- that was much healthier than the older MACD-family tunnel vision, but the recent exogenous work had mostly been **filters, switchers, attribution, and risk overlays** rather than a direct standalone exogenous signal family
- the highest-value under-served Track C question was therefore: do **lagged macro shocks themselves** have standalone directional value for crypto under the same fair daily harness?
- so I deliberately avoided another benchmark-leader interrogation and built a new **direct exogenous-impulse breadth** scan

Work completed:
- added `examples/exogenous_impulse_benchmark.rs`
- conservative alignment rule unchanged: each crypto bar only sees the latest macro close **strictly before** that date
- macro impulse construction:
  - **5-day macro returns**
  - **63-day rolling z-scores**
  - threshold **|z| >= 1.0**
- variants tested under the same fair daily harness (**signal at close, next-open entry, fixed 21-bar hold, 0.1% taker each side**):
  - `SPX impulse follow`
  - `VIX impulse inverse`
  - `DXY impulse inverse`
  - `Macro impulse combo` (majority vote)
- stress universes tested:
  - `Base5`
  - `NoDOGE`
  - `Legacy4`
  - `Legacy5BNB`
  - `OldGuardNoBNB`
  - `LargeCaps5`
- built and ran with:
  - `cargo build --example exogenous_impulse_benchmark`
  - `cargo run --example exogenous_impulse_benchmark --profile sweep 2>&1`

Key findings:
- this family was **not dead on arrival**; the stronger results came from the **single-factor** impulse legs, not the majority combo
- `VIX impulse inverse` won **3/6** universes and was strongest on broader baskets:
  - `Base5`: **+17,306.6%**, **4/4** quarter windows, **15/15** resamples
  - `NoDOGE`: **+11,141.7%**, **3/4**, **15/15**
  - `LargeCaps5`: **+14,834.0%**, **4/4**, **15/15**
- `SPX impulse follow` also won **3/6** universes and looked cleaner on harsher legacy baskets:
  - `Legacy4`: **+8,347.4%**, **3/4**, **15/15**
  - `Legacy5BNB`: **+11,195.1%**, **3/4**, **15/15**
  - `OldGuardNoBNB`: **+11,509.0%**, **4/4**, **15/15**
- `DXY impulse inverse` stayed secondary (**0/6** universe wins), and the `Macro impulse combo` mostly diluted the stronger standalone legs

Honest read:
- this is the first exogenous result that looks like a **real standalone breadth candidate** instead of just a macro filter / switch / overlay story
- but it is still an early fixed-hold daily screen, not a promotion result, and it could become the next overfitting basin very easily if I jump straight into z-threshold tuning

Overfitting / trust risk:
- the raw returns are large, but they are still aggregate summed-trade results under the same simple daily harness used for breadth scanning
- the honest next question is **not** “optimize this harder”; it is whether this family survives **apples-to-apples head-to-head** versus the current yardsticks and whether one or two symbols / states are carrying the whole effect

What’s next:
- compare the useful exogenous legs directly against `MACD+Regime`, `Turtle+Regime+MACD`, and the majority ensemble under the same universes / chronology-first ranking
- add symbol / macro-state attribution if the head-to-head still looks real
- do **not** let this become “the strategy” just because it is the first exogenous family to print strong chronology tables

## 12:10 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution / visual trust**, then **carry / cross-impact / exogenous breadth**
- that was healthier than the old MACD-family tunnel vision, but after the strong first macro-impulse screen the highest-value next step was clearly **comparison**, not another new family and not exogenous-local tuning
- so this cycle stayed in **Track C**, but did the honest thing: put the new exogenous impulse rows directly into the same chronology-first yardstick table as the current daily leaders

Work completed:
- added `examples/exogenous_impulse_head_to_head.rs`
- compared five rows under the same fair daily harness:
  - `SPX impulse follow`
  - `VIX impulse inverse`
  - `MACD+Regime`
  - `Turtle+Regime+MACD`
  - `Ensemble(Majority 2/3)`
- same assumptions preserved everywhere:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
  - lagged macro close only (`strictly before` each crypto bar date)
- new snapshot artifacts written:
  - `snapshots/exogenous_impulse_head_to_head_latest.md`
  - `snapshots/exogenous_impulse_head_to_head_latest.csv`
  - `snapshots/exogenous_impulse_head_to_head_20260329T121057Z.md`
  - `snapshots/exogenous_impulse_head_to_head_20260329T121057Z.csv`
- built and ran with:
  - `cargo run --example exogenous_impulse_head_to_head --profile sweep 2>&1`

Key findings:
- the first isolated macro-impulse result was **overstated** once forced into apples-to-apples comparison against the yardsticks
- universe wins under chronology-first ranking:
  - `MACD+Regime`: **2/6**
  - `Ensemble(Majority 2/3)`: **2/6**
  - `Turtle+Regime+MACD`: **1/6**
  - `SPX impulse follow`: **1/6**
  - `VIX impulse inverse`: **0/6**
- important nuance: the exogenous legs were **not dead**, just less dominant than the isolated scan implied
  - `OldGuardNoBNB`: `SPX impulse follow` is the chronology-first winner at **+1484.7%**, **4/4** quarter windows, **15/15** resamples, ahead of `MACD+Regime` (**+1189.2%**, **4/4**, **15/15**)
  - `Base5`: `VIX impulse inverse` stays respectable at **+1993.1%**, **4/4**, **15/15**, but still trails `MACD+Regime` (**+3781.1%**) and the majority ensemble (**+3490.3%**)
  - `Legacy4`: `VIX impulse inverse` has the highest raw return (**+1625.6%**) but loses chronology-first ranking to the majority ensemble because it slips to **3/4** quarter windows vs **4/4**
- broader read:
  - the direct exogenous family survives best as a **secondary breadth candidate on specific harsher baskets**, not as a clean new all-around leader
  - the earlier exogenous breadth table looked much stronger partly because it compared macro legs only against each other, not against the incumbent yardsticks

Honest read:
- this was a good **anti-worship** session
- it did not kill the new exogenous family, but it stopped it from quietly becoming “the strategy” after one exciting isolated screen
- the more honest framing now is:
  - `SPX impulse follow` deserves follow-up mainly on **old-guard / harsher** baskets
  - `VIX impulse inverse` is still interesting on **broader modern** baskets
  - but neither displaces the current yardsticks overall once compared directly under the same ranking rules

Overfitting / trust risk:
- the original exogenous result was very easy to overread because the raw returns were large
- this head-to-head shows why isolated breadth wins are not enough; a family can look impressive in its own table and still fail to take the belt from the current yardsticks
- next work should focus on **attribution / structure**, not threshold tuning

What’s next:
- best follow-up is **symbol and macro-state attribution** for `SPX impulse follow` and `VIX impulse inverse`, especially to see whether the old-guard SPX result is broad or carried by a few names
- do **not** start a local z-threshold / lookback optimization loop
- project focus remains healthier if the exogenous family is treated as a **candidate breadth bucket**, not a new privileged default

## 13:40 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution / visual trust**, then **carry / cross-impact / exogenous breadth**
- that was much healthier than the old MACD-family tunnel vision, but the newest risk was obvious: the fresh exogenous impulse family could easily become the next over-read local basin
- after the apples-to-apples head-to-head, the highest-value next step was therefore **structure attribution**, not another new family and definitely not exogenous-local tuning
- so this cycle stayed in **Track C**, but in the most anti-overfitting way available: ask which symbols and which lagged macro states are actually carrying the surviving `SPX impulse follow` and `VIX impulse inverse` niches

Work completed:
- added `examples/exogenous_impulse_attribution.rs`
- compared only the two surviving direct exogenous legs:
  - `SPX impulse follow`
  - `VIX impulse inverse`
- same fair daily harness preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - lagged macro close only (`strictly before` each crypto bar date)
- new snapshot artifacts written:
  - `snapshots/exogenous_impulse_attribution_latest.md`
  - `snapshots/exogenous_impulse_attribution_latest.csv`
  - `snapshots/exogenous_impulse_attribution_20260329T133958Z.md`
  - `snapshots/exogenous_impulse_attribution_20260329T133958Z.csv`
- built and ran with:
  - `cargo build`
  - `cargo run --example exogenous_impulse_attribution --profile sweep 2>&1`

Key findings:
- both surviving exogenous legs are **mostly `MacroStress` stories**, not balanced all-state edges
- `SPX impulse follow` on the key old-guard basket (`OldGuardNoBNB`) is broad enough to keep alive, but it is not uniformly distributed:
  - strongest contributors: **BCH (+452.2%)**, **EOS (+445.0%)**, **XRP (+348.3%)**
  - weaker legs: **ETH (+121.8%)**, **LTC (+117.4%)**
  - state split: **+1262.5%** in `MacroStress` vs **+130.0%** in `MacroRiskOn`
- `VIX impulse inverse` is even more clearly a **stress-harvesting** family:
  - `Base5`: **+2011.2%** in `MacroStress` vs **+116.7%** in `MacroRiskOn`
  - `OldGuardNoBNB`: **+2052.8%** in `MacroStress` vs **+70.6%** in `MacroRiskOn`
  - `MacroNeutral` is mildly to clearly negative on most baskets
  - symbol support is broad enough to matter, but still basket-dependent:
    - `Base5`: **DOGE (+624.1%)**, **XRP (+438.6%)**, **SOL (+426.4%)**
    - `OldGuardNoBNB`: **EOS (+591.7%)**, **XRP (+438.6%)**, **LTC (+391.8%)**

Honest read:
- this did **not** turn the exogenous family into a new benchmark leader
- but it did improve the interpretation materially: the surviving impulse edges are **not one-symbol flukes**, yet they are also **not broad regime-agnostic signals**
- the exogenous bucket now looks strongest as a **secondary macro-stress breadth family** rather than a general replacement for the current yardsticks

Overfitting / trust risk:
- the obvious trap from here would be local z-threshold / lookback tuning around a family that now “has a story”
- the honest lesson is narrower: the family survives as a **structural stress-state candidate**, not as a promoted strategy and not as a license for more nearby knob-turning

What’s next:
- if exogenous impulse work continues, do a **stress-state portfolio / capped-book comparison** against the current yardsticks, or a cleaner symbol-cluster attribution on the old-guard SPX niche
- do **not** start a local exogenous tuning loop from here

## 15:31 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution / visual trust**, then **carry / cross-impact / exogenous breadth**, with the freshest local cluster around the new **exogenous impulse** bucket
- that was still much healthier than the older MACD-family tunnel vision, but it was now at risk of becoming a new local basin: the exogenous family had survived its own scan, then head-to-head, then attribution
- the highest-value next step from `PLAN.md` was therefore **Track A × Track C trust**, not another exogenous tweak: force the surviving SPX/VIX impulse rows through the same **top-3 capped-book portfolio** lens Noah actually cares about and see whether they still matter once capital concentration is realistic

What I built:
- added `examples/exogenous_impulse_portfolio_audit.rs`
- direct comparison set under the same fair daily harness:
  - `SPX impulse follow`
  - `VIX impulse inverse`
  - `MACD+Regime`
  - `Turtle+Regime+MACD`
  - `Ensemble(Majority 2/3)`
- assumptions preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **top-3 strength-capped** book
  - lagged macro close only (`strictly before` each crypto bar date)
- built and ran with:
  - `cargo build --example exogenous_impulse_portfolio_audit`
  - `cargo run --example exogenous_impulse_portfolio_audit --profile sweep 2>&1`
- snapshot artifacts written:
  - `snapshots/exogenous_impulse_portfolio_latest.md`
  - `snapshots/exogenous_impulse_portfolio_latest.csv`
  - `snapshots/exogenous_impulse_portfolio_20260329T153044Z.md`
  - `snapshots/exogenous_impulse_portfolio_20260329T153044Z.csv`

Key findings:
- this was a useful **trust compression** result: the exogenous impulse family fades materially under realistic portfolio construction
- `VIX impulse inverse` is the cleaner exogenous row, but it is still well behind the incumbent yardsticks on the broader baskets:
  - `Base5`:
    - `Turtle+Regime+MACD` **+1,758,370.7%**, **Sharpe 6.70**, **34.3% MaxDD**
    - `Ensemble(Majority 2/3)` **+1,473,655.8%**, **Sharpe 6.19**, **44.1% MaxDD**
    - `VIX impulse inverse` only **+6,913.7%**, **Sharpe 4.74**, **39.8% MaxDD**
  - `LargeCaps5`:
    - `Turtle+Regime+MACD` **+664,871.1%**, **Sharpe 7.28**
    - `Ensemble(Majority 2/3)` **+216,544.9%**, **Sharpe 6.14**
    - `MACD+Regime` **+159,715.4%**, **Sharpe 5.92**
    - `VIX impulse inverse` only **+2,035.6%**, **Sharpe 3.21**
- `SPX impulse follow` also loses the earlier old-guard niche once capital concentration is realistic:
  - `OldGuardNoBNB`:
    - `Ensemble(Majority 2/3)` **+14,286.5%**, **Sharpe 3.67**, **56.5% MaxDD**
    - `MACD+Regime` **+1,919.9%**, **Sharpe 3.50**, **39.1% MaxDD**
    - `SPX impulse follow` **+1,684.4%**, **Sharpe 2.73**, **74.2% MaxDD**
    - `VIX impulse inverse` **+1,639.7%**, **Sharpe 2.57**, **66.1% MaxDD**
- universe-level read:
  - the capped-book winners are still the incumbent yardsticks, mostly **Turtle+Regime+MACD** / **majority ensemble** on modern baskets and **majority ensemble / MACD+Regime** on the harsher old-guard baskets
  - the exogenous bucket now looks like a **secondary macro-stress niche**, not a new benchmark family

Honest read:
- this was exactly the anti-overfitting step the project needed
- the earlier exogenous breadth result was real enough to survive direct comparison and attribution, but not real enough to survive **portfolio construction realism** as a new leader story
- good result because it prevents another exciting standalone family from quietly becoming “the strategy” off summed-trade tables

Overfitting / trust risk:
- the obvious trap from here would be opening another local exogenous loop (“maybe a different impulse strength ranking or portfolio cap rescues it”)
- the honest lesson is narrower: direct macro impulse can stay alive as a **secondary stress-state breadth family**, but it is not the main portfolio candidate under current realism

What’s next:
- do **not** keep drilling local exogenous variants next cycle
- better next moves are either:
  - another under-served orthogonal **Track C** family, or
  - a more integrated cross-family **portfolio-trust summary** that puts trend, ensemble, cross-impact, and exogenous candidates under one realistic allocation lens

## 16:58 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution / visual trust**, then **carry / cross-impact / exogenous breadth**, with the freshest local cluster around the new **exogenous impulse** bucket
- that was still much healthier than the older MACD-family tunnel vision, but it was now at risk of becoming another fragmented comparison problem: each family had its own artifact, and the project could still over-remember whichever table was most flattering
- the highest-value under-served item from `PLAN.md` was therefore a **Track A × Track C integration step**: force the representative family leaders through one shared realistic portfolio lens instead of comparing separate files mentally

Work completed:
- added `examples/cross_family_portfolio_summary.rs`
- compared under the same fair daily capped-book harness:
  - `MACD+Regime`
  - `Turtle+Regime+MACD`
  - `Ensemble(Majority 2/3)`
  - `CrossImpact residual BTC`
  - `SPX impulse follow`
  - `VIX impulse inverse`
- assumptions preserved everywhere:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **top-3 strength-capped** book
  - lagged macro close only for exogenous rows
- built and ran with:
  - `cargo build --example cross_family_portfolio_summary`
  - `cargo run --example cross_family_portfolio_summary --profile sweep 2>&1`
- new outputs written:
  - `snapshots/cross_family_portfolio_summary_latest.md`
  - `snapshots/cross_family_portfolio_summary_latest.csv`
  - `snapshots/cross_family_portfolio_summary_20260329T165814Z.md`
  - `snapshots/cross_family_portfolio_summary_20260329T165814Z.csv`

Key findings:
- the incumbent yardsticks still dominate the realistic portfolio lens on modern baskets:
  - `Base5`: `Turtle+Regime+MACD` **+1,758,370.7%**, **Sharpe 6.70**, **34.3% MaxDD**; majority ensemble **+1,473,655.8%**, **Sharpe 6.19**
  - `LargeCaps5`: `Turtle+Regime+MACD` **+664,871.1%**, **Sharpe 7.28**; majority ensemble **+216,544.9%**, **Sharpe 6.14**; `MACD+Regime` **+159,715.4%**, **Sharpe 5.92**
- the exogenous rows remain clearly secondary once put into the same capped-book frame:
  - `Base5` `VIX impulse inverse`: only **+6,913.7%**, **Sharpe 4.74**
  - `OldGuardNoBNB` `SPX impulse follow`: **+1,684.4%**, **Sharpe 2.73**, **74.2% MaxDD**
- the one meaningful non-incumbent exception is **`CrossImpact residual BTC` on `OldGuardNoBNB`**:
  - **+3,342.3%**, **Sharpe 3.71**, **41.5% MaxDD**
  - that edges majority ensemble on Sharpe (**3.67**) and `MACD+Regime` (**3.50**) on the harsher old-guard basket

Honest read:
- this is a useful **project-control** result, not a new leader claim
- it reinforces that exogenous impulse should stay in the **secondary stress-state niche** bucket
- it also slightly upgrades the old-guard residual cross-impact result: still not a universal winner, but the one non-incumbent row that can actually top a realistic portfolio table on the harsher basket

Overfitting / trust risk:
- the obvious trap from here would be to treat the old-guard cross-impact win as a reason to reopen another nearby threshold-tuning loop
- the honest lesson is narrower: if interaction work returns, it should come back through a **more structural graph / cluster / residual** construction, not another local variant of the same blunt expression

What’s next:
- do **not** continue exogenous-local work by default
- best next move is likely either:
  - a more structural follow-up on the surviving **old-guard interaction** niche, or
  - another under-served orthogonal **Track C** family entirely

## 17:02 UTC — Infra note

Arc clarified that the **GitHub token is expired host-wide**.
- This is already escalated to Noah.
- Local commits are safe and should push later once auth is restored.
- Operational rule until then: **skip the push step** and do **not** keep re-sending the same blocker every cycle.
- Only surface it again if auth is restored or the failure mode materially changes.

## 18:40 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **execution / portfolio trust** plus **exogenous breadth**, with the most useful surviving non-incumbent niche now being the **old-guard residual cross-impact** row from the integrated capped-book summary
- that was healthier than the old MACD tunnel vision, but the next honest question was clearly structural, not another exogenous loop and not another local cross-impact tweak
- so I used this cycle on an under-served **Track C structural falsification**: require the residual interaction links themselves to be **stable across two calibration halves** and see if the old-guard niche survives

Work completed:
- added `examples/cross_impact_residual_stable_benchmark.rs`
- same fair daily harness as the other breadth scans:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- construction details:
  - residualized lagged **3-bar** peer returns using rolling **252-bar BTC beta**
  - early fixed calibration window split into **two halves**
  - each target only keeps peers that score positively in **both** halves
  - compared:
    - `CrossImpact residual all`
    - `Residual stable top2`
    - `Residual stable consensus`
    - `CrossSectionalMomentum`
    - `MACD+Regime`
- built and ran with:
  - `cargo build`
  - `cargo run --example cross_impact_residual_stable_benchmark --profile sweep 2>&1`

Key findings:
- this was a strong **narrowing / anti-overfitting** result: the old-guard residual interaction positive does **not** survive a peer-link stability requirement
- the broad residualized row still stays alive where expected:
  - `OldGuardNoBNB`: `CrossImpact residual all` **+1157.4%**, **4/4** quarter windows, **15/15** resamples
  - `Legacy4`: **+525.9%**, **4/4**, **14/15**
  - `NoDOGE`: **+1059.1%**, **3/4**, **14/15**
- but the stable-link variants mostly collapsed because the target-level peer intersections became too sparse:
  - `OldGuardNoBNB`: `Residual stable top2` **0 trades**, **0/4**, **0/15**
  - `Base5`, `NoDOGE`, `LargeCaps5`: stable variants also printed **0 trades**
  - only `Legacy4` / `Legacy5BNB` produced any meaningful stable-link rows, and even there they were weak: **+231.6%**, **0/4**, **0/15**
- printed stable peer sets make the reason visible: many targets retain only **one or zero** repeated peers across halves, which kills the supposedly structural local graph story

Honest read:
- this is useful precisely because it is negative
- the surviving residual cross-impact niche still looks more like **broad basket confirmation / diffuse propagation** than a clean, stable target-local interaction graph
- that keeps the old-guard result alive only in the narrower sense already suggested by the recent portfolio summary, not as proof of a robust relational edge family

Overfitting / trust risk:
- it would be easy to rescue this by relaxing the stability requirement or nudging the calibration split / peer count
- that would be exactly the wrong lesson; the honest answer is that the structural graph story got materially weaker once link stability was demanded

What’s next:
- do **not** open another local peer-selection / stable-link optimization loop
- if ticker-interaction work returns, it should come back through a genuinely different structure (sector residuals, latent cluster components, or richer relational models)
- otherwise the next cycle should pivot again toward another under-served orthogonal **Track C** bucket rather than staying in interaction-local follow-ups

## 20:17 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **exogenous breadth**, then **portfolio / execution trust**, then a short **interaction structural falsification** pass
- that mix was much healthier than the old MACD-family tunnel vision, but it still risked another subtle failure mode: treating the current benchmark narrative as stable just because the same top row kept getting repeated
- the highest-value under-served item this cycle was therefore a clean **Track A lab-control** task: quantify **snapshot drift** directly so the project stops confusing “same nominal leader” with “same trust story”

Work completed:
- added `examples/benchmark_drift_audit.rs`
- small dependency added: `csv = "1.3"` in `Cargo.toml`
- audit inputs:
  - archived `snapshots/head_to_head_*.csv`
  - latest `snapshots/head_to_head_latest.csv`
  - latest `snapshots/cross_family_portfolio_summary_latest.csv`
- built and ran with:
  - `cargo build --example benchmark_drift_audit`
  - `cargo run --example benchmark_drift_audit --profile sweep 2>&1`
- new outputs written:
  - `snapshots/benchmark_drift_audit_latest.md`
  - `snapshots/benchmark_drift_audit_latest.csv`
  - `snapshots/benchmark_drift_audit_20260329T201736Z.md`
  - `snapshots/benchmark_drift_audit_20260329T201736Z.csv`

Key findings:
- the archived chronology-first benchmark leader stayed **nominally stable**:
  - `MACD+Regime` remained **#1** across all archived `head_to_head` snapshots (`20260324T053442Z` through latest)
- but the **internal trust story moved materially anyway**:
  - plain `MACD` improved from **13/15 -> 15/15** resamples
  - plain `MACD` score improved by **+0.060**
  - `Turtle+Regime+MACD` and `Turtle+MACD` swapped relative positions despite both staying **15/15** on resamples
- the latest realistic capped-book Sharpe-first winners remain **split across families / universes**, not concentrated in the benchmark-table leader:
  - `Turtle+Regime+MACD`: wins **Base5** and **LargeCaps5**
  - `Ensemble(Majority 2/3)`: wins **NoDOGE**, **Legacy4**, **Legacy5BNB**
  - `CrossImpact residual BTC`: wins **OldGuardNoBNB**

Honest read:
- this is not a new strategy result; it is a **research-control / trust** result
- useful because it makes one hidden risk explicit: a benchmark can look stable at the headline level while the **interpretation underneath it** is drifting as cache or methodology changes
- the fair `head_to_head` benchmark still matters, but it should be treated as a **yardstick lens**, not as a single global answer to “what is the best strategy?”

Overfitting / trust risk:
- the trap here would be to overread “`MACD+Regime` stayed #1” as proof the project can stop worrying about benchmark drift
- the honest lesson is the opposite: **top-line stability can mask meaningful internal movement**, so archived snapshots need to be part of the evidence, not just bookkeeping

What’s next:
- do **not** spend another cycle turning this into a meta-reporting loop
- the value of this audit is that it clears the way for the next session to pivot back toward an under-served **Track C** family with better lab hygiene
- keep using the benchmark as a yardstick, but avoid collapsing the project back into one default “best strategy” story

## 21:44 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **exogenous breadth**, then **portfolio / execution trust**, then a short **interaction structural falsification** pass and finally **benchmark-drift control**
- that mix was much healthier than the old MACD-family tunnel vision, but it was still clustering around interpretation / niche-family cleanup rather than a genuinely different model class
- the highest-value under-served item from `PLAN.md` was therefore a real **Track C richer-model probe**: test whether a simple rolling feature model can beat the current yardsticks under the same fair daily harness, instead of doing another nearby trend / exogenous / interaction tweak

Work completed:
- added `examples/cross_sectional_linear_model_benchmark.rs`
- construction:
  - rolling **pooled ridge-linear panel model** trained on the prior **252 daily bars** across the current universe
  - features available at the close only:
    - `cs_momentum_rank`
    - `cs_trend_score`
    - `macd_gap`
    - `sma200_gap`
    - `21d reversal`
    - `21d realized vol`
  - target = next-open to **21-bar-later** open return
  - each day: rank symbols by predicted return, go long the top **2** positive scores and short the bottom **2** negative scores
- same fair daily harness preserved:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- comparison set:
  - `LinearAlpha(panel)`
  - `CrossSectionalMomentum`
  - `MACD+Regime`
  - `Ensemble(Majority 2/3)`
- built and ran with:
  - `cargo build --example cross_sectional_linear_model_benchmark`
  - `cargo run --example cross_sectional_linear_model_benchmark --profile sweep 2>&1`

Key findings:
- the first pass was **much stronger than expected**: `LinearAlpha(panel)` won **6/6** universes and stayed **4/4** quarter windows, **15/15** resamples everywhere
- examples:
  - `Base5`: **+3346.3%**, **341 trades**, **52.8%** win rate vs `MACD+Regime` **+3148.8%**, **4/4**, **15/15**
  - `NoDOGE`: **+2834.8%**, **356 trades**, **55.3%** vs `MACD+Regime` **+1908.1%**
  - `Legacy4`: **+3387.2%**, **391 trades**, **59.6%** vs majority ensemble **+790.8%**
  - `Legacy5BNB`: **+2986.4%**, **465 trades**, **53.8%** vs majority ensemble **+1511.1%**
  - `OldGuardNoBNB`: **+2241.3%**, **421 trades**, **52.3%** vs `CrossSectionalMomentum` **+1362.5%** and `MACD+Regime` **+1024.5%**
  - `LargeCaps5`: **+4348.7%**, **518 trades**, **58.3%** vs `MACD+Regime` **+1985.5%**

Honest read:
- this is the strongest fresh **Track C** breadth result in a while precisely because it is **not** another nearby family tweak
- but it is also exactly the kind of more flexible model that can hide leakage / overfitting if handled casually
- so the right framing is **candidate discovered, trust work next** — not benchmark replacement, not Hall of Fame talk, and definitely not local parameter worship

Overfitting / trust risk:
- the result is strong enough that the main risk now is **believing it too quickly**
- even though the features are lagged and the harness stayed fair, a rolling panel model has more ways to accidentally overfit than the recent hand-declared families
- the next honest checks are coefficient / feature attribution, harsher portfolio-realism comparison, and leak-sanity pressure tests — not more local feature or threshold tuning

What’s next:
- keep this richer-model row alive as a **research candidate**, not a conclusion
- next follow-up should be **trust-first**:
  - coefficient / feature attribution
  - capped-book portfolio realism vs the incumbent yardsticks
  - leak-sanity / chronology sanity checks around the rolling training setup
- do **not** let “first richer model to win 6/6” silently become “the strategy”

## 23:14 UTC — Cron summary

Oriented first: reread `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context.

Top-down call before execution:
- the last 3–5 sessions spent most of their time on **exogenous breadth**, then **portfolio / execution trust**, then a short **interaction structural falsification**, **benchmark drift control**, and finally the new **richer-model breadth probe**
- that mix was much healthier than the old MACD-family tunnel vision, but the newest risk was obvious: the rolling panel model could easily become the next over-read shiny object
- the highest-value next step from `PLAN.md` was therefore clearly **Track A trust**, not more features and not another breadth family: hit the new linear model with a **null-control sanity check** before it gets any special status

Work completed:
- added `examples/cross_sectional_linear_model_sanity.rs`
- compared under the same fair daily harness:
  - `LinearAlpha(panel)`
  - `LinearAlpha(shuffled_y)` — same rolling ridge model, but training labels are deterministically rotated inside each rolling window to break feature -> future-return alignment
  - `MACD+Regime`
- preserved assumptions:
  - signal at close
  - next-open entry
  - fixed **21-bar** hold
  - **0.1%** taker each side
  - **4** quarter windows + **15** CPCV-style resamples
- added snapshot artifacts:
  - `snapshots/cross_sectional_linear_model_sanity_latest.md`
  - `snapshots/cross_sectional_linear_model_sanity_latest.csv`
  - `snapshots/cross_sectional_linear_model_sanity_20260329T231402Z.md`
  - `snapshots/cross_sectional_linear_model_sanity_20260329T231402Z.csv`
- built and ran with:
  - `cargo build --example cross_sectional_linear_model_sanity`
  - `cargo run --example cross_sectional_linear_model_sanity --profile sweep 2>&1`

Key findings:
- the real panel row still wins every tested basket and keeps the original clean chronology profile:
  - `Base5`: **+3346.3%**, **4/4** quarter windows, **15/15** resamples
  - `NoDOGE`: **+2834.8%**, **4/4**, **15/15**
  - `Legacy4`: **+3387.2%**, **4/4**, **15/15**
  - `Legacy5BNB`: **+2986.4%**, **4/4**, **15/15**
  - `OldGuardNoBNB`: **+2241.3%**, **4/4**, **15/15**
  - `LargeCaps5`: **+4348.7%**, **4/4**, **15/15**
- but the shuffled-label control is **far too strong** to treat the family as cleanly trusted yet:
  - `Legacy5BNB`: **+2175.6%**, **4/4**, **15/15**
  - `LargeCaps5`: **+1432.3%**, **4/4**, **15/15**
  - `Base5`: **+1732.0%**, **4/4**, **13/15**
  - even the weakest shuffled basket (`NoDOGE`) still posts **+767.6%**, **3/4**, **10/15**
- coefficient stability is at least interpretable rather than absurd:
  - persistent positive weight on **`cs_trend_score`**, **`macd_gap`**, and **`reversal21`**
  - persistent negative weight on **`sma200_gap`** and **`vol21`**

Honest read:
- this was exactly the anti-worship result the project needed
- the richer-model candidate survives the first sanity check, but the control staying this strong means there is still a **ranking-structure / hidden-common-signal** red flag
- so the right framing is **candidate still alive, but not trust-clean**

Overfitting / trust risk:
- if I had stopped at the 6/6 win table, it would have been very easy to quietly promote this into the new default family
- this control says that would be premature: some of the apparent edge may be coming from broad ranking structure or other shared regularities that survive even after the supervised label relationship is broken

What’s next:
- keep this family alive, but stay in **Track A** around it next rather than opening a feature-selection loop
- best next checks are:
  - capped-book portfolio realism vs the incumbent yardsticks
  - a stronger null-control / leak diagnostic
  - possibly a simpler rank-only baseline to separate supervised alpha from generic cross-sectional ordering structure
- do **not** start a local ridge-lambda / feature-include tuning basin from here
