# 2026-03-26

## 01:xx UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, yesterday’s memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track A trust audits** plus **Track C orthogonal breadth probes**.
  - That was still a reasonable use of time; we were **not** over-refining one benchmark leader this cycle.
  - The highest-value under-served item from `PLAN.md` was the richer follow-up to the funding/carry work: test whether the weak funding spread result was mostly an accounting issue.
- Chose **Track C — Broaden edge discovery**, but in a stricter way: improve the economic realism of the carry test rather than tuning the same naive spread thresholds again.
- Built successfully with `cargo build`.
- Added a new structural breadth harness: `krypto/examples/funding_realized_carry.rs`.
  - Purpose: rerun the **long-most-negative-funded / short-most-positive-funded** cross-asset spread on `BTCUSDT / ETHUSDT / SOLUSDT` **4h**, but this time include **realized funding cashflows actually earned/paid during the hold**.
  - Same basic execution assumptions as the prior spread test: signal at close, next-open entry, fixed **1 / 3 / 6 bar** hold, **0.1% taker each side**.
  - New decomposition: total PnL is split into **price convergence** and **funding carry**.
- Ran `cargo run --example funding_realized_carry --profile sweep 2>&1`.
- Key findings:
  - Best chronology-first configuration stayed in the same neighborhood as the naive test: **entry_z 1.0 / spread_z 2.5 / hold 6**.
  - Result with explicit carry accounting:
    - **combined return: -0.5%**
    - **price component: -0.9%**
    - **funding component: +0.4%**
    - **58 trades**
    - **50.0%** win rate
    - **2/4** positive quarter windows
    - **5/15** positive resamples
  - So the carry is **real but too small** to rescue the strategy; the price path remains the bigger problem in this simple construction.
  - Most shorter / tighter variants stayed worse and often outright negative.
- Honest read: this was a valuable breadth cycle because it turned a vague “maybe the carry test was too naive” objection into a measured answer. The answer is **not** “no carry exists”; it is **“there is some carry, but not enough to overcome weak spread timing in this simple rule.”**
- Overfitting / trust risk: a structurally sensible family can still look better in story-space than in market-space. Improving the accounting made the diagnosis sharper, but it did not create an edge.
- What’s next:
  - If funding / basis work continues, it should now move to a **meaningfully richer construction** — e.g. **perp-vs-spot basis**, **realized carry harvesting**, **residualized neutrality**, or **event-conditioned carry**.
  - Otherwise breadth should continue toward other genuinely orthogonal structural families rather than drift back into another nearby daily trend tweak.

## 03:59 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and the recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track C breadth probes** (cross-sectional stress, funding recheck, leader-laggard, funding-spread, realized carry) with some earlier **Track A** trust work.
  - That made **Track A — Trust the lab** the under-served track this cycle.
  - The highest-value gap was not another nearby strategy variant; it was **snapshot discipline** for the newer portfolio-realism / capped-book stress audit, which still had no archived artifacts like the head-to-head benchmark does.
- Chose **Track A — Trust the lab**.
- Built successfully with `cargo build`.
- Extended `krypto/examples/trend_portfolio_stress_audit.rs` so it now writes both stable latest files and timestamped archives on each rerun:
  - `snapshots/trend_portfolio_stress_latest.md`
  - `snapshots/trend_portfolio_stress_latest.csv`
  - `snapshots/trend_portfolio_stress_<UTC timestamp>.md`
  - `snapshots/trend_portfolio_stress_<UTC timestamp>.csv`
- Ran `cargo run --example trend_portfolio_stress_audit --profile sweep 2>&1`.
- First archived snapshot from the new flow:
  - `snapshots/trend_portfolio_stress_20260326T035911Z.{md,csv}`
- Key rerun findings under the capped-book lens (top-3 active trades by strength):
  - **Modern / broader baskets** still mostly favor `Turtle+Regime+MACD`:
    - `Base6`: **+681,746.3%**, **Sharpe 8.08**, **MaxDD 32.9%**
    - `LegacyCore4`: **+13,592.8%**, **Sharpe 4.01**, **MaxDD 66.0%**
    - `LargeCaps6`: **+27,415.4%**, **Sharpe 4.53**, **MaxDD 52.4%**
  - **Harsher survivorship / legacy baskets** still favor `MACD+Regime`:
    - `NoDOGE`: **+40,271.4%**, **Sharpe 7.26**, **MaxDD 39.9%**
    - `Legacy5`: **+10,483.4%**, **Sharpe 3.63**, **MaxDD 58.4%**
    - `OldGuard6`: **+6,824.7%**, **Sharpe 4.15**, **MaxDD 49.2%**
    - `OldGuardNoBNB`: **+3,324.7%**, **Sharpe 4.59**, **MaxDD 40.9%**
- Honest read: this session did **not** discover a new edge or reshuffle the ranking. Its value was making a critical trust audit **auditable** instead of chat-only.
- Overfitting / trust risk: without archived portfolio-stress snapshots, the project can over-remember whichever stress result was last discussed. This fix reduces that risk materially, but it does not lower the bar for promotion.
- What’s next:
  - Use the new portfolio-stress snapshots as a stable baseline for one sharper Track A extension — most likely a **latency-aware / entry-timing-aware capped-book audit**.
  - Keep avoiding the trap where “most recently benchmarked leader” quietly becomes “the strategy.”

## 05:25 UTC — Direction nudge from Noah: broaden roadmap, keep harness work alive

- Noah asked to explicitly add several longer-term directions into planning / memory:
  - ticker-to-ticker impact from first principles using ML models
  - exogenous / non-crypto context data (VIX, FX, stocks, etc.)
  - ensemble models
  - more interesting / more complex models generally
  - passive / maker-style execution should return to scope for credible candidates
- Agreed and clarified the passive-execution point:
  - passive execution was not rejected as an idea
  - it fell out of focus because it had mainly been helping the old mean-reversion family that later failed realistic execution testing
  - it should now come back as part of realistic execution evaluation for credible current candidates, not as a proxy for strategy validity
- Updated `PLAN.md` to add a broader longer-term roadmap covering:
  - cross-asset / ticker-interaction models
  - exogenous context data
  - ensemble methods
  - richer ML / relational models
  - passive execution reintroduction for current credible families
- Updated `MEMORY.md` with the new meta-lesson: keep investing in the harness, but do not let harness work crowd out breadth indefinitely.
- Current stance remains: continue harness / trust work, but with an explicit nudge to keep broader discovery active and visible.

## 07:00 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and the recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track C breadth** and then **Track A snapshot discipline**.
  - That meant the most under-served high-value item was still **Track A**, specifically the next unfinished trust task already called out in `PLAN.md`: a **latency-aware / entry-timing-aware variant of the capped-book portfolio audit**.
  - We were at risk of over-remembering the plain portfolio-stress snapshot as if it were timing-robust, especially for `Turtle+Regime+MACD` on modern baskets.
- Chose **Track A — Trust the lab**.
- Built successfully with `cargo build`.
- Added a new audit harness: `krypto/examples/trend_portfolio_latency_audit.rs`.
  - Purpose: keep the same daily trend leaders, stress universes, and capped-book lens as `trend_portfolio_stress_audit.rs`, but compare:
    - **Baseline** = next-open entry / next-open fixed-hold exit
    - **Delay1** = one-extra-bar delay on both entry and exit
  - New snapshot outputs:
    - `snapshots/trend_portfolio_latency_latest.md`
    - `snapshots/trend_portfolio_latency_latest.csv`
    - `snapshots/trend_portfolio_latency_<UTC timestamp>.md`
    - `snapshots/trend_portfolio_latency_<UTC timestamp>.csv`
- Ran `cargo run --example trend_portfolio_latency_audit --profile sweep 2>&1`.
- First archived snapshot from the new flow:
  - `snapshots/trend_portfolio_latency_20260326T070000Z.{md,csv}`
- Key capped-book findings from the archived snapshot:
  - **Modern / broader baskets lose much of the earlier Turtle-combo edge under delay**:
    - `Base6`: baseline capped-Sharpe leader is `Turtle+Regime+MACD` (**8.56**), but delayed capped-Sharpe leader flips to `MACD+Regime` (**7.09**)
    - `NoDOGE`: baseline `Turtle+Regime+MACD` (**8.93**) also flips to delayed `MACD+Regime` (**8.58**)
    - `LargeCaps6`: `Turtle+Regime+MACD` still leads, but capped return falls from **+28,176.2%** to **+12,463.3%** and Sharpe from **4.96** to **4.35**
  - **Legacy / survivorship-harsher baskets mostly strengthen the filtered-MACD case under delay**:
    - `OldGuard6`: `MACD+Regime` improves from **+6,824.7%, Sharpe 4.15** to **+9,415.3%, Sharpe 4.96**
    - `OldGuardNoBNB`: `MACD+Regime` improves from **+3,324.7%, Sharpe 4.59** to **+4,354.8%, Sharpe 5.03**
    - `LegacyCore4`: delayed capped-Sharpe leader becomes `MACD+Regime` (**3.79**)
  - **Plain `MACD`** improves on some delayed legacy baskets (`Legacy5`, `LargeCaps6`, `OldGuard6`), but still does not earn a trustworthy all-around lead once universe and timing stress are considered jointly.
- Honest read: this materially changes the trust story. The earlier capped-book snapshot made `Turtle+Regime+MACD` look like the clean modern-basket portfolio leader, but that edge is much more **timing-sensitive** than the static portfolio-stress view implied. `MACD+Regime` now looks like the cleaner delayed-execution portfolio candidate overall.
- Overfitting / trust risk: this result is a reminder that even a stronger portfolio-construction lens can still flatter strategies if timing assumptions are left too optimistic. The project still must not let one benchmark leader silently become “the strategy.”
- What’s next:
  - Fold the latency-aware portfolio read back into the broader benchmark story rather than keep isolated audit files drifting apart.
  - Best next Track A step is likely either:
    - chronology / resample slices inside the new latency audit, or
    - a compact head-to-head summary that combines **timing realism + capped-book portfolio realism + universe stress** in one place.

## 10:14 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, the recent #krypto Discord context, and the top-down three-track question before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track A trust audits** plus a recent block of **Track C breadth probes**.
  - That was still directionally right, but the latest gap was specific: the new latency-aware portfolio audit still lived in a separate file with no chronology lens, which risked replacing one flattering leaderboard story with another.
  - Highest-value next item from `PLAN.md`: add **chronology / resample slices** to the latency-aware capped-book audit so timing fragility can be compared across both **time** and **portfolio construction**.
- Chose **Track A — Trust the lab**.
- Built successfully with `cargo build`.
- Extended `krypto/examples/trend_portfolio_latency_audit.rs` so it now reports, for each universe / strategy / execution scenario:
  - capped-book **quarter-window passes** (**4** windows, positive return + at least **30** trades)
  - capped-book **CPCV-style chronological resample passes** (**15** test combinations)
  - average capped-book return across those window/resample slices
- Kept the important trust property from the main fair harness: use the **full-history signals / plans**, then restrict **trade counting and portfolio days** to the evaluation windows, so slice context does not get reset artificially.
- Ran `cargo run --example trend_portfolio_latency_audit --profile sweep 2>&1`.
- Fresh archived snapshot:
  - `snapshots/trend_portfolio_latency_20260326T101347Z.{md,csv}`
- Key findings:
  - The earlier full-sample read was only half the story. `MACD+Regime` still often tops the **delayed capped-book Sharpe** table:
    - `Base6`: **+191,673.0%**, **Sharpe 7.09**, **1/4** quarter windows, **5/15** resamples
    - `NoDOGE`: **+54,029.5%**, **Sharpe 8.58**, but only **1/15** resamples
    - `OldGuard6`: **+9,415.3%**, **Sharpe 4.96**, **0/4** quarter windows, **4/15** resamples
    - `OldGuardNoBNB`: **+4,354.8%**, **Sharpe 5.03**, **0/4** quarter windows, **1/15** resamples
  - So delayed filtered-MACD is **not** the simple trust winner once chronology is added.
  - `Turtle+Regime+MACD` remains timing-fragile in level terms on some modern baskets, but often keeps **better delayed chronology counts**:
    - `NoDOGE`: **5/15** delayed resamples
    - `LargeCaps6`: **5/15** delayed resamples
    - `OldGuardNoBNB`: **5/15** delayed resamples
  - Other notable delayed reads:
    - `Legacy5`: delayed **plain MACD** tops Sharpe at **4.26** with **1/4** quarter windows and **5/15** resamples
    - `LargeCaps6`: delayed `Turtle+Regime+MACD` still leads with **+12,463.3%**, **Sharpe 4.35**, **1/4** quarter windows, **5/15** resamples
- Honest read: the new chronology layer makes the result more useful and less flattering. The project should stop treating “best delayed Sharpe” as equivalent to “most trustworthy delayed candidate.”
- Overfitting / trust risk: isolated audit files can each crown a different leader depending on lens. That is exactly why the next trust step should be an integrated summary that combines **timing realism + capped-book realism + chronology stability + universe stress** in one ranking view.
- What’s next:
  - fold the latency-aware chronology read into one integrated benchmark / ranking summary for the trend candidates
  - keep breadth visible after that; the project has spent enough consecutive cycles on trust work that Track C should return soon unless the integrated summary reveals a new audit hole

## 13:30 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track A trust audits** after a shorter block of **Track C breadth** work.
  - That was starting to get concentrated, but the highest-value remaining loose end was still the one called out in `PLAN.md`: stop leaving the latency / portfolio / chronology conclusions split across separate files.
  - I chose to close that trust gap first, then explicitly rebalance toward breadth next cycle.
- Chose **Track A — Trust the lab**.
- Built successfully with `cargo build`.
- Extended `krypto/examples/trend_portfolio_latency_audit.rs` to add an explicit **integrated ranking summary** on top of the existing capped-book latency audit.
  - The new integrated view scores each trend candidate across:
    - baseline capped-book universe wins
    - delayed capped-book universe wins
    - baseline + delayed quarter-window passes
    - baseline + delayed CPCV-style resample passes
    - average baseline / delayed capped-book Sharpe
    - average delay-vs-baseline degradation / improvement
- Ran `cargo run --example trend_portfolio_latency_audit --profile sweep 2>&1`.
- Fresh archived snapshot:
  - `snapshots/trend_portfolio_latency_20260326T133028Z.{md,csv}`
- Key integrated findings:
  - `Turtle+Regime+MACD` ranks first on the new integrated score (**0.363**) because it still dominates the **baseline capped-book universe** view (**5/7** wins), but it remains very timing-sensitive in level terms:
    - avg delayed return delta **-96,808.4%**
    - avg delayed Sharpe delta **-0.58**
    - delayed resamples only **30/105** positive overall
  - `MACD+Regime` ranks second (**0.289**) because it dominates the **delayed full-sample universe** view (**5/7** wins) and improves average delayed Sharpe (**+0.78**), but its delayed chronology remains weak overall:
    - delayed resamples only **15/105** positive
  - plain `MACD` ranks third (**0.221**) with only **1/7** delayed universe wins and **0/7** baseline wins, but it quietly has the **best aggregate chronology counts**:
    - **31/105** positive baseline resamples
    - **31/105** positive delayed resamples
- Honest read: this is exactly why the project should not promote a trend leader yet. Different candidates still win depending on whether the lens is **baseline capped-book portfolio**, **delayed full-sample timing realism**, or **aggregate chronology stability**.
- Overfitting / trust risk: the integrated summary reduces the risk of cherry-picking whichever audit table flatters the current favorite, but it also makes the main trust problem clearer — there is still **no single trust-clean trend winner**.
- What’s next:
  - Rebalance toward **Track C breadth** next cycle unless a new audit hole appears.
  - Best next breadth targets are genuinely orthogonal families: **ticker-interaction / cross-impact**, **exogenous context features**, **ensemble methods**, or a richer **basis / carry** construction rather than another nearby trend refinement.

## 16:27 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track A trust audits** (portfolio realism, latency realism, integrated ranking) with a shorter earlier block of **Track C breadth** work.
  - That was no longer the highest-value use of time for this cycle.
  - The project was at risk of over-refining the current trend yardsticks instead of testing whether a broader combination idea could survive the same fair harness.
- Chose **Track C — Broaden edge discovery** via the newly requested roadmap bucket: **ensemble methods**.
- Built successfully with `cargo build`.
- Added a new breadth harness: `krypto/examples/ensemble_family_benchmark.rs`.
  - Purpose: test whether simple, pre-declared ensembles improve robustness under the same fair assumptions rather than just produce a prettier full-sample return.
  - Same assumptions as the other daily breadth scans:
    - signal at close
    - next-open entry
    - fixed **21-bar** hold
    - **0.1%** taker each side
  - Stress universes: `Base5`, `NoDOGE`, `Legacy4`, `Legacy5BNB`, `OldGuardNoBNB`, `LargeCaps5`
  - Compared:
    - `MACD+Regime`
    - `Turtle+Regime+MACD`
    - `CrossSectionalMomentum`
    - `Ensemble(Trend consensus 2/2)`
    - `Ensemble(MACD+Regime OR CSM)`
    - `Ensemble(Majority 2/3)` = vote across `MACD+Regime`, `Turtle+Regime+MACD`, and `CrossSectionalMomentum`
- Ran `cargo run --example ensemble_family_benchmark --profile sweep 2>&1`.
- Key findings:
  - The strongest ensemble was the **2-of-3 majority vote**, not the stricter trend-consensus rule.
  - Universe wins:
    - `Ensemble(Majority 2/3)`: **3/6**
    - `MACD+Regime`: **2/6**
    - `Turtle+Regime+MACD`: **1/6**
    - all others: **0/6**
  - Specific reads:
    - `Base5`: `Ensemble(Majority 2/3)` **+3784.8%**, **316 trades**, **4/4** quarter windows, **15/15** resamples — slightly above `MACD+Regime` (**+3781.1%**)
    - `Legacy5BNB`: `Ensemble(Majority 2/3)` **+1775.4%**, **4/4**, **15/15** vs `MACD+Regime` **+1902.1%**, **4/4**, **14/15**
    - `LargeCaps5`: `Ensemble(Majority 2/3)` **+3060.6%**, **4/4**, **15/15** vs `MACD+Regime` **+2982.4%**, **4/4**, **15/15**
    - `OldGuardNoBNB`: `MACD+Regime` still leads at **+1189.2%**, **4/4**, **15/15**; the majority ensemble softens to **+928.1%**, **3/4**, **14/15**
  - Important negative result:
    - `Ensemble(Trend consensus 2/2)` added **no value** — it was effectively identical to `Turtle+Regime+MACD` in this harness.
    - `Ensemble(MACD+Regime OR CSM)` often improved raw return but usually weakened pass counts, so looser signal union is not an automatic robustness win.
- Honest read:
  - This is the first ensemble result that looks genuinely worth keeping alive.
  - But it is **not** a trust-clean promotion candidate: the majority vote helps on some baskets, yet it still loses the harsher old-guard basket and does not solve the broader execution / latency trust questions already documented elsewhere.
- Overfitting / trust risk:
  - Ensemble methods can become another way to hide dependence on the same trend exposure.
  - The good sign here is that the majority vote improved **breadth** more than it improved just one cherry-picked headline, but it still needs attribution and economic realism stress.
- What’s next:
  - Best next breadth step is either:
    - decompose whether the majority ensemble is actually adding orthogonal information vs just smoothing trend exposure, or
    - move to another roadmap bucket such as **exogenous context data** / **ticker-interaction ML**.
  - Do **not** let this immediately become another “optimize the new leaderboard leader” loop.

## 19:33 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and the recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track A trust audits** followed by one **Track C breadth** cycle on ensembles.
  - That made the highest-value next step **not** another trend audit and **not** another benchmark polish pass.
  - The right move was to finish the explicit next item in `PLAN.md`: test whether the new majority-vote ensemble is actually adding orthogonal information or just smoothing the same trend exposure.
- Chose **Track C — Broaden edge discovery**, but as an attribution / trust pass on the new ensemble bucket rather than a fresh family yet.
- Built successfully with `cargo build`.
- Added `krypto/examples/ensemble_family_attribution.rs`.
  - Purpose: decompose `Ensemble(Majority 2/3)` by **symbol**, **stress universe**, and **coalition source**.
  - Same fair daily assumptions as the earlier breadth benchmark: signal at close, next-open entry, fixed **21-bar** hold, **0.1%** taker each side.
  - Attribution buckets:
    - `MACD+Regime` + `Turtle+Regime+MACD`
    - `MACD+Regime` + `CrossSectionalMomentum`
    - `Turtle+Regime+MACD` + `CrossSectionalMomentum`
    - all three agree
- Ran `cargo run --example ensemble_family_attribution --profile sweep 2>&1`.
- Key findings across the six breadth stress universes:
  - Majority-vote coalition split:
    - **`MACD+Regime` + `CrossSectionalMomentum`**: **744 trades**, **+3893.7%** aggregate return
    - **all three agree**: **114 trades**, **+1626.5%**, **71.9%** win rate
    - **pure trend-pair consensus** (`MACD+Regime` + `Turtle+Regime+MACD`): only **85 trades**, **-284.9%** aggregate return
    - **`Turtle+Regime+MACD` + `CrossSectionalMomentum` without `MACD+Regime`**: effectively **0 trades**
  - Orthogonality read:
    - `CrossSectionalMomentum` changed the majority decision on **744 trades** for **+3893.7%** aggregate return
    - the pure trend-consensus slice was actually **net negative**, so the ensemble is **not** mainly “trend consensus made safer”
  - Important caveat:
    - this does **not** make the majority vote the new standalone leader; in this attribution harness, standalone `MACD+Regime` and standalone `CrossSectionalMomentum` still post higher aggregate returns because the vote is more selective
- Honest read:
  - This is still a useful breadth result because it answered the real question.
  - The ensemble’s useful contribution appears to be **CSM confirming / vetoing filtered trend exposure**, which is a more genuinely orthogonal story than I expected.
  - But that also means the ensemble bucket has answered its immediate question; it should **not** become the next local optimization rabbit hole.
- Overfitting / trust risk:
  - Ensembles can still hide dependence on the same underlying family. This pass reduced that risk materially by showing where the vote’s returns actually come from.
  - But selective attribution is still not deployment evidence.
- What’s next:
  - Move breadth to another roadmap bucket next cycle — best candidates are **exogenous context data** or **ticker-interaction / cross-impact** rather than more ensemble-local tuning.

## 22:33 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and recent #krypto Discord context before choosing work.
- Explicit top-down check:
  - The last 3-5 sessions spent most of their time on **Track A trust audits** plus one **Track C ensemble** cycle.
  - That was no longer the highest-value use of time.
  - The right rebalance was another **Track C breadth** session, but in a new roadmap bucket rather than more ensemble-local or trend-local tweaking.
- Chose **Track C — Broaden edge discovery** via the newly-added **exogenous context** bucket.
- Built successfully with `cargo build`.
- Added `krypto/examples/exogenous_context_benchmark.rs`.
  - Purpose: test whether simple lagged macro / cross-market context improves the current filtered-trend yardstick under the same fair daily assumptions.
  - Data source: local FRED CSV caches for **SP500**, **VIX**, and broad **DXY** (`DTWEXBGS`) stored under `krypto/data/exogenous/`.
  - Conservative alignment rule: each crypto bar only sees the latest macro close **strictly before** that crypto date, to avoid same-day close leakage.
  - Benchmarked five variants across `Base5`, `NoDOGE`, `Legacy4`, `Legacy5BNB`, `OldGuardNoBNB`, `LargeCaps5`:
    - `MACD+Regime`
    - `MACD+Regime + SPX risk-on`
    - `MACD+Regime + VIX guard`
    - `MACD+Regime + DXY weak`
    - `MACD+Regime + macro combo`
- Ran `cargo run --example exogenous_context_benchmark --profile sweep 2>&1`.
- Key findings:
  - Overall universe wins:
    - `MACD+Regime`: **4/6**
    - `SPX risk-on`: **1/6**
    - `DXY weak`: **1/6**
    - `VIX guard`: **0/6**
    - `macro combo`: **0/6**
  - On the modern / broader baskets, the macro gates mostly **reduced return and trade count without improving chronology**:
    - `Base5`: baseline **+54,983.0%**, **15/15** resamples vs `SPX risk-on` **+49,254.8%**, **15/15**
    - `NoDOGE`: baseline **+37,207.0%**, **15/15** vs `SPX risk-on` **+32,406.1%**, **15/15**
    - `LargeCaps5`: baseline **+46,268.9%**, **15/15** vs `SPX risk-on` **+40,400.2%**, **15/15**
  - Two harsher-basket clues were still worth noting:
    - `Legacy4`: `SPX risk-on` slightly improves chronology (**12/15 vs 11/15**) but still cuts full return (**+12,311.5% vs +12,786.6%**)
    - `OldGuardNoBNB`: `DXY weak` improves chronology (**14/15 vs 13/15**) but sharply cuts return (**+8,540.7% vs +16,361.3%**)
  - The full `macro combo` was generally too restrictive and did not emerge as a robustness winner.
- Honest read:
  - This was still a useful breadth cycle because it opened a genuinely new roadmap bucket with a conservative alignment rule.
  - But the result is mostly negative: **simple binary macro long-gates behave more like blunt trade suppressors than trust-clean alpha adders**.
- Overfitting / trust risk:
  - Exogenous data can be seductive because it sounds structurally richer than pure price signals.
  - This run is a good reminder that simply pasting macro state onto crypto trend signals is not enough; leakage discipline and richer feature design matter.
- What’s next:
  - If exogenous context stays in scope, move to **symbol/regime attribution** or **macro-conditioned sizing/ranking**, not more local tuning of these same one-bit gates.
  - Otherwise the next breadth bucket should be **ticker-interaction / cross-impact**.
- Posted the concise result summary to #krypto Discord and updated `PLAN.md` / `MEMORY.md` accordingly.
