# T85 Hyperopt Report — HEDGE_ATR_PERIOD (Current Production Config)

**Date:** 2026-05-07
**Agent:** Kira
**Session:** cron 9de6700f — hyperparameter optimization (3h)

---

## Audit: Hardcoded Parameter Found

**Parameter:** `HEDGE_ATR_PERIOD` in `src/live/bot.rs`

**What it does:** BTC ATR lookback period used in the USDT hedge overlay. The live bot sizes positions at `HEDGE_SIZE_MULT` (0.25) when BTC ATR(HEDGE_ATR_PERIOD) > 45th percentile of HEDGE_LOOKBACK (252) daily true ranges. This is a risk-reduction overlay, not an entry signal.

**Old hardcoded value:** 21 (no documented justification)
**New validated default:** 38 (T75 winner on HOLD_MAX=12; T85 winner on HOLD_MAX=15/HSM=0.25)

---

## T85 Sweep Design

- **Param range:** HEDGE_ATR_PERIOD ∈ [5..=100] step 1 (96 integer values)
- **Validation:** 9 universes × 6 walk-forward windows (252d train / 252d test)
- **Config:** HOLD_MAX=15, HSM=0.25, ATR_ENTRY_MULT=0.00, ATR_RANK(AP=17,LB=41,T=5.0)
- **Selection rule:** pass_rate desc → avg_sharpe desc → avg_max_dd asc

---

## T85 Results

### Winner: P=38

| Metric | P=38 (winner) | P=21 (baseline) | Delta |
|--------|-------------|-----------------|-------|
| Pass rate | 52/60 (86.7%) | 50/60 (83.3%) | **+2 windows** |
| Avg Sharpe | 1.481 | 1.309 | **+13.2%** |
| Avg return | +19.1% | +17.1% | +1.9pp |
| Avg MaxDD | 9.8% | 10.1% | **-0.3pp** |
| Win rate | 48.6% | 48.6% | flat |

### Full Plateau (pass_rate ≥ 86%)

| P | Pass | Sharpe | Return% | MaxDD% |
|---|------|--------|---------|--------|
| **38** | **52/60** | **1.481** | **19.1** | **9.8** |
| 51 | 52/60 | 1.445 | 19.6 | 10.1 |
| 49 | 52/60 | 1.443 | 19.5 | 10.1 |
| 37 | 52/60 | 1.434 | 18.5 | 10.0 |
| 45 | 52/60 | 1.425 | 18.9 | 10.0 |
| 50 | 52/60 | 1.416 | 19.4 | 10.2 |
| 39 | 52/60 | 1.416 | 18.1 | 10.0 |

### Cross-Config Confirmation

| Config variant | HOLD_MAX | HSM | Winner |
|---|---|---|---|
| T75 | 12 | 0.40 | **P=38** (50/60, Sharpe 1.432) |
| T85 (current) | 15 | 0.25 | **P=38** (52/60, Sharpe 1.481) |

P=38 is the **consistent winner across two different production config variants**. This is the key validation that P=38 is robust, not an artifact of HOLD_MAX=12.

---

## Exact-Live Verification

After confirming P=38 as winner, `live_bot_exact_equity.rs` was re-run against current production config (P=38 already set in `src/live/config.rs`):

```
Final equity: 2.75x
Daily account Sharpe: 1.02
Max drawdown: 22.3%
Trades: 286 (win rate 46.9%)
```

Note: P=38 was already the default in config.rs (set by T75). No code change was needed. The T85 sweep confirms it holds with current HOLD_MAX=15/HSM=0.25.

---

## Comparison Chart

**Chart:** `/home/ubuntu/.openclaw/workspace-krypto/charts/comparison_chart.png`

Shows equity curves (Base5, full history) for P=21 (baseline) vs P=38 (winner) vs runner-ups P=51, P=49, P=37, P=45, plus pass-rate bar chart with Sharpe overlay.

---

## Files

- `examples/t85_hedge_atr_period_current.rs` — sweep harness
- `snapshots/t85_hedge_atr_period_summary.csv` — 96-value sweep results
- `snapshots/t85_hedge_atr_period_windows.csv` — per-universe per-window breakdown
- `snapshots/t85_hedge_atr_period_equity.csv` — Base5 equity curves per P value
- `charts/plot_hedge_atr_period.py` — chart generation script
- `charts/comparison_chart.png` — equity comparison chart

---

## Conclusion

**HEDGE_ATR_PERIOD = 38** is confirmed as the production default. Two independent sweeps (T75 on HOLD_MAX=12, T85 on HOLD_MAX=15) agree on the winner, and P=38 already lives in `src/live/config.rs` as the production default. No code change required.

The parameter was previously hardcoded at 21 with no documented justification. P=38 improves robustness by +2 OOS windows (+13.2% Sharpe) while slightly reducing average drawdown.