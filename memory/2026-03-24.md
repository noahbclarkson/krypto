# 2026-03-24

## 02:29 UTC — Cron summary

- Read `PLAN.md`, pulled latest `v2-rewrite`, and checked the #krypto Discord session context before starting.
- Built the repo successfully with `cargo build`.
- Reran `cargo run --example head_to_head_comparison --profile sweep` to execute the current top priority: explain the reported `MACD+Regime` chronology weak pair.
- Important finding: on the **current live 3000-bar cache**, the earlier `MACD+Regime` weak-pair note is now stale. The fair harness prints:
  - `MACD+Regime`: score **0.756**, **+3960.6%**, **469 trades**, **4/4** quarter windows, **15/15** positive chronological resamples, **4/8** stress universes
  - `MACD`: score **0.599**, **+3540.4%**, **683 trades**, **4/4** quarter windows, **13/15** positive chronological resamples, **4/8** stress universes
- So the old plan note that `MACD+Regime` still failed `[2,3] = -12.1% across 51 trades` no longer holds under the current cache state.
- Added a new diagnostic tool: `krypto/examples/macd_regime_failure_diagnose.rs`
  - Purpose: if a chronology miss appears again, print exact block date ranges plus long/short decomposition and worst trades.
  - This is mainly a research utility to make future failure-attribution sessions faster.
- Updated `PLAN.md` to mark the old weak-pair drill-down as done/stale and replaced it with next steps around harsher survivorship stress and benchmark snapshotting.
- Updated `MEMORY.md` with the new meta-lesson: chronology conclusions can drift as the live cache grows, so benchmark outputs need explicit snapshot/version tracking.
- Updated `krypto/HALL_OF_FAME.md` benchmark snapshot section with the rerun note and the new 15/15 result for `MACD+Regime`.
- Discord report posted: benchmark rerun + cache-sensitivity warning + next step toward harsher survivorship stress.

## 05:34 UTC — Cron summary

- Pulled latest `v2-rewrite`, rechecked the current plan, and reran `cargo run --example head_to_head_comparison --profile sweep` after extending the benchmark harness.
- Added **benchmark snapshot output** directly to `krypto/examples/head_to_head_comparison.rs` so every rerun now writes:
  - `snapshots/head_to_head_latest.md`
  - `snapshots/head_to_head_latest.csv`
  - `snapshots/head_to_head_<UTC timestamp>.md`
  - `snapshots/head_to_head_<UTC timestamp>.csv`
- First archived benchmark snapshot from the new flow:
  - `snapshots/head_to_head_20260324T053442Z.md`
  - `snapshots/head_to_head_20260324T053442Z.csv`
- The rerun kept the same top-level ranking as the previous session:
  - `MACD+Regime`: **0.756 score**, **+3960.6%**, **469 trades**, **15/15** positive resamples, **4/8** stress universes
  - `MACD`: **0.599 score**, **+3540.4%**, **683 trades**, **13/15** positive resamples, **4/8** stress universes
- Important nuance preserved by the snapshot: `MACD+Regime` stays #1 overall in the fair harness, but plain `MACD` still wins the more legacy-heavy baskets (`Legacy5`, `LegacyCore4`, `NoDOGE`, `LargeCaps6`), so survivorship / basket-selection risk is still unresolved.
- Updated `PLAN.md`, `MEMORY.md`, and `krypto/HALL_OF_FAME.md` to record the new artifact trail and move the next priority to a harsher survivorship benchmark / delisting-proxy test.

## 08:33 UTC — Cron summary

- Pulled latest `v2-rewrite`, checked the current benchmark plan, built successfully with `cargo build`, and extended `examples/head_to_head_comparison.rs` with one deliberately harsher **OldGuard6** stress universe:
  - `BTCUSDT, ETHUSDT, XRPUSDT, LTCUSDT, BNBUSDT, EOSUSDT`
  - Rationale: remove recent superstar alts (`SOL`, `DOGE`, `ADA`) and add older 2017-cycle survivors as a simple survivorship / delisting proxy.
- Reran `cargo run --example head_to_head_comparison --profile sweep` and archived a fresh snapshot:
  - `snapshots/head_to_head_20260324T083329Z.md`
  - `snapshots/head_to_head_20260324T083329Z.csv`
- New headline results with **9** stress universes:
  - `MACD+Regime`: **0.745 score**, **+3960.6%**, **469 trades**, **4/4** quarter windows, **15/15** positive resamples, **4/9** stress universes
  - `MACD`: **0.610 score**, **+3540.4%**, **683 trades**, **4/4** quarter windows, **13/15** positive resamples, **5/9** stress universes
- The new **OldGuard6** basket is won by plain `MACD`, not the filtered variant:
  - `MACD`: **+2514.0%**
  - `MACD+Regime`: **+2166.0%**
- Interpretation: the harsher old-guard basket weakens the earlier “filtered MACD is clearly best everywhere” story. `MACD+Regime` still holds the best all-around composite score because its chronology remains cleaner (**15/15** vs **13/15**), but plain `MACD` is now the basket winner in **5/9** universes.
- Overfitting-risk read: survivorship / universe-selection sensitivity is still unresolved; no candidate deserves HOF induction or paper-trading promotion yet.

## 09:05 UTC — Direction update after Noah discussion

- Noah pushed for a more top-down view: don't rush a strategy, spend more time dissecting the backtest mechanism, and broaden the search beyond the MACD family.
- Agreed. Current work has improved rigor substantially, but has also become too locally optimized on the benchmark leader board.
- Updated `PLAN.md` to shift the project into a three-track research program:
  1. **Trust the lab** — deeper trend-harness execution / contamination / fee-slippage audit
  2. **Stress the current leader** — keep testing MACD / MACD+Regime / Turtle family as benchmark leaders, not conclusions
  3. **Broaden edge discovery** — systematically test new families (cross-asset, pair / leader-laggard, funding/basis, ensemble, richer crypto-native features, advanced models later)
- Added an explicit rule to evaluate every cycle whether too much time is being spent refining one candidate versus improving trust or widening search coverage.
- Downgraded lingering "ready for paper trading / fully validated" language in `PLAN.md`; current leaders are benchmark candidates only.
- Need to update the krypto cron prompt next so the autonomous loop re-reads recent work, notices over-focus, and balances strategy discovery against benchmark refinement.

## 11:31 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, current git history, and the latest #krypto Discord session context before choosing work.
- Explicit top-down check: the last several sessions were heavily concentrated on MACD-family benchmark refinement (resampling, attribution, universe stress, snapshots). Useful, but over-focused. Chose **Track A — Trust the lab** for this cycle instead of more leader-board archaeology.
- Built successfully with `cargo build` (same existing warnings only).
- Added a new trust-audit example: `krypto/examples/trend_execution_audit.rs`.
  - It reruns the main trend benchmark candidates under alternate execution assumptions while keeping the same base universe and CPCV-style resampling structure.
  - Scenarios tested: baseline, +5bps slippage/side, +10bps slippage/side, one-extra-bar entry/exit delay, one-extra-bar delay +10bps slippage, and stricter warmup (`200 -> 250` bars).
- Ran `cargo run --example trend_execution_audit --profile sweep 2>&1`.
- Key findings:
  - **Baseline reproduced** the current fair-harness ranking exactly: `MACD+Regime` remained #1 at **+3960.6%**, **469 trades**, **15/15** positive resamples.
  - **Small extra slippage is NOT the main fragility**: adding **10 bps extra slippage each side** only reduced full-sample returns by:
    - `MACD+Regime`: **-93.8%**
    - `Turtle+Regime+MACD`: **-69.8%**
    - `MACD`: **-136.6%**
    while the ordering barely changed.
  - **Timing latency matters much more**: a one-extra-bar entry/exit delay cut:
    - `MACD+Regime`: **-975.0%**
    - `MACD`: **-1062.8%**
    - `Turtle+Regime+MACD`: **-210.5%**
    - `Turtle+Regime`: **+8.5%** (roughly unchanged)
    This makes `Turtle+Regime` the cleanest timing-robust candidate under this audit.
  - **Warmup handling is a trust-risk seam**: increasing warmup from **200** to **250** bars caused major chronology-pass collapses despite still-strong full-sample returns:
    - `MACD+Regime`: **15/15 -> 6/15**
    - `Turtle+Regime+MACD`: **15/15 -> 0/15**
    - `Turtle+Regime`: **15/15 -> 0/15**
    - `Turtle+MACD`: **15/15 -> 0/15**
    - `MACD`: **13/15 -> 15/15**
- Honest read: the next trust problem is not “fees are off by 5 bps”; it is that chronology resample conclusions may still be partially driven by **window-context / indicator-warmup handling**.
- Updated `PLAN.md` to mark the execution audit done and made **warmup / slice-context normalization** the next priority before leaning harder on chronology scores.
- Updated `MEMORY.md` with the new lesson: latency materially reshuffles the benchmark, while warmup sensitivity exposes a real harness-trust issue.

## 14:31 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, current git history, and the recent #krypto Discord session before choosing work.
- Explicit top-down check: the last 3-5 sessions were still mostly MACD-family benchmark refinement (attribution, survivorship stress, snapshots, execution audit). That was becoming too concentrated, so I stayed on **Track A — Trust the lab** instead of more local leaderboard optimization.
- Built successfully with `cargo build`.
- Audited the chronology harness itself and found the core trust bug: quarter/resample windows were recomputing indicators on **sliced test windows**, which reset indicator state / warmup inside each slice and could fabricate warmup-sensitivity conclusions.
- Fixed both harnesses:
  - `krypto/examples/head_to_head_comparison.rs`
  - `krypto/examples/trend_execution_audit.rs`
- New approach: generate signals on the **full-history dataframe**, then evaluate only trades whose signal/entry/exit fall inside the target test window. This preserves indicator context while still restricting the test period.
- Rebuilt and reran `cargo run --example trend_execution_audit --profile sweep 2>&1`.
- Key findings after the fix:
  - Baseline unchanged: `MACD+Regime` still leads at **+3960.6%**, **469 trades**, **15/15** resamples.
  - The earlier warmup-collapse story was mostly a harness artifact. Under the corrected window logic, `Warmup250` keeps **15/15** resamples for `MACD+Regime`, `MACD`, `Turtle+Regime`, `Turtle+Regime+MACD`, and `Turtle+MACD`.
  - Warmup still moves returns / trade counts meaningfully:
    - `MACD`: **+3540.4% -> +4427.8%**, trades **683 -> 668**
    - `MACD+Regime`: **+3960.6% -> +3634.8%**, trades **469 -> 461**
  - Timing latency remains the real economic fragility:
    - `MACD+Regime`: **-975.0%** vs baseline under one-extra-bar delay
    - `MACD`: **-1062.8%**
    - `Turtle+Regime`: **+8.5%** (roughly unchanged)
- Attempted a full rerun of `head_to_head_comparison`, but it stalled long after data load; killed it rather than burn the cycle. Need a fresh rerun / snapshot next session under the corrected harness.
- Honest trust read: the lab is in better shape now because one fake source of chronology instability is gone, but we still should not over-trust the current leader — latency sensitivity is real and the official benchmark snapshots still need to be regenerated under the corrected logic.

## 17:32 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, latest git history, and the recent #krypto Discord context before choosing work.
- Explicit top-down check: the last several sessions were mostly MACD-family benchmark refinement plus trust-audit cleanup. The highest-value next step was to align the **official benchmark artifacts** with the corrected context-preserving window logic, so I stayed on **Track A / Track B bridge work** instead of doing more stale interpretation.
- `git pull` was already up to date. Recent commits confirmed the sequence: execution audit, context-preserving window fix, and prior benchmark snapshotting were all in place.
- Built successfully with `cargo build`.
- Reran `cargo run --example head_to_head_comparison --profile sweep 2>&1` under the corrected harness. The process hung after artifact generation again, but it successfully wrote fresh official snapshot files:
  - `snapshots/head_to_head_latest.md`
  - `snapshots/head_to_head_latest.csv`
  - `snapshots/head_to_head_20260324T145809Z.md`
  - `snapshots/head_to_head_20260324T145809Z.csv`
- Key corrected snapshot results:
  - `MACD+Regime`: **0.822 score**, **+3960.6%**, **469 trades**, **4/4** walk-forward, **15/15** resamples, **4/9** universe wins, **50/64** bootstrap wins
  - `MACD`: **0.659 score**, **+3540.4%**, **683 trades**, **4/4** walk-forward, **15/15** resamples, **5/9** universe wins
- Important trust read: after the context-preserving window fix, the old residual chronology penalty on plain `MACD` disappears in the official snapshot too. The main unresolved split is now **selection / economic realism**, not warmup-driven chronology artifacts.
- Honest caveat: this does **not** make either candidate deployment-ready. Filtered MACD still leads on composite stability, plain MACD still wins more legacy-heavy baskets, and the timing-latency audit remains the bigger trust warning.
- Updated `PLAN.md` and `MEMORY.md` to mark the corrected snapshot rerun done and shift the next emphasis toward `OldGuard6` attribution, harsher legacy baskets, and broader-family search.

## 20:54 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, current git history, and the latest #krypto Discord context before choosing work.
- Explicit top-down check: the last 5-ish sessions were still concentrated on MACD-family benchmark refinement and trust-harness cleanup. That was useful, but over-concentrated. I chose a **Track B survivorship-stress** task that clarifies the current leader split instead of just polishing the leaderboard again.
- Built successfully with `cargo build`.
- Added a focused diagnostic example: `krypto/examples/oldguard_macd_diagnose.rs`.
  - Purpose: decompose the `OldGuard6` basket (`BTC, ETH, XRP, LTC, BNB, EOS`) **symbol-by-symbol** and **block-by-block** under the same 21-bar next-open / 0.1% taker harness.
- Ran `cargo run --example oldguard_macd_diagnose --profile sweep 2>&1`.
- Key findings:
  - Diagnostic matches the benchmark headline: plain `MACD` still wins `OldGuard6` **+2514.0% vs +2166.0%**.
  - But the divergence is **not just an EOS artifact** and not even a simple symbol-count story:
    - full-sample symbol deltas split **3-3**
    - plain `MACD` leads through **XRP (+292.7% delta)**, **LTC (+250.8%)**, and **EOS (+177.5%)`
    - `MACD+Regime` offsets with stronger **ETH (+186.3%)** and **BNB (+170.6%)`
  - The deeper split is chronological / regime-driven:
    - `MACD+Regime` dominates the two large trend-aligned blocks:
      - block **0** (`2018-01-05..2019-03-06`): **-501.9% delta** in favor of `MACD+Regime`, **5/6** symbols
      - block **2** (`2020-05-06..2021-07-05`): **-709.7% delta** in favor of `MACD+Regime`, **4/6** symbols
    - plain `MACD` wins the other four blocks:
      - block **1**: **+443.1%**, **5/6** symbols
      - block **3**: **+127.9%**, **5/6** symbols
      - block **4**: **+32.4%**, **4/6** symbols
      - block **5**: **+146.4%**, **4/6** symbols
- Honest read: the old-guard split now looks like a **regime / basket-composition trade-off**, not a single-symbol mirage. That weakens the easy narrative that plain `MACD` only wins because one dead old alt revived.
- Updated `PLAN.md` and `MEMORY.md` accordingly.
- Next step: add an even harsher old-guard basket **without BNB** (exchange-token bias) or with capped concentration, then start widening search beyond the MACD family again.

## 23:55 UTC — Cron summary

- Oriented on `PLAN.md`, `MEMORY.md`, today + yesterday memory, current git history, and the recent #krypto Discord session before choosing work.
- Explicit top-down check: the last 3-5 sessions were heavily concentrated on MACD-family benchmark refinement (execution audit, window-context fixes, snapshotting, OldGuard attribution). That was useful but over-concentrated, so I deliberately chose **Track C — Broaden edge discovery**.
- Built successfully with `cargo build`.
- Added a new breadth harness: `krypto/examples/non_trend_family_benchmark.rs`.
  - Purpose: benchmark **non-trend families** under the same fair assumptions instead of comparing them via mixed scripts.
  - Setup: non-BTC basket **ETH/SOL/XRP/DOGE/ADA**, with **BTC** used as the benchmark leader for relative-strength / lead-lag features.
  - Same execution assumptions for all candidates: **signal at close, next-open entry, 21-bar hold, 0.1% taker each side**.
- Ran `cargo run --example non_trend_family_benchmark --profile sweep 2>&1`.
- Results:
  - `MACD+Regime`: **+3624.5%**, **386 trades**, **4/4** quarter windows, **15/15** resamples
  - `MACD`: **+3220.3%**, **556 trades**, **4/4**, **15/15**
  - `Turtle+Regime`: **+2701.1%**, **288 trades**, **4/4**, **15/15**
  - `CrossSectionalMomentum`: **+3171.7%**, **514 trades**, **4/4**, **14/15**
  - `LeadLag`: **+807.1%**, **289 trades**, **3/4**, **13/15**
  - `CrossSectionalMeanReversion`: **+521.2%**, **395 trades**, **3/4**, **12/15**
  - `RelativeStrength`: **+325.5%**, **429 trades**, **2/4**, **9/15**
- Honest read:
  - Most scanned non-trend families are **materially weaker** than the current trend yardsticks under current daily assumptions.
  - But **CrossSectionalMomentum is competitive enough to earn deeper follow-up**; it is the first broaden-the-search result that looks like a real second-family candidate rather than obvious noise.
  - Trust risk: this may still be partly **disguised trend / alt-beta exposure**, not a truly orthogonal edge, so the next step is attribution / harsher basket stress, not promotion.
- Updated `PLAN.md` and `MEMORY.md` accordingly.
