
## 05:06 UTC — Chart rendering workaround

- Discord session requested installing missing fonts to fix blank Plotters PNG exports.
- Elevated apt access is NOT available from this Discord session, so system font installation could not be performed here.
- Workaround implemented: exported equity curve data to JSON and rendered visible PNGs with a custom no-font Python PNG renderer.
- Fixed outputs:
  - `krypto/charts/equity_curves_fixed.png`
  - `krypto/charts/drawdown_fixed.png`
- Lesson: when Plotters font backend is broken and apt is unavailable, use no-font renderer/browser-free export rather than shipping blank charts.

## 05:24 UTC — Cron summary

- Oriented on PLAN.md, recent git history, and latest Discord context.
- Built the repo successfully with `cargo build` (only existing warnings in `src/paper/adapter.rs` and `src/live/bot.rs`).
- Ran `cargo run --example investigate_top_candidates --profile sweep 2>&1` to re-check prior walk-forward "top candidates" with timeframe-calibrated windows.
- Result: no alternative trend-library strategy survived robustness gates.
  - ETHFDUSD 1d `VolAdjMomentum`: OOS Sharpe 4.44, +4.3%, but 0/1 windows passed → not robust.
  - ETHFDUSD 4h `VolAdjMomentum`: OOS Sharpe 5.17, +4.0%, but 0/6 passed → not robust.
  - XRPFDUSD 1d / DOGEFDUSD 1d / BTCFDUSD 4h / ETHFDUSD 1h variants were negative, flat, or had no valid windows.
- Updated `PLAN.md`: replaced paper-trading-next framing with systematic stress testing / comparison as the next priority.
- Updated `MEMORY.md` with the new lesson: isolated high OOS Sharpe with weak window counts is noise, not edge.
- Updated `krypto/GRAVEYARD.md` with a new entry killing the alternative trend-library candidates.
- Will report to Noah in #krypto that the alt candidates failed robustness and next step is a true head-to-head comparison harness / harsher validation on Turtle + Regime + MACD.

## 05:42 UTC — Cron summary

- Built a new apples-to-apples benchmark: `krypto/examples/head_to_head_comparison.rs`.
- Design: same 6-symbol USDT universe (BTC/ETH/SOL/XRP/DOGE/ADA), same 21-bar hold, same 0.1% taker fees, and **next-open execution** for every strategy.
- Ran `cargo run --example head_to_head_comparison --profile sweep 2>&1`.
- Result: **plain MACD ranked first** in the fair harness, not Turtle+Regime+MACD.
  - `MACD`: +3540.4% aggregate return, 683 trades, 53.6% win rate, **4/4 positive walk-forward windows**.
  - `Turtle+Regime+MACD`: +3309.0%, 349 trades, 55.9% win rate, **4/4 windows positive**.
  - `MomentumBreakout`: +2803.1%, 266 trades, 4/4 windows positive.
  - All seven tested trend variants passed the simple positive-return / 30+ trades window gate.
- Key lesson: ranking claims are sensitive to execution assumptions and script design; we need a single comparison harness before trusting "best strategy" narratives.
- Updated `PLAN.md` to make **CPCV / harsher resampling on MACD vs Turtle+Regime+MACD** the next priority instead of paper-trading readiness.
- Updated `MEMORY.md` with the comparison-harness finding.
- Discord report should say: new fair benchmark installed, MACD currently leads, next step is harsher resampling rather than deployment.

## 08:29 UTC — Cron summary

- Oriented on `PLAN.md`, recent git history, and latest #krypto Discord context; no new direction from Noah beyond the standing research mandate.
- Built the repo successfully with `cargo build` (same existing warnings only).
- Extended `krypto/examples/head_to_head_comparison.rs` with harsher validation:
  - **6-block chronological resampling** (15 CPCV-style test combinations)
  - **leave-one-symbol-out** ranking checks
  - **symbol bootstrap** ranking checks
  - **equity / drawdown PNG outputs** for the top two strategies
- Ran `cargo run --example head_to_head_comparison --profile sweep 2>&1`.
- Result: raw return leader and stability leader diverge.
  - `MACD`: **+3540.4%**, 683 trades, **4/4 quarter windows positive**, but **14/15** positive chronological resamples.
  - `Turtle+Regime`: +3193.0%, 398 trades, **15/15** positive chronological resamples, but 3/4 quarter windows positive.
  - `Turtle+Regime+MACD`: +3309.0%, 349 trades, **4/4 quarter windows positive** and **15/15** positive chronological resamples.
- Honest read: the cleanest robustness candidate is now **Turtle+Regime+MACD**, while **MACD** remains the strongest raw-return candidate. No strategy earned deployment status from this session.
- Wrote charts:
  - `krypto/charts/head_to_head_top2_equity.png`
  - `krypto/charts/head_to_head_top2_drawdown.png`
- Updated `PLAN.md`, `MEMORY.md`, and `krypto/HALL_OF_FAME.md` to reflect the harsher-resampling result.
- Discord report should say: MACD still leads on raw return, but regime-filtered Turtle variants are more stable under harsher chronological resampling; next step is survivorship/universe stress, not deployment.

## 09:22 UTC — Font-backed chart export restored

- Noah asked for charts to be descriptive and contain text.
- Re-ran `generate_equity_curves` and `rigorous_validation` after font/ttf support was added.
- Plotters text rendering now works again.
- Sent updated inline Discord charts with captions/explanations:
  - `charts/rigorous_validation_equity.png`
  - `charts/equity_curves.png`
  - `charts/drawdown_chart.png`
- Keep using the font-backed renderer by default now that the host supports it again.

## 11:29 UTC — Cron summary

- Oriented on `PLAN.md`, pulled latest repo state (`git pull` clean), and checked recent #krypto Discord context.
- Built successfully with `cargo build` (same existing warnings only).
- Extended `examples/head_to_head_comparison.rs` with **explicit universe-stress baskets**:
  - `Base6` = BTC/ETH/SOL/XRP/DOGE/ADA
  - `Legacy5` = BTC/ETH/XRP/LTC/BNB
  - `Majors4` = BTC/ETH/BNB/LTC
  - `AltMix6` = ETH/SOL/XRP/DOGE/ADA/LTC
  - `NoSOL` = BTC/ETH/XRP/DOGE/ADA/BNB
- Added automatic data loading for `LTCUSDT` and `BNBUSDT`, then ran:
  - `cargo run --example head_to_head_comparison --profile sweep 2>&1`
- Result: **MACD ranked #1 in all 5 explicit universes**:
  - Base6: +3540.4% vs Turtle+Regime+MACD +3309.0%
  - Legacy5: +2271.6% vs +1910.7%
  - Majors4: +1781.0% vs +1574.6%
  - AltMix6: +3647.4% vs +3107.6%
  - NoSOL: +3544.8% vs +3152.8%
- But chronology stress still disagrees:
  - `MACD`: 14/15 positive resamples
  - `Turtle+Regime`: 15/15
  - `Turtle+Regime+MACD`: 15/15
  - `Turtle+MACD`: 15/15
- Honest conclusion: MACD is **not** just a newer-alt artifact; it is the strongest raw-return / basket-robust candidate. But regime-filtered Turtle variants remain cleaner under chronology-aware robustness.
- Updated `MEMORY.md`, `PLAN.md`, and `krypto/HALL_OF_FAME.md` to reflect the new split between universe robustness and chronology robustness.
- Committed + pushed repo changes: `f3f924d` — `research: add universe stress to head-to-head harness`
- Posted summary to #krypto Discord (message `1485603154476400700`).
- Next step set in `PLAN.md`: inspect the single failing MACD resample and build a composite ranking score instead of over-weighting one axis.

## 14:29 UTC — Cron summary

- Oriented on `PLAN.md`, pulled latest repo state (`git pull` clean), and checked latest #krypto Discord context.
- Built successfully with `cargo build` (same existing warnings only).
- Extended `examples/head_to_head_comparison.rs` with two ranking/diagnostic upgrades:
  - **composite ranking score** = chronology 35%, universe stress 20%, bootstrap 15%, leave-one-out 10%, walk-forward 10%, full return 5%, trade depth 5%
  - explicit **failing chronological resample diagnostics** (block-pair level)
- Re-ran `cargo run --example head_to_head_comparison --profile sweep 2>&1`.
- Result: **MACD still ranks #1 overall** under the weighted score:
  - `MACD`: score **0.824**, +3540.4%, 683 trades, 4/4 quarter windows, 14/15 resamples, #1 in 5/5 stress universes
  - `Turtle+MACD`: score 0.564, +3010.5%, 15/15 resamples
  - `Turtle+Regime+MACD`: score 0.556, +3309.0%, 15/15 resamples
  - `Turtle+Regime`: score 0.552, +3193.0%, 15/15 resamples
- Key diagnostic: the **only** MACD chronology miss is **test blocks [0,2]**, which returns **-16.5% across 72 trades**.
- Honest interpretation: MACD remains the best all-around candidate in the fair weighted harness, but it still has one specific chronology weakness that needs per-symbol / regime attribution before trust should increase.
- Updated `PLAN.md` and `MEMORY.md` with the new ranking framework and the explicit MACD failure pair.
- Next step set in `PLAN.md`: inspect the `[0,2]` MACD resample at the per-symbol / per-block level and export richer per-resample visuals.

## 17:29 UTC — Cron summary

- Oriented on `PLAN.md`, pulled latest repo state (`git pull` clean), and checked recent #krypto Discord context.
- Built successfully with `cargo build` (same existing warnings only).
- Extended `examples/head_to_head_comparison.rs` with **per-symbol / per-block attribution** for the known MACD chronology miss and exported:
  - `charts/macd_failure_pair_symbol_returns.png`
- Re-ran `cargo run --example head_to_head_comparison --profile sweep 2>&1`.
- Result: the lone MACD failing resample (**test blocks [0,2]**, **-16.5%**, 72 trades) is **not** a broad basket collapse.
  - Main damage is concentrated in **BTCUSDT (-88.9%)** and **ETHUSDT (-88.2%)** across 12 trades each.
  - Offsetting symbols stay positive on the same pair: **DOGE +49.2%**, **ADA +44.7%**, **SOL +39.9%**, **XRP +26.7%**.
  - `Turtle+Regime+MACD` stays positive on the same windows for BTC/ETH (**BTC +57.8%, ETH +110.8%**), which points to a filter / regime issue rather than the whole timeframe being toxic.
- Updated `PLAN.md`: marked attribution + failure chart export done; next priority is explaining the BTC/ETH macro / whipsaw regime inside blocks `[0,2]`.
- Updated `MEMORY.md` and `krypto/HALL_OF_FAME.md` with the new attribution result.
- Next Discord report should say: MACD still leads overall, but its only chronology miss is now traced mainly to BTC/ETH; next step is explaining that regime mechanically rather than abandoning the candidate.

## 20:29 UTC — Cron summary

- Oriented on `PLAN.md`, pulled latest repo state (`git pull` clean), and checked recent #krypto Discord context.
- Built successfully with `cargo build` (same existing warnings only).
- Investigated the lone plain-MACD chronology failure mechanically instead of just ranking around it.
- Mapped failing resample blocks `[0,2]` to calendar ranges:
  - block 0 = `2018-01-05 .. 2018-12-11`
  - block 2 = `2019-11-18 .. 2020-10-23`
- Key mechanical finding: the old MACD weakness is mostly **wrong-side BTC/ETH long exposure in the 2018 bear block**, not a full-basket collapse.
  - BTC block 0: longs `-54.8%`, shorts `+38.7%`
  - ETH block 0: longs `-96.7%`, shorts `+144.6%`
- Implemented a light fix directly in `examples/head_to_head_comparison.rs`:
  - new strategy = **`MACD+Regime`**
  - only take MACD longs when `price > SMA200`
  - only take MACD shorts when `price < SMA200`
- Re-ran `cargo run --example head_to_head_comparison --profile sweep 2>&1`.
- Result: **`MACD+Regime` is now the strongest all-around candidate in the fair harness**:
  - score **0.769**
  - full return **+3960.6%**
  - **469 trades**
  - **4/4** positive quarter windows
  - **14/15** positive chronological resamples
  - **#1 in 4/5** explicit stress universes
- Important caution: the filter improves the old `[0,2]` weakness, but `MACD+Regime` still has one chronology miss of its own: **test blocks `[2,3]` = `-12.1%` across 51 trades**.
- Updated `PLAN.md`, `MEMORY.md`, and `krypto/HALL_OF_FAME.md` to reflect the shift from plain MACD to filtered MACD as the leading benchmark candidate.
- Next step set in `PLAN.md`: stress-test `MACD+Regime` more directly and explain its new `[2,3]` weak pair before any Hall-of-Fame / paper-trading discussion.

## 23:29 UTC — Cron summary

- Oriented on `PLAN.md`, pulled latest repo state (`git pull` clean), and checked recent #krypto Discord context.
- Built successfully with `cargo build` (same existing warnings only).
- Extended `examples/head_to_head_comparison.rs` again in two directions:
  - added harsher / legacy-biased stress baskets: `LegacyCore4`, `NoDOGE`, `LargeCaps6`
  - added direct **MACD+Regime failure-pair attribution** plus chart export at `charts/macd_regime_failure_pair_symbol_returns.png`
- Re-ran `cargo run --example head_to_head_comparison --profile sweep 2>&1`.
- Result: the earlier “MACD+Regime clearly wins” framing softened under tougher basket stress.
  - `MACD+Regime`: score **0.709**, **+3960.6%**, 469 trades, **14/15** resamples, **#1 in 4/8** universes
  - `MACD`: score **0.691**, **+3540.4%**, 683 trades, **14/15** resamples, **#1 in 4/8** universes
  - `Turtle+MACD` / `Turtle+Regime` / `Turtle+Regime+MACD` still hold **15/15** chronology passes but do not win basket stress
- Key new diagnostic: `MACD+Regime`’s lone failing resample **[2,3] = -12.1% across 51 trades** is concentrated in:
  - **XRPUSDT -66.8%**
  - **ETHUSDT -42.5%**
  - **DOGEUSDT -15.2%**
  - offsets: **ADAUSDT +75.2%**, **SOLUSDT +33.8%**, **BTCUSDT +3.5%**
- Important nuance: block **2** is mostly okay; the real damage is primarily **block 3**, which suggests a later whipsaw / regime-friction problem rather than the filter failing uniformly across both blocks.
- Updated `PLAN.md` and `MEMORY.md` with the new stress / attribution read.
- Updated `krypto/HALL_OF_FAME.md` to record that the benchmark remains research-only and that legacy-basket stress now splits `MACD+Regime` and `MACD` 4-4.
- Next step set in `PLAN.md`: map pair `[2,3]` to exact calendar ranges and long/short decomposition before any HOF / paper-trading discussion.
