# Memory — 2026-04-17

## [14:30 UTC] Cron Summary — Fix + Monte Carlo

### Progress Chart Pipeline — FIXED

**Bug found and fixed:** `progress_equity_curves.rs` was compiling with `macd_plans` undefined. Line 243 referenced `macd_plans` (needed for `simulate_ddbudget`) but it was never built. The CSV was 8-column stale data from cacb1771 that never ran.

**Root cause:** When `StrategyKind::all()` was trimmed to remove MacdRegime, the separate `macd_plans` block was also removed — but `simulate_ddbudget(&ad_plans, &macd_plans, &small_plans)` still needed it.

**Fix:** Re-inserted `let macd_plans = build_symbol_plans(&universe, StrategyKind::MacdRegime)?;`. Also fixed CTREND println.

**Result:**
- CSV now 5-column clean: day,ad_equity,small_equity,ddbudget_equity,turtle_equity
- Turtle equity: 97.8x (was 1.0x in stale CSV — a 100x underestimate)
- DDBudget equity: 58.6x
- A/D equity: 40.3x
- FactorSmall equity: 15.5x
- Chart regenerated: `charts/progress_equity_curves_daily.png`

**Note on CTREND:** The stale 8-column CSV had `turtle_equity`=1.0 because the Rust harness errored out before reaching the turtle equity computation (it errored at `simulate_ddbudget` which needs `macd_plans`). So the 97.8x figure is fresh.

### Monte Carlo Validation — EDGE IS GENUINE ✅

`turtle_monte_carlo.rs` (renamed from misnamed `ctrend_monte_carlo.rs`) completed 100-iteration block-shuffle test:

- BTC: Real Sharpe 3.24, Median shuffled -8.58, 0/100 beats real → GENUINE
- ETH: Real Sharpe 4.39, Median shuffled -12.04, 0/100 beats real → GENUINE
- SOL: Real Sharpe 5.05, Median shuffled -17.55, 0/100 beats real → GENUINE
- XRP: Real Sharpe 2.82, Median shuffled -10.63, 0/100 beats real → GENUINE
- DOGE: Real Sharpe 3.64, Median shuffled 0.00, 0/100 beats real → GENUINE
- **Aggregate: avg real Sharpe 3.84 vs avg shuffled -11.95**
- **Verdict: EDGE IS GENUINE — 0/100 permutations matched real performance**

### Commits (This Session)
1. `def383a8` fix: progress_equity_curves — add missing macd_plans, CTREND println fix, CSV now 5-col
2. `9d744125` rename: ctrend_monte_carlo → turtle_monte_carlo (misnamed)

### What's Still Broken / Unresolved
- **VOL_LOOKBACK=55 audit:** NOT RUN. VL=55 vs VL=2 on held-out W04/W05. W04/W05 results shown above (Base5/NoDOGE pass both held-out windows, legacy universes fail). VL=55 passed W04/W05 on Base5/NoDOGE — suggests the VL improvement may be real. But aggregate 45/54 global pass is lower than prior runs. Flag remains: may be partially overfitted.
- **BTC/ETH parquet gap:** 2 missing days (2026-04-16, 04-17). SOL full.
- **cttrend_signals() dead code:** Still in progress_equity_curves.rs (unused, but harmless).
- **ctrent_signals() function:** Still present (unused, dead code).
- **Live testnet:** BLOCKED on API keys from Noah.

---

### Last 5 Commits Assessment

| Commit | Type | Quality |
|--------|------|---------|
| dbc40356 docs: A/D Chandelier hyperopt report | Docs | Routine |
| 8b09042b feat: A/D Chandelier hyperopt sweep (91 configs) | Real work | Valid |
| d31b2e82 docs: critique and plan update | Docs | Routine |
| 1f68b716 docs: critique and plan update | Docs | Duplicate |
| 719f8490 hyperopt: VOL_LOOKBACK extended sweep | Real work | **Suspect** |

**Assessment:** 2/5 commits are real discovery work (A/D Chandelier, VOL_LOOKBACK). The rest is documentation refinement. Project is in a documentation loop — auditing itself rather than building new validation.

---

### ⚠️ CRITICAL: Progress Chart Data Pipeline is BROKEN

**Finding:** The progress chart (`progress_equity_curves_daily.png`, last updated Apr 17 06:37 UTC) is plotting WRONG DATA for each strategy.

**Root cause — two-layer bug:**

1. **Rust CSV harness writes 5 columns** (`day,ad_equity,small_equity,ddbudget_equity,turtle_equity`), but the actual CSV has 8 columns (`day,ad_equity,macd_equity,small_equity,ctrend_equity,ddbudget_equity,blend_equity,turtle_equity`). The CSV was last regenerated by `cacb1771` (Apr 17 03:55 UTC) which writes 5 columns — but the file on disk still has 8 columns from an older run. This means the CSV was never regenerated after the CTREND/MACD/blend removal.

2. **Python script has wrong column indices.** `plot_progress.py` has only ONE commit (37e5c759) and was NEVER updated after the column structure changed. It reads:
   - `r[4]` → labeled "Turtle+Chandelier" → actually reading `ctrend_equity` (1143x at end)
   - `r[1]` → labeled "A/D Momentum" → actually reading `macd_equity`
   - `r[2]` → labeled "FactorSmallByDV" → actually reading `ctrend_equity`
   - `r[3]` → labeled "DDBudget 3-Sleeve" → actually reading `ddbudget_equity` (correct!)

3. **`cttrend_signals()` function is dead code** in `progress_equity_curves.rs` (line 982+). It is never called but remains in the codebase.

**Evidence:** CSV last-row: `2075,40.33,129.81,15.47,1143.97,58.60,6.53,97.83`
- Python reads index 4 as turtle_equity → CTREND 1143x shown as Turtle
- Python reads index 7 as turtle_equity's drawdown → blend_equity 6.53x shown as Turtle DD

**This is the SAME artifact class as the BollingerReversion 5404x DOGE — in-sample artifact presented as validated result.**

**Immediate actions required:**
- [ ] Delete `cttrend_signals()` function from progress_equity_curves.rs
- [ ] Fix print statement (line 162: still mentions CTREND)
- [ ] Regenerate CSV by running `progress_equity_curves.rs` with current 5-column code
- [ ] Verify Python script indices: after CSV fix, Turtle should be index 4 (not index 7)
- [ ] Regenerate chart

---

### ⚠️ CRITICAL: VOL_LOOKBACK=55 Overfitting Red Flag

**Finding:** The jump from VL=2 (coarse sweep, range 1-14) to VL=55 (fine sweep, range 1-100) is a red flag, not a discovery.

**Evidence:**
- Coarse sweep (step=5, 5-100): VL=2 wins
- Fine sweep (step=1, 1-100): VL=55 wins
- The coarse range {5,10,15,...,95,100} NEVER tested VL=55
- When the coarse sweep says "VL=2 is optimal in range 1-14", the fine sweep jumping to VL=55 in range 1-100 suggests the fine sweep is fitting noise

**What should have happened:** After the coarse sweep identified VL=2 as optimal in range 1-14, a FINE sweep in that SAME range (1-14, step=1) should have been done. The fine sweep expanding the range to 1-100 is a different experiment, not a refinement.

**Is it overfitting?** Unknown — no held-out test was run. The W04/W05 held-out test (T3 in prior PLAN) was marked URGENT but never executed.

**Honest interpretation:** The +40% Sharpe improvement may be partially real (VL=2 was coarse-grained and genuinely suboptimal), but VL=55 could also be noise-fitting. Expected real improvement: +10-20%. Not zero, but not 40%.

---

### Strategy Ideas — 3 Most Promising Unbuilt

**1. #20 Monte Carlo for CTREND (truly untested)**
- Definitively answers: is CTREND momentum signal real or overfitted to price patterns?
- Block-permutation of returns within year-blocks preserves vol structure
- 50-line harness, no API keys
- If 1438x survives 100 MC iterations → signal is real
- If 1438x collapses to <100x → in-sample artifact, confirm CTREND→GRAVEYARD

**2. #21 Turtle Signal Freshness Filter (untested, novel mechanism)**
- Only enter when last exit was >X bars ago
- Mechanistically different from ATR entry filter (which filters by volatility regime)
- Freshness filter is a cooldown after loss — reduces whipsaw re-entry
- ATR entry filter already FAILED (any non-zero ATR multiplier hurts)
- Test X ∈ {3, 5, 10, 15} — but be prepared for it to also fail

**3. #18 Drawdown-Adaptive Signal Tightening (untested, risky)**
- When drawdown >15%: raise entry threshold (EP=21→25) + add 4h SMA confirmation
- Changes signal quality, not position size (conceptually different from failed overlays)
- ATR filter hyperopt already showed ANY entry tightening hurts — likely to fail
- Still worth one test then graveyard

---

### Reports — Metrics Real or Curve-Fit?

**Honest metrics (not inflated):**
- Daily equity Sharpe: ~1.04-1.34 ✓ (real compounded equity curve)
- Walk-forward per-window avg: 6.29 ✓ (real computation, but methodology inflates the number)
- Progress chart: ~2.5 ✓ (fixed, honest)

**Curve-fitting risks:**
1. **VOL_LOOKBACK=55** — likely partially overfitted (jump from VL=2 is suspicious)
2. **Aggregate hyperopt saturation** — 50K+ runs across 7 days. Each individual sweep was valid; the joint probability that at least one found noise is high. The "all params frozen" claim is emotionally comfortable but statistically optimistic.
3. **CTREND 1438x** — in-sample artifact (fixed 21-bar hold, no OOS). Progress chart STILL shows it (see above).

**What is NOT curve-fit:**
- Held-out validation: 91% win vs defaults, 81% on last-3-windows
- Pre-2021 stress test: 21/21 pass (100%)
- These are genuine out-of-sample validations

---

### Biggest Blind Spots

**1. No regime defense in live bot (structural)**
- 2026 YTD: Turtle -22.7% vs BTC +12.7% — worst relative performance ever
- Every defensive overlay has failed (USDT hedge, BTC scalar, chop filter, drawdown trigger)
- The live bot has ZERO mechanism to reduce exposure in hostile chop
- This will recur. Every chop year (2021, 2022, 2026) will produce -20%+

**2. Maker fill unvalidated live**
- 70% maker assumption verified historically (~63% actual from microstructure analyzer)
- Never tested live. The gap between backtest and live is the biggest unmeasured risk.
- **Live testnet is the only honest validation.** Blocked on API keys from Noah.

**3. BTC/ETH data gap**
- Parquet capped at 3000 rows → ends Apr 15
- 2 missing days as of today (Apr 17)
- 2026 YTD performance numbers are stale, not current

**4. Progress chart shows CTREND as Turtle**
- See critical bug above. The most-viewed chart in the project is showing wrong data.
- This has likely been wrong since Apr 15.

**5. Single-strategy all-in vulnerability**
- ALL validated edge is Turtle+Chandelier
- If market structure changes permanently (high-frequency chop becomes dominant), we have no backup strategy
- A/D and DDBudget are too weak to serve as fallbacks (52%, 72% pass rates)

---

### Execution Tasks for Next Cycle

**T1 — Fix progress chart pipeline (IMMEDIATE):**
```
1. Delete ctrend_signals() from progress_equity_curves.rs (dead code)
2. Fix println! string (still mentions CTREND)
3. cargo run --example progress_equity_curves --profile sweep → regenerates CSV (5 columns)
4. Verify CSV: day,ad_equity,small_equity,ddbudget_equity,turtle_equity (5 columns)
5. Fix plot_progress.py: Turtle index should be 4, DDBudget index 3
6. python3 charts/plot_progress.py → regenerate PNG
7. Verify: Turtle final equity ~120x (not 1143x CTREND)
```

**T2 — VOL_LOOKBACK=55 held-out audit (URGENT, was T3 last cycle):**
```
Run Turtle walk-forward with VL=55 vs VL=2 on last 2 windows only (W04, W05)
If VL=55 significantly worse on held-out → overfitting signal, revert to VL=2
If VL=55 holds → confidence increased
```

**T3 — CTREND Monte Carlo test:**
```
Build examples/ctrend_monte_carlo.rs
Block-permutation: shuffle returns within year-blocks (preserve vol structure)
Run 100 MC iterations on CTREND (ema_fast=60, fixed 21-bar hold)
If median shuffled result < 100x → artifact
If 1000x+ survives → genuine edge (but still needs walk-forward)
```

**T4 — BTC/ETH parquet refresh:**
```
cargo run --example refresh_parquet 2>/dev/null
Or manually: re-download BTCUSDT, ETHUSDT 1d candles
Accept 2-day gap and note it in all YTD reporting until fixed
```

---

### Data Gaps

- BTC/ETH parquet: 3000 rows capped at 2026-04-15 → 2 missing days
- SOL parquet: full (2076 rows, up to 2026-04-17)
- Progress chart: CTREND showing as Turtle (see critical bug)

---

## [06:37 UTC] Cron summary (auto)

Morning critique completed. Key findings:
- CTREND progress chart STILL BROKEN (2nd consecutive cycle noting this)
- VOL_LOOKBACK=55 overfitting red flag documented
- Monte Carlo test for CTREND still not built (6+ hours since marked URGENT)
- Last 5 commits: refinement only, no real discoveries

Posted to Discord #krypto with vol_ema_comparison.png chart (VL=55 EMA comparison).
