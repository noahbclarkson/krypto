# Memory — 2026-05-01

**Session:** Cron / Critique + Strategy Research | Kira | 2026-05-01 09:57 UTC

---

## [09:57 UTC] Orient

- **Git:** `v2-rewrite`, 8 commits since last session, no new code branches
- **Discord:** 0 new messages in channel session — nothing from Noah
- **Current truth:** Live Turtle-only ATR exit bug-fixed (2026-05-01 02:00 UTC). Prior Turtle-only validation metrics are STALE. T38 (corrected live-path revalidation) is the single most important unstarted task.
- **Reports health:** `daily_progress.csv` shows 2026-05-01 harness run: Turtle 108.1x (Sharpe 0.98) vs prior 124.1x/1.00 and 221.5x/1.04. The equity dropped because CHAND_PERIOD=11 was used in the harness (not CHAND_PERIOD=7). Harness needs update. DDBudget still milestone-aggregated. Equity numbers are inconsistent across runs.

---

## [09:57 UTC] Critique — What We Actually Built Last Session

### 1. Live Turtle ATR Stop Bug Fix ✅ — GENUINE
Found and fixed in `src/live/bot.rs`:
- ATR buffer seeded with wrong period (CHAND_PERIOD=7 vs TURTLE_ATR_PERIOD=24)
- Long trailing stop used `lowest_low - ATR` instead of `highest_high - ATR`
- HOLD_MAX not enforced during ATR warmup

Added unit tests. `cargo test live::bot` passes 7/7. `cargo build --profile sweep` compiles clean. **This is real progress.**

### 2. VOL_LOOKBACK VL=96 Sweep ⚠️ — SAME-HARNESS ARTIFACT RISK
Commit `76c6fa12` ("hyperopt-2026-05-01: VOL_LOOKBACK extensive sweep — winner VL=96") is the **exact same pattern as EP=24**:
- VL=8 was confirmed on 2026-04-29 via 100-value sweep
- VL=96 was "found" on 2026-05-01 via another 100-value sweep
- Same harness, same universes, higher resolution sweep, different winner
- EP=24 was similarly "found" on the same session it was rejected in (in-sample inflation on same OOS data)
- **The VOL_LOOKBACK situation is EP=24.VL. Until VL=96 vs VL=8 is tested on held-out data or Base5-only (a universe the sweep was NOT optimized for), treat VL=96 as unstable.**
- PLAN.md already flagged this: "Same-harness artifact" — but we shipped it anyway
- **Verdict:** Keep production at VL=8 until Base5-only comparison confirms VL=96 wins in the universe we actually care about

### 3. ATR_RANK=5 — Live Integrated, Equity Harness Broken
- ATR_RANK=5 is in `config.rs` and `bot.rs` ✅
- BUT `daily_progress.csv` 2026-05-01 shows **108.1x / Sharpe 0.98** for Turtle+Chandelier vs 124.1x / 1.00 prior run and 221.5x / 1.04 on 2026-04-29
- Root cause: The 2026-05-01 run used CHAND_PERIOD=11 in the harness, not CHAND_PERIOD=7. Harness has stale params.
- ATR_RANK=5 live_bot_only shows 41.0x / 0.87 — this is a DIFFERENT STRATEGY (live bot semantics) and cannot be compared to 108.1x directly
- **The equity number is not trustworthy because the harness isn't synchronized with config.rs. This is a data integrity failure.**

### 4. S6 close_losers GRAVEYARDED ✅ — Correct
- `examples/rebalancing_9universe.rs`: S6 close_losers incompatible with Turtle-only live exit
- 0 trades across all universes/windows — Turtle ATR fires in 3-5 bars, never reaches 5-bar rebalancing check
- GRAVEYARD confirmed. No further action.

### 5. Donchian Sleeve REJECTED ✅
- 34/54 pass (63%) < 69.1% guardrail. Correct decision. No further action.

### 6. Regime-Adaptive Exit (RAE) — UNBUILT
- Marked as "NEW CONCEPT" in strategy-ideas.md. Never built. Still untested after 2+ sessions of being listed.

---

## [09:57 UTC] The 3 Most Promising Unbuilt Ideas

### #1: T38 — Corrected Live Turtle-Only Walk-Forward Validation
**Priority: CRITICAL.** The 2026-05-01 bug fix changed `src/live/bot.rs` semantics. ALL prior Turtle-only walk-forward results (including ATR_RANK=5 validation) are now stale. We MUST re-run the walk-forward harness with exact live-bot semantics (Turtle-only exit, correct ATR buffer, HOLD_MAX independent of ATR warmup) before citing any Turtle-only result as production evidence.

This is trust infrastructure, not research. Without it, every "validated" Turtle-only metric is potentially wrong.

### #2: Regime-Adaptive Exit (RAE) — Vol-Conditional Chandelier Multiplier
**Priority: MEDIUM.** Concept genuinely untested. Prior vol-contingent Chandelier failed (uniform multiplier change → identical results). RAE uses a different mechanism: conditional multiplier based on current ATR percentile rank rather than static vol schedule. Hypothesized mechanism: high-vol regime → looser stop (avoid premature stop-out), low-vol regime → tighter stop (capture chop breaks faster).

What to build: `examples/regime_adaptive_exit_walkforward.rs` — sweep `high_vol_mult ∈ {1.0, 1.05, 1.1, 1.15} × low_vol_mult ∈ {2.0, 2.1, 2.2, 2.3} × 9 universes × 6 windows` vs fixed M=2.30 baseline.

**Reject if:** pass rate or Sharpe degrades. If no improvement: vol-conditional exit space is truly exhausted and we should stop revisiting it.

### #3: Live-Path Parity Audit
**Priority: HIGH.** The gap between research harness and live bot code has caused multiple false conclusions (dual-exit attribution, S6 incompatibility, EP=24 inflation). Build `examples/live_path_parity_audit.rs` that:
1. Runs both the research harness and the live bot semantics on identical data
2. Compares equity curves bar-by-bar
3. Reports `LIVE_COMPATIBLE` / `RESEARCH_ONLY` / `REQUIRES_LIVE_INTEGRATION` for every strategy result in HALL_OF_FAME.md

This is audit infrastructure. It prevents the next month of validating things the bot cannot trade.

---

## [09:57 UTC] Are Our Metrics Real? Sharpe 5.0+ Claims

**Short answer: No — we don't know.**

Walk-forward Sharpe 3.170 (per-window, fee-corrected) is the most defensible number. Daily equity Sharpe 1.04 is the honest number. The 5.0+ Sharpe claims in HALL_OF_FAME are:
- Per-window average Sharpe ratios (methodologically inflated — means of ratios ≠ ratio of means)
- Not directly comparable to daily equity Sharpe
- Computed on a specific 9-universe set that includes favorable chop windows

The equity number (108.1x vs 221.5x) is unreliable because the harness uses stale params (CHAND_PERIOD=11 vs CHAND_PERIOD=7 production default).

**Specific curve-fitting concerns:**
- **Fee model**: Fixed in T35, but old runs used the buggy no-fee model
- **Harness params drift**: progress_equity_curves.rs uses CHAND_PERIOD=11; production is CHAND_PERIOD=7
- **VOL_LOOKBACK**: VL=96 may be same-harness artifact (same pattern as EP=24)
- **ATR_RANK=5 equity**: 41.0x is from live-bot-only semantics, incomparable to research harness 108.1x
- **DDBudget Sharpe 7.24**: milestone-aggregated, not comparable to daily equity Sharpe of 1.04

---

## [09:57 UTC] Biggest Blind Spots

### Blind Spot #1: We Have Not Re-Run the Corrected Turtle-Only Validation
We fixed a live-path bug and have not re-validated. Every metric that mentions "Turtle-only" is potentially wrong. This is the biggest credibility problem in the project right now.

### Blind Spot #2: Equity Harness Desync from Production Config
`progress_equity_curves.rs` uses CHAND_PERIOD=11. Production config is CHAND_PERIOD=7. We ship equity charts with wrong params and don't notice because we don't have automated parity checking.

### Blind Spot #3: We're Confirming, Not Discovering
ATR_EMA [1..200] → NULL. ATR_ENTRY_MULT [0..2.00] step 0.01 → EM=0.00. VOL_LOOKBACK [1..100] → VL=96 (same harness as VL=8). FRESHNESS_COOLDOWN [0..70] → cd=0. HOLD_MAX [1..100] → HM=12. Every parameter has been confirmed at least twice on the same harness.

The research loop is a confirmation spiral. The only genuinely untested ideas are S6 rebalancing (built but graveyard'd), T31 Donchian sleeve (built but rejected), T40 RAE (never built), and T38 live-path revalidation (never built). We are not running out of ideas to test — we are running out of willingness to build the untested ones.

### Blind Spot #4: Only Testing on 6 Symbols
All walk-forward validation is on Base5/NoDOGE/LargeCaps5/Legacy3-5/etc. These are the most liquid, most trending crypto assets. We have not tested on mid-caps (BNB/LINK/AVAX/MATIC/UNI — rejected at 60%), low-volume legacy (LTC/EOS/BCH — fail in chop), or stablecoin pairs. Our pass rates are artificially inflated by only validating on the assets that trend best.

### Blind Spot #5: No Live Testnet = All Metrics Upper Bounds
4+ weeks blocked on API keys. Every number is a backtest. We cannot know if the live bot has bugs until it runs on testnet.

---

## [09:57 UTC] Honest Project Status

**Research:** Exhausted of new mechanisms. All untested ideas either built+rejected (Donchian sleeve, S6, position scaling) or never built (RAE, live-path parity). Confirming settled results at higher resolution is not research.

**Engineering:** One genuine bug fixed (live Turtle ATR stop). One confirmed incompatibility discovered (S6 + Turtle-only). Multiple stale-param desyncs remain (harness vs config.rs).

**Validation:** All Turtle-only metrics are stale until T38 re-runs. All equity numbers are unreliable until harness params sync with config.rs.

**Live:** BLOCKED. No API keys = no testnet = no real validation.

**Net verdict:** The project is in a holding pattern. Research loop is closed by exhaustion, not by proof. The highest-value actions are: (1) re-run T38, (2) sync harness params, (3) build RAE once if there's appetite, (4) get API keys.