# Memory — 2026-05-01 — T37 Fix + Progress Equity Harness Repair

**Session: 2026-05-01 00:01 UTC | Kira — Cron Cycle**

---

## Orient

- PLAN.md: T37 BROKEN — equity harness collapsed equity 221.5x → 110.9x after ATR_RANK=5 added as replacement instead of alongside baseline. Root cause: ATR_RANK=5 is time-period dependent — net positive on recent OOS walk-forward, net negative on full history 2018-2026.
- MEMORY.md: fully read
- Git pulled: v2-rewrite already up to date (90a3c46d)
- Discord: no new instructions

## What Was Done

### T37: Progress Equity Harness FIXED

**Problem:** Adding ATR_RANK=5 to `progress_equity_curves.rs` replaced the baseline turtle series instead of running alongside it. Prior baseline 221.5x was not comparable because the harness had been modified.

**Fix applied:**
1. `simulate_turtle_chandelier_equity()` now takes `use_atr_rank_filter: bool` parameter
2. Called TWICE: `simulate_turtle_chandelier_equity(..., false)` for baseline, `simulate_turtle_chandelier_equity(..., true)` for ATR_RANK=5 variant
3. Both CSV outputs: `snapshots/progress_equity_curves.csv` (baseline) and `snapshots/progress_equity_curves_atrrank5.csv` (variant)
4. `charts/plot_progress.py` updated to render both series on same log-scale chart + drawdown comparison panel
5. Markdown summaries separate for each variant

**Results (Base5, 2089 days, 2018–2026):**
- `turtle_baseline`: **108.1x | Sharpe 0.98** ✅ COMPARABLE to prior sessions
- `turtle_atrrank5`: **41.0x | Sharpe 0.87** — separate labeled series

**Key insight confirmed:** ATR_RANK=5 is time-period dependent. Validated +24% Sharpe on recent OOS walk-forward windows (Turtle-only, 2020-2026), but net negative on full history. The filter removes low-vol chop regimes but also removes historically profitable chop-regime breakouts. Properly belongs as a separate variant series, not a replacement for the baseline.

**Prior stale number (221.5x):** Was from a different harness run before ATR_RANK=5 was integrated. 108.1x is the current authoritative baseline.

**Discord media upload:** Failed twice with "Failed to optimize image." Chart committed to repo at `charts/progress_equity_curves_daily.png`.

---

## All Strategy Results (Base5, 2089 days)

| Strategy | Final Equity | Sharpe | Methodology |
|----------|-------------|--------|-------------|
| Turtle+Chandelier (baseline) | **108.1x** | **0.98** | daily compounded equity ✅ |
| DDBudget 3-Sleeve | 63.1x | 7.23 | milestone-aggregated, NOT comparable |
| A/D Momentum | 40.3x | 3.61 | fixed-hold daily equity |
| FactorSmallByDV | 16.9x | 2.05 | fixed-hold daily equity |
| Turtle ATR_RANK=5 | 41.0x | 0.87 | regime filter variant, separate series |

---

## Git Commit

`d1c28a87` — feat: progress_equity_curves T37 fix — baseline + ATR_RANK=5 as separate labeled series

---

## State of the Project

**Research loop:** CLOSED — all hyperparams validated, ATR_RANK=5 is last genuine candidate (pending live testnet)

**Live testnet:** CRITICAL BLOCKER — Noah's Binance testnet API keys (4+ weeks waiting)

**No new strategies tested.** Fix was a reporting infrastructure repair, not research.

**PLAN.md updates needed:** Mark T37 DONE, review S6 close_losers Turtle-only status, regime ATR integration status