# Memory — 2026-05-01

## Session: Critique + Plan Update | Kira | 2026-05-01 20:13 UTC

---

## BRUTAL HONESTY CRITIQUE

### 1. The Equity Number Is Broken and Nobody Is Fixing It

`daily_progress.csv` shows two conflicting Turtle equity figures:
- **progress_equity_curves.rs**: 108.1x, VL=2, CHAND_PERIOD=11 (WRONG — production is CHAND_PERIOD=7)
- **live_compatible_wf.rs**: Base5 aggregate 15.44x across 7 windows, VL=8

These are NOT comparable. One is full-history daily compounded equity. The other is window-aggregated compound return. The 108.1x figure is stale (uses wrong Chandelier period) and the VOL_LOOKBACK mismatch makes reconciliation impossible without re-running.

The plan has flagged this as CRITICAL since T36. It is still not fixed. Two sessions of "T38 partial complete" with no equity export.

**The real number we should be citing is unknown** until progress_equity_curves is synced to config.rs (CHAND_PERIOD=7, VL=8) and re-run.

### 2. The Research Loop Closed Prematurely — We Are Confirming, Not Discovering

Every session since 2026-04-25 has been a confirmation of settled results:
- HOLD_MAX 1..100 step 1 sweep (2026-04-30): HM=12 confirmed, no change
- ATR_ENTRY_MULT 201-value sweep (2026-04-29-30): EM=0.00 confirmed, no change  
- VOL_LOOKBACK 100-value sweep (2026-05-01): VL=96 "found," flagged as same-harness artifact like EP=24, still unresolved
- ATR_EMA [1..200] (2026-04-30): NULL confirmed at higher resolution, no change

The claim "research loop CLOSED" is accurate in that we've exhausted the testable parameter space on current mechanisms. But we haven't stopped running the harness — we keep re-running it at higher resolution and calling it new validation.

**The actual state:** Research IS closed. We should stop running hyperopts and build infrastructure.

### 3. S6 close_losers Was Validated Wrong — Then Called a "Candidate"

S6 (close_losers I=5) passed 9-universe validation 48/54 with +3.067 Sharpe improvement on the same-harness baseline. It was labeled a "candidate." But MEMORY.md shows it produces **0 trades on Turtle-only exit** — the live bot's actual execution path. The validation harness uses Chandelier dual-exit, which holds positions long enough for the 5-bar rebalancing check to fire. The live bot's Turtle ATR stop exits in 3-5 bars, before close_losers ever activates.

This is not a "candidate" — it's incompatible with the production strategy. It should be in GRAVEYARD.

### 4. DDBudget's 7.23 Sharpe Is Misleading — But We Already Knew This

The daily_progress.csv methodology label `milestone_aggregated_not_comparable` was added. Good. But the file still contains two rows for 2026-04-30 with `milestone_aggregated_inflated` and `milestone_aggregated_not_comparable` — inconsistent labels. A reader comparing 7.23 to Turtle's 0.98 concludes DDBudget is 7x better. It's not.

### 5. The 71.4% Pass Rate for Turtle-Only Live WF Is Stale

The live_compatible_wf (T38 partial) reports 45/63 pass (71.4%). This is computed AFTER the bug fix (commit 79a3442d) on the same data as the previous stale 34/54 result. It is the best number we have for Turtle-only OOS performance, but:

- The 34/54 fee-corrected result (T35, pre-bug-fix) used the OLD wrong-stop ATR mechanism
- The new 45/63 uses the corrected stop
- These are not comparable — we don't know if the 71.4% is better because the stop is fixed OR because it's a different harness run

**We need a clean head-to-head: same data, same params, old vs new stop logic, to measure the bug fix impact.**

### 6. The Biggest Blind Spot: We Have No Live Testnet. Everything Is a Simulation Upper Bound.

Four weeks and counting blocked on Noah's API keys. Every number we cite — Sharpe 0.98, 108.1x equity, 71.4% pass — is a simulation ceiling. The live execution reality (maker fill rates, slippage on volatile days, exchange API uptime, order book depth on DOGE) is completely unknown.

We have spent 4+ weeks optimizing parameters on a simulator we cannot verify. This is the definition of overfitting risk: more iterations on unverifiable data.

### 7. Donchian Entry — Higher Sharpe, Lower Pass Rate — Was Correctly Rejected

The Donchian result (86% pass, Sharpe 9.41 vs Turtle 100%, Sharpe 5.60) was correctly rejected via absolute guardrail (not relative improvement). This discipline is holding. Good.

### 8. VL=96 Is EP=24 All Over Again

On 2026-04-29, VL=8 was confirmed on the production parameter harness. On 2026-05-01 (same session cycle), VL=96 was "found" on the identical methodology. The EP=24 pattern is: found in the same session as CHAND_P=11, reverted after held-out confirmation. VL=96 is the same pattern. Keep VL=8 until T37 Base5-only confirmation.

---

## Most Important Unbuilt Ideas (From strategy-ideas.md Review)

### #1 — T38: Corrected Live Turtle-Only Walk-Forward (STALE — needs rebuild)
ALL Turtle-only metrics are stale after the 2026-05-01 bug fix. This is the single most critical unbuilt task. Without it, no Turtle-only metric is credible.

### #2 — Regime-Adaptive Exit (RAE)
Vol-conditional Chandelier multiplier. Prior attempt (uniform multiplier) was GRAVEYARD. RAE proposes conditional adjustment based on ATR percentile rank — a genuinely different mechanism. Small build, definitive test.

### #3 — T37: VL=96 vs VL=8 Base5-Only Confirmation
Small, one-harness run. Either confirms VL=96 is real (moves to production) or confirms VL=8 (removes noise from claims).

---

## Reporting Integrity Issues

1. **progress_equity_curves.rs**: Uses CHAND_PERIOD=11, not CHAND_PERIOD=7. Equity numbers from this harness may be unreliable. Flagged T36 since 2026-05-01, not fixed.
2. **VOL_LOOKBACK mismatch**: Progress harness uses VL=2, live bot uses VL=8. Equity from progress harness is not comparable to live bot equity.
3. **daily_progress.csv inconsistency**: Two different methodology labels for DDBudget in adjacent rows (2026-04-30). Should be consistent.
4. **S6 close_losers**: Wrongly kept as "candidate" — incompatible with live Turtle-only exit (0 trades). GRAVEYARD entry.

---

## What's Actually Working

1. **Anti-overfit discipline is solid.** Absolute guardrails (not relative improvement) correctly rejected Donchian sleeve, ATR entry filter, position scaling overlays.
2. **Bug fix was genuine and necessary.** The 2026-05-01 ATR buffer fix was a real structural error, found and fixed correctly.
3. **Fee accounting fixed.** T35 correctly identified and fixed the `(1-fee)/(1-fee)` cancellation bug.
4. **Held-out validation practice.** The EP=24 revert after held-out confirmation is the right behavior.

---

## Project State

Research: CLOSED (genuinely). Every testable mechanism exhausted.
Live testnet: BLOCKED (4+ weeks, Noah's Binance testnet API keys).
T38: INCOMPLETE (stale Turtle-only metrics).
Infrastructure debt: mounting (VOL mismatch, CHAND_PERIOD mismatch, S6 incompatibility).

**The single most important thing advancing the project is testnet API keys. Everything else is consolidation.**