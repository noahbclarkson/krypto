# Memory — 2026-04-30 00:41 UTC — Critique & Plan Update

**Session: Fifth Critique Cycle | Kira**

---

## Orient

- Git pulled: `f4b8b262` (T29 + T30), `250d73ed` (funding observer), `a9663671` (ATR entry filter), `fd38010c` (prior critique), `1a3dfe05` (ATR EMA 1-200)
- Discord: no new instructions
- Memory: fully read `memory/2026-04-29.md` (prior session critique) and `memory/2026-04-30.md` (T29/T30 completed)

---

## Brutal Critique: What's Actually Good vs. What We're Convincing Ourselves About

### ✅ Genuinely Good (Trust These)

1. **ATR EMA [1..200] × 9u × 54w = 10,800 runs = NULL.** Comprehensive. ATR_EMA=1 confirmed. Genuine null result — no smoothing improvement anywhere in the range.
2. **ATR_ENTRY_MULT current-params sweep: 201 values.** Candidate EM=0.94 (42/54, Sharpe 5.34) found vs baseline EM=0.00 (40/54, Sharpe 3.15). Anti-overfit discipline correctly kept production at EM=0.00 — this is real discipline, not just saying no.
3. **T29 funding observer: COMPLETE.** Live Binance public API. Regime NEAR NEUTRAL (-0.04% ann). No extremes detected. Correctly identified as the only idea testable without Noah's API keys.
4. **T30 mid-cap rejection: CORRECT.** MidCaps4 3/5 pass (60%) below 70%. Top5-only production universe confirmed. We didn't force this to pass.
5. **Equity bug: FIXED.** e55659e8 corrected off-by-one in progress_equity_curves.rs.
6. **Anti-overfitting track record is genuine.** EP=24 reverted, ATR_ENTRY_MULT=0.85 reverted, EP=43 rejected — we actually learn from failures.

### ⚠️ We're Convincing Ourselves On

1. **"Research loop closed" — but we just ran 10,800 more hyperopt runs.** ATR EMA [1..200] was confirmed NULL at range [1..30] on 2026-04-17. The 2026-04-29 [1..200] sweep re-confirmed the same result on the same harness. This is the 4th ATR EMA confirmation. The research loop isn't closed — it's in a confirmation spiral. Running the same sweep at higher resolution after you've already gotten NULL doesn't make the NULL more valid. It makes it look like we're afraid to stop.

2. **"ATR_ENTRY_MULT=0.00 confirmed" — by the same harness we keep re-running.** We keep saying the research loop is closed, then we find one more gap (ATR_ENTRY_MULT on current params), sweep 201 values, find EM=0.94 as the winner, correctly not change production (anti-overfit discipline), then declare the loop closed. The loop isn't closed — we're still searching. The honest statement is: we have one live candidate (EM=0.94) that needs held-out validation before promotion.

3. **T31 Donchian sleeve — "genuinely untested" for how many sessions now?** Plan.md has carried this since at least 2026-04-29. The Donchian result (2026-04-28) showed it wins +3.8 Sharpe but loses -14pp pass rate as a *replacement*. We correctly identified it as a *complement* candidate and then never tested it. Two sessions of "T31: genuinely untested" without building it is not rigor — it's avoidance.

### 🚨 Genuine Blind Spots

| Blind Spot | Severity | Status |
|-----------|----------|--------|
| **T31 Donchian sleeve: never built** | HIGH | "Genuinely untested" in PLAN for 2+ sessions. Not a hard problem — walk-forward harness exists. |
| **T32 Sharpe methodology mismatch: never fixed** | HIGH | DDBudget 7.24 vs Turtle 1.04 — incomparable. daily_progress.csv still misleads readers. Identified 2026-04-29, not fixed. |
| **Research loop: confirmation spiral, not closed** | MEDIUM | ATR EMA [1..200] just re-confirmed NULL on already-settled params. ATR_ENTRY_MULT 201-value sweep just ran on current params. We're optimizing confirmation, not discovering. |
| **3+ weeks without live testnet** | CRITICAL | Blocker unchanged. All metrics are simulation upper bounds. |
| **Funding observer: built but not observing** | MEDIUM | We built T29 but the last run was 2026-04-30 00:08. We don't have continuous monitoring running. |

---

## The 3 Most Promising Unbuilt Ideas

### 1. T31: Donchian as Portfolio Complement — GENUINELY UNTESTED, TWO SESSIONS OVERDUE
We identified Donchian (strictest breakout, all-time high) as a complement not replacement in the 2026-04-28 result. Never built the test.
- Hypothesis: Turtle(75%) + Donchian(25%) sleeve captures different regime dynamics. Donchian W04 (bear chop): Sharpe +15.7 vs Turtle +1.3.
- What to build: `examples/donchian_sleeve_walkforward.rs`. Turtle(75%) randomly selects which entry type fires per signal. Donchian fires only when `close > max(high)` (strictest).
- Reject if: Turtle Sharpe collapses >10% or pass rate drops >5pp.

### 2. T32: Sharpe Methodology Integrity Fix — CRITICAL, IDENTIFIED BUT IGNORED
DDBudget "Sharpe 7.24" vs Turtle "Sharpe 1.04" in `daily_progress.csv` are apples-to-oranges.
- DDBudget: milestone-aggregated (mean of per-window Sharpe, inflates ~7x)
- Turtle: daily compounded equity Sharpe (true risk-adjusted metric)
- Any reader compares 7.24 vs 1.04 and concludes DDBudget is 7x better — wrong.
- Fix: Add `sharpe_methodology` column to CSV, or recompute DDBudget on daily equity.
- This is the single most important reporting integrity fix. Low code change, high value.

### 3. Funding Observer: Move From "Built" to "Observing"
T29 is complete but we're not running it continuously.
- Build: `scripts/run_funding_observer.sh` (cron or loop). Poll every hour. Update CSV log.
- Value: 30 days of live funding rate observation with z-score/percentile context. Addresses bear/chop regime weakness qualitatively.
- After 30 days: we have enough data to say whether funding extremes correlate with drawdowns. Cannot be backtested — only live observation.

---

## Reports Directory: Metrics Real or Curve-Fit?

**What we have:**
- `daily_progress.csv` — 5 strategies with incomparable Sharpe values
- `progress_equity_curves.csv` — equity time series, correct for Turtle (221.5x, Sharpe 1.04)
- `snapshots/` — 80+ CSV files, many hyperopt confirmations

**Are they real?**
- Turtle daily equity Sharpe 1.04: REAL. Compounded from actual daily returns. No look-ahead.
- Walk-forward per-window Sharpe ~3.15 (current baseline): REAL methodology but inflated vs daily equity (not comparable to 1.04).
- DDBudget 7.24: CURVE-FIT or at minimum incomparable methodology. Milestone-aggregated Sharpe inflates by ~7x vs daily equity. The number exists in the same CSV as Turtle 1.04 as if they're peers — they're not.
- ATR EMA [1..200] = NULL: REAL result. Conservative interpretation — ATR smoothing doesn't help. 10,800 runs confirms it.
- ATR_ENTRY_MULT=0.94 candidate: MARGINAL. 42/54 vs 40/54 (+2 pass), Sharpe 5.34 vs 3.15 (+2.19). Improvement is real but needs held-out validation.

**Biggest curve-fit risk:** DDBudget. The milestone-aggregated Sharpe is structurally inflated. The 7.24 number is not comparable to anything else in that CSV.

---

## Biggest Blind Spot: We're In a Confirmation Spiral, Not a Discovery Loop

We keep re-running settled parameters with higher resolution. ATR_EMA was confirmed NULL at [1..30] on 2026-04-17. We ran [1..200] on 2026-04-29 and got the same result. ATR_ENTRY_MULT was confirmed null on stale params. We ran 201 values on current params and found EM=0.94 — which we correctly didn't promote. 

This is the opposite of "research loop closed." It's research loop spinning. The loop closes when you STOP RUNNING NEW HYPEROPTS and START BUILDING THE THINGS YOU SAID YOU'D BUILD.

We have three things flagged as genuinely untested for 2+ sessions:
1. T31 Donchian sleeve (complement, not replacement)
2. T32 Sharpe methodology fix
3. Funding observer → continuous monitoring

None of these are hard. None require Noah's API keys. All of them were in the plan 2 sessions ago.

---

## What We Got Right This Session

1. T29 (funding observer) — actually built and ran it correctly via live API
2. T30 (mid-cap rejection) — correctly rejected at 60% pass rate
3. ATR_ENTRY_MULT candidate identified correctly without promotion
4. Anti-overfit discipline held: EM=0.00 stayed in production despite real improvement signal

---

## Next 3 Execution Tasks (Priority Order)

**T31: Donchian Sleeve Walk-Forward** — HIGH PRIORITY, genuinely overdue
- Build `examples/donchian_sleeve_walkforward.rs`
- Turtle(75%) + Donchian(25%) portfolio complement on Base5 × 7 windows
- Reject if: Turtle Sharpe collapses >10% or pass rate drops >5pp
- This was in the plan 2 sessions ago. Build it.

**T32: Sharpe Methodology Integrity Fix** — HIGH PRIORITY, identified but ignored
- Add `sharpe_methodology` column to `reports/daily_progress.csv`
- Document: DDBudget uses milestone-aggregated (inflated), Turtle uses daily equity (honest)
- Or recompute DDBudget on daily equity for true comparison
- Low code change. Fix the misleading comparison.

**Funding Observer: Continuous Monitoring** — MEDIUM PRIORITY
- Build `scripts/run_funding_observer.sh` with cron or sleep loop
- Poll every hour, update CSV log
- After 30 days: sufficient data to assess correlation between funding extremes and drawdowns

---

## Anti-Spin Check

- Did I re-run any already-settled hyperopts? NO (this session — prior session ran ATR_EMA [1..200])
- Did I force any result to pass that shouldn't? NO. Mid-caps correctly rejected at 60%.
- Am I claiming confidence I don't have? NO. Research loop is NOT closed — it's in confirmation spiral.
- Did I complete the items I said were "next"? PARTIAL. T29 built. T31/T32 still not built from prior session.

---

## Commit

`git add -A && git commit -m "docs: critique and plan update" && git push`