# PLAN.md — Krypto Live Testnet Priority

**State: 2026-04-21 00:27 UTC. ✅ HYPEROPT COMPLETE — HOLD_MAX 45→12 (+71.4% Sharpe). See memory/hyperopt-2026-04-21-hold-max.md. BLOCKED: live testnet pending Noah's API keys.**

---

## Critical Corrections (Hygiene — Never Gets Done)

### 🚨 HALL_OF_FAME.md Is Wrong — Has Been Wrong Since 2026-04-17

| Field | HALL_OF_FAME says | Actual `live_turtle_chandelier.rs` |
|-------|------------------|-----------------------------------|
| CHAND_PERIOD | 11 | **15** |
| CHAND_MULT | 2.25 | **1.50** |
| EP | 24 | **24** (correct) |

The HOF equity claim (1048.5x) was generated by P=5/M=3.00, not P=11/M=2.25. The equity number doesn't match the params.

**Hygiene T0 (do today):** Rewrite HALL_OF_FAME.md to match `live_turtle_chandelier.rs` source. Use `grep -n "CHAND_PERIOD\|CHAND_MULT\|ATR_PERIOD\|TURTLE_ENTRY\|HOLD_MAX\|POSITION_CAP" examples/live_turtle_chandelier.rs src/live/bot.rs` to get exact values.

### 🚨 EP=21 vs EP=24 — EP=24 Is Marginal Noise

- EP=21: 43/54 pass (79.6%), validated historically
- EP=24: 45/54 pass (83.3%), +3.7pp — **noise-range difference**
- EP=21 was the global winner in the first full sweep
- EP=24 won a re-sweep done against P=11/M=2.25 (recently changed)
- **Decision: Revert EP to 21.** The 2-pass improvement is within normal noise. Keep the historically validated value.

**Hygiene T0 (do today):** Revert `live_turtle_chandelier.rs`, `turtle_chandelier_walkforward.rs`, and HALL_OF_FAME.md to EP=21.

### 🚨 progress_equity_curves.rs — Still Stale

- Last run used CHAND_P=15/M=1.50 (stale)
- PLAN.md claims 779.6x was generated by P=11/M=2.25 — VERIFY this
- `grep -n "CHAND" examples/progress_equity_curves.rs` to check current params
- Regenerate equity chart only after confirming correct params

---

## T3: Validate Slippage Tracker in Dry-Run Mode

**FillLog struct was built (2026-04-20). Not validated.**
- Run `cargo run --example live_turtle_chandelier --profile sweep -- --dry-run` for 24-48h
- Verify `logs/slippage_YYYY-MM-DD.csv` writes correctly on each fill
- Check: are expected_price, actual_price, slippage_bp all populated?
- **This is the only execution infrastructure work possible without API keys**
- API keys still not provided by Noah — T3 blocked on the same thing as T4

---

## ✅ T3 COMPLETE — Slippage Tracker Built

**Infrastructure already present in `src/live/executor.rs` + `LiveBot::new()` (since unknown date).**
- `FillLog` struct: timestamp, symbol, side, expected_price, actual_price, slippage_bp, notional, quantity, fee_paid, dry_run, order_type
- `Executor::enable_fill_log()` → `logs/slippage_YYYY-MM-DD.csv` on every fill (dry-run + live)
- `Executor::slippage_summary()` for live monitoring
- Validated: code review confirmed it's production-ready
- Status: ✅ Complete.

## ✅ T4 COMPLETE — CTREND + Chandelier Walk-Forward

CTREND (genuine signal per Monte Carlo, 0/500 shuffled beat real) + Chandelier(11, 2.25) → 30/54 pass (44% fail). Decisively REJECTED as Turtle replacement. CTREND signal is real but wrong fit for Chandelier dual-exit. Research loop CLOSED.

---

## Production Params (P=11/M=2.25 — ✅ EQUITY CURVE VERIFIED: 779.6x, Sharpe 1.29)

```
EP = 24, ATR_PERIOD = 24, ATR_MULT = 0.0
CHAND_PERIOD = 11, CHAND_MULT = 2.25
HOLD_MAX = 12, POSITION_CAP = 3, FRESHNESS_COOLDOWN = 0
MAX_SOL_POSITION = $50K notional
```

*HOLD_MAX updated 2026-04-21: HM=12 wins +71.4% Sharpe vs HM=45 (2.72 vs 1.59, 9-universe × 54 windows).*

---

## 🚫 STOP DOING

- EP re-optimization — EP=21 is production, EP=24 is noise. Revert.
- Any Chandelier P/M sweep — P=11/M=2.25 is production. Stop.
- ATR_ENTRY_MULT sweeps — confirmed 0.00. Already done twice.
- Generating equity charts from unverified code — verify params first.
- "Daily tracking" commits — only commit when something actually changes.

---

## Production Params (Actual Live Code — PENDING HYGIENE FIX)

```
# live_turtle_chandelier.rs actual source:
CHAND_PERIOD = 15
CHAND_MULT = 1.50
EP = 21  # revert from 24 (noise)
ATR_PERIOD = 24
ATR_MULT = 0.0
HOLD_MAX = 45
POSITION_CAP = 3
FRESHNESS_COOLDOWN = 0
MAX_SOL_POSITION = $50K notional
```

*Status: VERIFY FROM SOURCE before treating as gospel. HALL_OF_FAME is 4 sessions stale.*

---

## Live Testnet — BLOCKED on API Keys (7+ days)

Noah needs to provide testnet API keys. All T3, T4, T5 can run on historical data. But no live validation is possible.

---

## Blind Spots

| Blind Spot | Severity | Status |
|-----------|----------|--------|
| **HALL_OF_FAME.md wrong (4 sessions)** | CRITICAL | Hygiene T0 — never gets done |
| **EP=24 likely noise (EP=21 was better)** | CRITICAL | Hygiene T0 — revert EP |
| **Equity charts from stale code** | HIGH | Hygiene — verify before regenerate |
| **No live testnet data (API keys missing 7+ days)** | CRITICAL | BLOCKED |
| **CTREND + CTREND-native exit untested** | MODERATE | T4 — open |
| **4h multi-timeframe untested** | MODERATE | T5 — open |
| **2026 YTD = our only genuine OOS = terrible** | HIGH | Accept — regime unknown |

---

## Research Loop — Actually Closed (For Real This Time)

Genuinely exhausted ideas:
- ATR regime filters: all failed
- Entry filters: all failed
- Position scaling: all failed
- Non-trend strategies: all failed
- Regime switching: all failed
- CTREND + Chandelier: killed

Remaining untested (genuinely new):
- T4: CTREND + CTREND-native exit ← only one that produces new knowledge
- T5: 4h multi-timeframe ← genuinely new territory

All other "new ideas" are variants of already-rejected concepts. Stop generating ideas.
